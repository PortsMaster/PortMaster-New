There's a list of the existing global variables the game uses,
And a possibile explanation of what they do, they could be documented wrong tho !

g_calpha (number) [defined in api.lua] -> ?
print_alpha (number/nil) [logic in api.lua] -> The opacity of printing text