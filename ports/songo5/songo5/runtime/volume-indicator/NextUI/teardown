#!/bin/sh

PID_FILE="/tmp/vol_listener.pid"

if [ -f "$PID_FILE" ]; then
    PID=$(cat "$PID_FILE")
    echo "Stopping vol_listener PID=$PID..."
    kill "$PID"
    rm -f "$PID_FILE"
else
    echo "No vol_listener PID file found."
fi