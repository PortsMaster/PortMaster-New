#!/bin/sh

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
VOL_LISTENER="$SCRIPT_DIR/vol_listener_v2"
PID_FILE="/tmp/vol_listener.pid"

# Start in background
$VOL_LISTENER &

# Save PID
echo $! > "$PID_FILE"

echo "vol_listener started, PID=$(cat $PID_FILE)"