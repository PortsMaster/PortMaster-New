#!/bin/sh

DIR="$(dirname "$(readlink -f "$0")")"
exec "$DIR/../../zig/zig" c++ -target aarch64-linux-gnu.2.35 -mcpu=baseline \
    -std=gnu++14 \
    -D_LIBCPP_ENABLE_CXX17_REMOVED_UNARY_BINARY_FUNCTION=1 \
    "$@"
