--- original/button.lua
+++ modified/button.lua
@@ -5,11 +5,11 @@
 vec4 effect( vec4 color, Image texture, vec2 texture_coords, vec2 screen_coords ){
   vec4 pixel = Texel(texture, texture_coords );//This is the current pixel color
   vec4 other = vec4(52.0/255.0, 0.0, 5.0/255.0, percent);
-  number a = (1-other.a)*other.a+pixel.a;
+  number a = (1.0-other.a)*other.a+pixel.a;
   return vec4(
-    ( (1-other.a) * pixel.a*pixel.r + other.a*other.r )/a,
-    ( (1-other.a) * pixel.a*pixel.g + other.a*other.g )/a,
-    ( (1-other.a) * pixel.a*pixel.b + other.a*other.b )/a, 
+    ( (1.0-other.a) * pixel.a*pixel.r + other.a*other.r )/a,
+    ( (1.0-other.a) * pixel.a*pixel.g + other.a*other.g )/a,
+    ( (1.0-other.a) * pixel.a*pixel.b + other.a*other.b )/a, 
     min(a, pixel.a)
   );
 }
--- original/components/entity.lua
+++ modified/components/entity.lua
@@ -7,8 +7,9 @@
 
 Entity = {}
 
-local empty_fn = function(self)
-end
+-- Performance: Localize table functions and empty functions
+local t_insert, t_remove = table.insert, table.remove
+local empty_fn = function(self) end
 
 function Entity:new(o)
     o = o or {}
@@ -27,34 +28,49 @@
     if not t.name then
         t.name = t
     end
+
+    -- FIX: set ent BEFORE t:new() so init() sees a valid self.ent immediately.
+    -- The original set this before new() too, so this is safe.
     comp.ent = self
+
     if not self[t.name] then
         self[t.name] = comp
     end
-    table.insert(self.comps, t:new(comp))
-    if not comp.update then
-        comp.update = empty_fn
-    end
-    if not comp.render then
-        comp.render = empty_fn
-    end
+
+    -- t:new(comp) calls setmetatable(comp, t) and returns the same comp table.
+    -- We insert the return value but comp and t:new(comp) are the same object.
+    t_insert(self.comps, t:new(comp))
+
+    -- Stubs applied after new() on comp, exactly as the original does.
+    -- comp is the same table that new() mutated in place.
+    if not comp.update then comp.update = empty_fn end
+    if not comp.render then comp.render = empty_fn end
+
     return self, comp
 end
 
 function Entity:remove_comp(t, comp)
-    _ = comp.clean_up and comp:clean_up()
-    if self[t.name] == comp then
-        self[t.name] = nil
+    if comp.clean_up then comp:clean_up() end
+    
+    local name = t.name or t
+    if self[name] == comp then
+        self[name] = nil
     end
-    table.remove(self.comps, tablefind(self.comps, comp))
+    
+    -- tablefind should be a localized global if possible for speed
+    local idx = tablefind(self.comps, comp)
+    if idx then t_remove(self.comps, idx) end
 end
 
+-- Fast insertion for pre-made components
 function Entity:insert_comp(comp)
     comp.ent = self
-    table.insert(self.comps, comp)
+    t_insert(self.comps, comp)
     return self, comp
 end
 
+-- Retained from original: registers a component by type key without calling
+-- t:new() again. Used by some objects that construct the component themselves.
 function Entity:make_comp(t, comp)
     comp.ent = self
     if not self[t] then
@@ -65,33 +81,33 @@
 end
 
 function Entity:clean_up()
-    for i, comp in ipairs(self.comps) do
-        _ = comp.clean_up and comp:clean_up()
+    local comps = self.comps
+    for i = 1, #comps do
+        local c = comps[i]
+        if c.clean_up then c:clean_up() end
     end
 end
 
-
+-- PERFORMANCE: Using numeric for loops instead of ipairs
+-- Numeric loops are significantly faster in LuaJIT (LÖVE's engine)
 Entity.update = profile('entity.update', function(self)
-    for i, comp in ipairs(self.comps) do
-        comp:update()
+    local comps = self.comps
+    for i = 1, #comps do
+        comps[i]:update()
     end
-
-    -- if debug_mode and clicked() and not self.no_debug and mouse_cam_inside(self.x, self.y, 16, 16) then
-    --     debug_entity = self
-    --     debug_entity_show = true
-    -- end
 end, true)
 
 function Entity:debug()
-    for i, comp in ipairs(self.comps) do
-        if comp.debug then
-            comp:debug()
-        end
+    local comps = self.comps
+    for i = 1, #comps do
+        local c = comps[i]
+        if c.debug then c:debug() end
     end
 end
 
 function Entity:render()
-    for i, comp in ipairs(self.comps) do
-        comp:render()
+    local comps = self.comps
+    for i = 1, #comps do
+        comps[i]:render()
     end
-end
+end
\ No newline at end of file
--- original/components/object.lua
+++ modified/components/object.lua
@@ -1,3 +1,12 @@
+-- OPTIMIZATION: Local caching of frequently used functions
+local pairs = pairs
+local ipairs = ipairs
+local floor = math.floor
+local ceil = math.ceil
+local abs = math.abs
+local insert = table.insert
+local remove = table.remove
+
 Object = {}
 
 local object_list = {}
@@ -722,5 +731,4 @@
         end)
     end
 end
-register_collision('solid', 'player', solid_collision)
-
+register_collision('solid', 'player', solid_collision)
\ No newline at end of file
--- original/components/sprite.lua
+++ modified/components/sprite.lua
@@ -1,244 +1,250 @@
-Sprite = {offset_x=0, offset_y=0}
-
-local images = {}
-local quads = {}
-
-function hash(width, height, off_x, off_y, limit_x, limit_y)
-    return tostring(width)..'-'..tostring(height)..'-'..tostring(off_x)..'-'..tostring(off_y)..'-'..tostring(limit_x)..'-'..tostring(limit_y)
-end
-
-function create_sprite(file, width, height, off_x, off_y, limit_x, limit_y)
-    if quads[file] and quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)] then
-        return images[file], quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)], width ~= -1 and width or images[file]:getWidth(), height ~= -1 and height or images[file]:getHeight()
-    end
-
-    local q = {}
-    local image = images[file] or love.graphics.newImage(file)
-
-    width = width ~= -1 and width or image:getWidth()
-    height = height ~= -1 and height or image:getHeight()
-
-    local yc = 0
-    local xc = 0
-    for y = off_y, image:getHeight() - height, height do
-        for x = off_x, image:getWidth() - width, width do
-            table.insert(q, love.graphics.newQuad(x, y, width, height, image:getDimensions()))
-            xc = xc + 1
-            if xc == limit_x then
-                break
-            end
-        end
-        yc = yc + 1
-        if yc == limit_y then
-            break
-        end
-    end
-    images[file] = image
-    quads[file] = quads[file] or {}
-    quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)] = q
-
-    return image, q, width, height
-end
-
-local lookup = {}
-PAL_LOOKUP = lookup
-
-lookup[0] = {
-[255] = {255/255, 220/255, 194/255},
-[195] = {255/255, 159/255, 0/255},
-[168] = {208/255, 114/255, 44/255},
-[136] = {242/255, 57/255, 24/255},
-[92] = {33/255, 57/255, 103/255},
-[54] = {146/255, 88/255, 20/255},
-[33] = {166/255, 40/255, 0/255},
-[0] = {38/255, 9/255, 0/255},
-[0] = {38/255, 9/255, 0/255},
-[242] = {255/255, 220/255, 194/255},
-[189] = {255/255, 159/255, 0/255},
-[161] = {21/255, 44/255, 44/255},
-[130] = {242/255, 57/255, 24/255},
-[87] = {33/255, 57/255, 103/255},
-[48] = {146/255, 20/255, 20/255},
-[26] = {65/255, 10/255, 2/255},
-[0] = {38/255, 9/255, 0/255},
-[0] = {38/255, 9/255, 0/255},
-}
-
-lookup[1] = {
-[255] = {234/255, 183/255, 169/255},
-[195] = {255/255, 150/255, 0/255},
-[168] = {228/255, 71/255, 122/255},
-[136] = {48/255, 25/255, 25/255},
-[92] = {64/255, 45/255, 45/255},
-[54] = {154/255, 26/255, 67/255},
-[33] = {32/255, 7/255, 7/255},
-[0] = {0/255, 0/255, 0/255},
-[0] = {0/255, 0/255, 0/255},
-[242] = {242/255, 242/255, 242/255},
-[189] = {255/255, 150/255, 0/255},
-[161] = {58/255, 22/255, 20/255},
-[130] = {197/255, 54/255, 2/255},
-[87] = {90/255, 18/255, 60/255},
-[48] = {138/255, 25/255, 5/255},
-[26] = {37/255, 14/255, 14/255},
-[0] = {0/255, 0/255, 0/255},
-[0] = {0/255, 0/255, 0/255},
-}
-
-lookup[2] = {
-[255] = {255/255, 248/255, 225/255},
-[195] = {255/255, 150/255, 0/255},
-[168] = {138/255, 90/255, 33/255},
-[136] = {165/255, 62/255, 43/255},
-[92] = {52/255, 47/255, 101/255},
-[54] = {96/255, 66/255, 31/255},
-[33] = {96/255, 26/255, 44/255},
-[0] = {3/255, 1/255, 32/255},
-[0] = {3/255, 1/255, 32/255},
-[242] = {255/255, 215/255, 93/255},
-[189] = {255/255, 158/255, 18/255},
-[161] = {101/255, 23/255, 60/255},
-[130] = {244/255, 107/255, 4/255},
-[87] = {138/255, 1/255, 38/255},
-[48] = {197/255, 54/255, 2/255},
-[26] = {65/255, 2/255, 32/255},
-[0] = {3/255, 1/255, 32/255},
-[0] = {3/255, 1/255, 32/255},
-}
-
-lookup[3] = {
-[255] = {255/255, 255/255, 255/255},
-[195] = {255/255, 177/255, 0/255},
-[168] = {126/255, 128/255, 67/255},
-[136] = {255/255, 104/255, 0/255},
-[92] = {27/255, 85/255, 69/255},
-[54] = {58/255, 58/255, 24/255},
-[33] = {138/255, 30/255, 0/255},
-[0] = {3/255, 21/255, 0/255},
-[0] = {3/255, 21/255, 0/255},
-[242] = {242/255, 242/255, 242/255},
-[189] = {255/255, 177/255, 0/255},
-[161] = {46/255, 58/255, 6/255},
-[130] = {103/255, 101/255, 33/255},
-[87] = {88/255, 53/255, 122/255},
-[48] = {79/255, 45/255, 96/255},
-[26] = {33/255, 18/255, 48/255},
-[0] = {3/255, 21/255, 0/255},
-[0] = {3/255, 21/255, 0/255},
-}
-
-
-local actual_lookup = {}
-for i=0,255 do
-    table.insert(actual_lookup, {0, 0, 0})
-end
-
-for k, v in pairs(lookup[PAL]) do
-    actual_lookup[k + 1] = v
-end
-
-change_shader = love.graphics.newShader[[
-    extern vec3 lookup[256];
-
-    vec4 effect(vec4 color, Image texture, vec2 texture_coords, vec2 pixel_coords) {
-      vec4 pixel = Texel(texture, texture_coords);
-      return vec4(lookup[int(pixel.b * color.b * 255)], color.a * pixel.a);
-    }]]
-change_shader:send("lookup", unpack(actual_lookup))
-
-function set_pal(pal)
-    pal = math.floor(pal)
-    SAVE.PAL = pal
-    PAL = pal
-    for k, v in pairs(lookup[PAL]) do
-        actual_lookup[k + 1] = v
-    end
-    change_shader:send("lookup", unpack(actual_lookup))
-end
-
-function Sprite:new(o)
-    o = o or {}
-    setmetatable(o, self)
-    self.__index = self
-
-    o.frame = o.frame or 0
-    o.off_x = o.off_x or 0
-    o.off_y = o.off_y or 0
-    o.limit_x = o.limit_x or 0
-    o.limit_y = o.limit_y or 0
-    o.rot = o.rot or 0
-    o.image, o.quads, o.w, o.h = create_sprite(o.file, o.w, o.h, o.off_x, o.off_y, o.limit_x, o.limit_y)
-    o.shader = not o.noshader and change_shader or nil
-    return o
-end
-
-function Sprite:animate(frames, callback)
-    self.f = 0
-    self.frames = frames
-    self.callback = callback
-    self.frame = self.frames[1]
-end
-
-function Sprite:animate_wait(frames)
-    self.callback = function()
-        signal('done', self)
-    end
-    self:animate(frames, self.callback)
-    waitSignal('done', self)
-end
-
-function Sprite:update()
-    if self.frames then
-        self.f = self.f + 1
-        if self.f >= (#self.frames) * (self.anim_length or 6) then
-            if not self.continue then
-                self.frames = nil
-            else
-                self.f = 0
-            end
-            if self.callback then
-                self.callback()
-            end
-        else
-            self.frame = self.frames[math.floor(self.f / (self.anim_length or 6)) + 1]
-        end
-    end
-end
-
-current_shader = nil
-function Sprite:render()
-    if self.frame < 0 then
-        return
-    end
-    if current_shader ~= self.shader then
-        love.graphics.setShader(self.shader)
-        current_shader = self.shader
-    end
-    -- if self.shader then
-
-    --     love.graphics.setShader(self.shader)
-    --     if self.shader_data then
-    --         for k, v in pairs(self.shader_data) do
-    --             self.shader:send(k, v)
-    --         end
-    --     end
-    -- end
-
-    local xs = 0
-    local ys = 0
-    -- local xs = self.ent[Object] and self.ent[Object].x_remainder or 0
-    -- local ys = self.ent[Object] and self.ent[Object].y_remainder or 0
-
-    love.graphics.setColor(1, 1, 1, self.alpha or 1)
-    love.graphics.draw(
-        self.image, 
-        self.quads[self.frame + 1], 
-        xs+self.ent.x+(self.offset_x)*(self.flip and -1 or 1),
-        ys+self.ent.y+(self.offset_y)*(self.flip_y and -1 or 1),
-        self.rot,
-        self.flip and -1 or 1, 
-        self.flip_y and -1 or 1, 
-        self.origin_x or self.w/2, 
-        self.origin_y or self.h/2
-    )
-end
\ No newline at end of file
+Sprite = {offset_x=0, offset_y=0}
+
+local images = {}
+local quads = {}
+
+function hash(width, height, off_x, off_y, limit_x, limit_y)
+    return tostring(width)..'-'..tostring(height)..'-'..tostring(off_x)..'-'..tostring(off_y)..'-'..tostring(limit_x)..'-'..tostring(limit_y)
+end
+
+function create_sprite(file, width, height, off_x, off_y, limit_x, limit_y)
+    if quads[file] and quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)] then
+        return images[file], quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)], width ~= -1 and width or images[file]:getWidth(), height ~= -1 and height or images[file]:getHeight()
+    end
+
+    local q = {}
+    local image = images[file] or love.graphics.newImage(file)
+
+    width = width ~= -1 and width or image:getWidth()
+    height = height ~= -1 and height or image:getHeight()
+
+    local yc = 0
+    local xc = 0
+    for y = off_y, image:getHeight() - height, height do
+        for x = off_x, image:getWidth() - width, width do
+            table.insert(q, love.graphics.newQuad(x, y, width, height, image:getDimensions()))
+            xc = xc + 1
+            if xc == limit_x then
+                break
+            end
+        end
+        yc = yc + 1
+        if yc == limit_y then
+            break
+        end
+    end
+    images[file] = image
+    quads[file] = quads[file] or {}
+    quads[file][hash(width, height, off_x, off_y, limit_x, limit_y)] = q
+
+    return image, q, width, height
+end
+
+local lookup = {}
+PAL_LOOKUP = lookup
+
+lookup[0] = {
+[255] = {255/255, 220/255, 194/255},
+[195] = {255/255, 159/255, 0/255},
+[168] = {208/255, 114/255, 44/255},
+[136] = {242/255, 57/255, 24/255},
+[92] = {33/255, 57/255, 103/255},
+[54] = {146/255, 88/255, 20/255},
+[33] = {166/255, 40/255, 0/255},
+[0] = {38/255, 9/255, 0/255},
+[0] = {38/255, 9/255, 0/255},
+[242] = {255/255, 220/255, 194/255},
+[189] = {255/255, 159/255, 0/255},
+[161] = {21/255, 44/255, 44/255},
+[130] = {242/255, 57/255, 24/255},
+[87] = {33/255, 57/255, 103/255},
+[48] = {146/255, 20/255, 20/255},
+[26] = {65/255, 10/255, 2/255},
+[0] = {38/255, 9/255, 0/255},
+[0] = {38/255, 9/255, 0/255},
+}
+
+lookup[1] = {
+[255] = {234/255, 183/255, 169/255},
+[195] = {255/255, 150/255, 0/255},
+[168] = {228/255, 71/255, 122/255},
+[136] = {48/255, 25/255, 25/255},
+[92] = {64/255, 45/255, 45/255},
+[54] = {154/255, 26/255, 67/255},
+[33] = {32/255, 7/255, 7/255},
+[0] = {0/255, 0/255, 0/255},
+[0] = {0/255, 0/255, 0/255},
+[242] = {242/255, 242/255, 242/255},
+[189] = {255/255, 150/255, 0/255},
+[161] = {58/255, 22/255, 20/255},
+[130] = {197/255, 54/255, 2/255},
+[87] = {90/255, 18/255, 60/255},
+[48] = {138/255, 25/255, 5/255},
+[26] = {37/255, 14/255, 14/255},
+[0] = {0/255, 0/255, 0/255},
+[0] = {0/255, 0/255, 0/255},
+}
+
+lookup[2] = {
+[255] = {255/255, 248/255, 225/255},
+[195] = {255/255, 150/255, 0/255},
+[168] = {138/255, 90/255, 33/255},
+[136] = {165/255, 62/255, 43/255},
+[92] = {52/255, 47/255, 101/255},
+[54] = {96/255, 66/255, 31/255},
+[33] = {96/255, 26/255, 44/255},
+[0] = {3/255, 1/255, 32/255},
+[0] = {3/255, 1/255, 32/255},
+[242] = {255/255, 215/255, 93/255},
+[189] = {255/255, 158/255, 18/255},
+[161] = {101/255, 23/255, 60/255},
+[130] = {244/255, 107/255, 4/255},
+[87] = {138/255, 1/255, 38/255},
+[48] = {197/255, 54/255, 2/255},
+[26] = {65/255, 2/255, 32/255},
+[0] = {3/255, 1/255, 32/255},
+[0] = {3/255, 1/255, 32/255},
+}
+
+lookup[3] = {
+[255] = {255/255, 255/255, 255/255},
+[195] = {255/255, 177/255, 0/255},
+[168] = {126/255, 128/255, 67/255},
+[136] = {255/255, 104/255, 0/255},
+[92] = {27/255, 85/255, 69/255},
+[54] = {58/255, 58/255, 24/255},
+[33] = {138/255, 30/255, 0/255},
+[0] = {3/255, 21/255, 0/255},
+[0] = {3/255, 21/255, 0/255},
+[242] = {242/255, 242/255, 242/255},
+[189] = {255/255, 177/255, 0/255},
+[161] = {46/255, 58/255, 6/255},
+[130] = {103/255, 101/255, 33/255},
+[87] = {88/255, 53/255, 122/255},
+[48] = {79/255, 45/255, 96/255},
+[26] = {33/255, 18/255, 48/255},
+[0] = {3/255, 21/255, 0/255},
+[0] = {3/255, 21/255, 0/255},
+}
+
+-- Create palette texture from lookup table
+local function create_palette_texture(palette_index)
+    local imageData = love.image.newImageData(256, 1)
+    
+    -- Initialize all pixels to black
+    for i = 0, 255 do
+        imageData:setPixel(i, 0, 0, 0, 0, 1)
+    end
+    
+    -- Set colors from lookup table
+    for k, v in pairs(lookup[palette_index]) do
+        imageData:setPixel(k, 0, v[1], v[2], v[3], 1)
+    end
+    
+    local texture = love.graphics.newImage(imageData)
+    texture:setFilter('nearest', 'nearest')
+    texture:setWrap('clamp', 'clamp')
+    return texture
+end
+
+-- Create initial palette texture
+local palette_texture = create_palette_texture(PAL or 0)
+
+-- Shader using texture-based lookup (compatible with OpenGL ES 2.0+)
+change_shader = love.graphics.newShader[[
+    extern Image palette;
+
+    vec4 effect(vec4 color, Image texture, vec2 texture_coords, vec2 pixel_coords) {
+        vec4 pixel = Texel(texture, texture_coords);
+        float index = pixel.b * color.b;
+        vec3 newColor = Texel(palette, vec2(index, 0.5)).rgb;
+        return vec4(newColor, color.a * pixel.a);
+    }
+]]
+change_shader:send("palette", palette_texture)
+
+function set_pal(pal)
+    pal = math.floor(pal)
+    SAVE.PAL = pal
+    PAL = pal
+    
+    -- Recreate palette texture
+    palette_texture = create_palette_texture(pal)
+    change_shader:send("palette", palette_texture)
+end
+
+function Sprite:new(o)
+    o = o or {}
+    setmetatable(o, self)
+    self.__index = self
+
+    o.frame = o.frame or 0
+    o.off_x = o.off_x or 0
+    o.off_y = o.off_y or 0
+    o.limit_x = o.limit_x or 0
+    o.limit_y = o.limit_y or 0
+    o.rot = o.rot or 0
+    o.image, o.quads, o.w, o.h = create_sprite(o.file, o.w, o.h, o.off_x, o.off_y, o.limit_x, o.limit_y)
+    o.shader = not o.noshader and change_shader or nil
+    return o
+end
+
+function Sprite:animate(frames, callback)
+    self.f = 0
+    self.frames = frames
+    self.callback = callback
+    self.frame = self.frames[1]
+end
+
+function Sprite:animate_wait(frames)
+    self.callback = function()
+        signal('done', self)
+    end
+    self:animate(frames, self.callback)
+    waitSignal('done', self)
+end
+
+function Sprite:update()
+    if self.frames then
+        self.f = self.f + 1
+        if self.f >= (#self.frames) * (self.anim_length or 6) then
+            if not self.continue then
+                self.frames = nil
+            else
+                self.f = 0
+            end
+            if self.callback then
+                self.callback()
+            end
+        else
+            self.frame = self.frames[math.floor(self.f / (self.anim_length or 6)) + 1]
+        end
+    end
+end
+
+current_shader = nil
+function Sprite:render()
+    if self.frame < 0 then
+        return
+    end
+    if current_shader ~= self.shader then
+        love.graphics.setShader(self.shader)
+        current_shader = self.shader
+    end
+
+    local xs = 0
+    local ys = 0
+
+    love.graphics.setColor(1, 1, 1, self.alpha or 1)
+    love.graphics.draw(
+        self.image, 
+        self.quads[self.frame + 1], 
+        xs+self.ent.x+(self.offset_x)*(self.flip and -1 or 1),
+        ys+self.ent.y+(self.offset_y)*(self.flip_y and -1 or 1),
+        self.rot,
+        self.flip and -1 or 1, 
+        self.flip_y and -1 or 1, 
+        self.origin_x or self.w/2, 
+        self.origin_y or self.h/2
+    )
+end
--- original/main.lua
+++ modified/main.lua
@@ -1,611 +1,570 @@
-local profile_data = {}
-
-function profile_start()
-    profile_data = {}
-end
-
-function profile_report()
-    print('-----------------')
-    for k,v in pairs(profile_data) do
-        print(k, v)
-    end
-    print('-----------------')
-end
-
-local starts = {}
--- function profile_time_start(name)
---     starts[name] = os.clock()
--- end
-
--- function profile_time_end(name)
---     local start = starts[name]
---     local dif = os.clock() - start
---     if not profile_data[name] then
---         profile_data[name] = 0
---     end
-
---     profile_data[name] = profile_data[name] + dif
--- end
-
-function profile(name, fn, stop)
-    return fn
-    
-    -- if stop then
-    --     return fn
-    -- end
-    -- return function(a, b, c, d, e, f, g, h, i, j)
-    --     local start = os.clock()
-    --     local ret = fn(a, b, c, d, e, f, g, h, i, j)
-    --     local dif = os.clock() - start
-    --     if not profile_data[name] then
-    --         profile_data[name] = 0
-    --     end
-
-    --     profile_data[name] = profile_data[name] + dif
-    --     return ret;
-    -- end
-end
-
-local sfx = {}
-local last_played = {}
-local muted = {}
-
-function mute(f)
-    muted[f] = true
-end
-
-function unmute(f)
-    muted[f] = false
-end
-
-
-function play_sound(sound, pitch, vol, loop)
-    pitch = pitch or 0
-    if muted[sound] then
-        return
-    end
-    local s = sfx[sound]
-    if not s then
-        s = ripple.newSound(love.audio.newSource('res/sfx/'..sound..'.wav', 'static'), {
-            loop = loop,
-        })
-        sfx[sound] = s
-        last_played[sound] = -1
-    end
-    if last_played[sound] == SAVE.TICK_COUNT then
-        return
-    end
-    last_played[sound] = SAVE.TICK_COUNT
-    return s:play({
-        volume = SETTINGS.sfx_volume/100 * (vol or 1),
-        pitch = 2^(pitch/12),
-    })
-end
-
-song = nil
-song_name = nil
-function play_song(sound, opts)
-    if sound ~= 'TitleTheme.ogg' then
-        SAVE.SONG = sound
-    end
-    print(song_name, sound)
-    if song_name == sound then
-        return
-    end
-    song_name = sound
-    if song then
-        song:stop()
-    end
-    song = nil
-    if not sound then
-        return
-    end
-    local s = sfx[sound]
-    if not s then
-        s = ripple.newSound(love.audio.newSource('res/song/'..sound, 'static'), opts)
-        sfx[sound] = s
-        last_played[sound] = -1
-    end
-    print('huh?')
-    if last_played[sound] == SAVE.TICK_COUNT then
-        return
-    end
-    last_played[sound] = SAVE.TICK_COUNT
-    song = s:play({
-        volume = SETTINGS.music_volume/100
-    })
-    print(song._fadeVolume)
-end
-
-function new_game(best_times)
-    local TOP_LEVEL = 1
-    if SAVE then
-        TOP_LEVEL = SAVE.TOP_LEVEL
-    end
-    SAVE = {
-        LEVEL_NUM=1,
-        DEATHS=0,
-        COINS_MISSED=0,
-        MOTH_JUMP=MOTH_JUMP,
-        FLIP=FLIP,
-        PICK=PICK,
-        TICK_COUNT=0,
-        START_TICK=0,
-        SCORE=0,
-        SEEN_WAll_KEY=false,
-        COINS = {},
-        TIMES = {},
-        BEST_TIMES = best_times or {},
-        TOP_LEVEL=TOP_LEVEL,
-        GREED=0,
-        BEAT={},
-        OPENED={},
-        SONG='LockedUp.wav',
-    }
-
-    if set_pal then
-        set_pal(0)
-        play_song(nil)
-    end
-end
-new_game()
-
-json = require 'json'
-require 'util'
-
-local contents = love.filesystem.read('save.json')
-if contents then
-    local save = json.decode(contents)
-    for k,v in pairs(save) do
-        SAVE[k] = v
-    end
-    SAVE.OPENED = SAVE.OPENED or {}
-    print(inspect(SAVE))
-end
-
-require 'strict'
-
-
-scene = nil
-next_scene = nil
-main_canvas = nil
-small_canvas = nil
-debug_mode = nil
-_ = nil
-
-input_details = nil
-font = nil
-
-
-require 'config'
-
-require 'easing'
-require 'threads'
-
-ripple = require 'ripple'
-
-require 'components/entity'
-require 'events'
-require 'sequencer'
-require 'button'
-require 'slider'
-
-Input = require 'Input'
-
-require 'scenes/scene'
-require 'scenes/settings_scene'
-require 'scenes/game_scene'
-require 'scenes/main_menu'
-require 'scenes/break_scene'
-require 'scenes/level_editor'
-require 'scenes/level_editor_select'
-
-require 'Shake'
-
-function love.setUpdateTimestep(ts)
-  love.updateTimestep = ts
-end
-
-function love.run()
-
-  math.randomseed( os.time() )
-  math.random() math.random()
-  
-  if love.load then love.load(arg) end
-  
-  local dt = 0
-  local accumulator = 0
-  
-  -- Main loop
-  while true do
-  
-    -- Process events.
-    if love.event then
-      love.event.pump()
-      for e,a,b,c,d in love.event.poll() do
-        if e == "quit" then
-          if not love.quit or not love.quit() then
-            if love.audio then love.audio.stop() end
-            return
-          end
-        end
-        love.handlers[e](a,b,c,d)
-      end
-    end
-        
-    -- Update dt for any uses during this timestep of love.timer.getDelta
-    if love.timer then 
-      love.timer.step()
-      dt = love.timer.getDelta()
-    end
-
-    local fixedTimestep = love.updateTimestep
-    
-    if fixedTimestep then       
-      -- see http://gafferongames.com/game-physics/fix-your-timestep  
-      
-      if dt > 0.25 then      
-        dt = 0.25 -- note: max frame time to avoid spiral of death
-      end     
-      
-      accumulator = accumulator + dt
-      --_logger:debug("love.run - acc=%f fts=%f", accumulator, fixedTimestep) 
-
-      while accumulator >= fixedTimestep do
-        if love.update then love.update(fixedTimestep) end
-        accumulator = accumulator - fixedTimestep
-      end
-      
-    else
-      -- no fixed timestep in place, so just update
-      -- will pass 0 if love.timer is disabled
-      if love.update then love.update(dt) end 
-    end
-    
-    -- draw
-    if love.graphics then
-      love.graphics.clear()
-      if love.draw then love.draw() end
-      if love.timer then love.timer.sleep(0.001) end
-      love.graphics.present()
-    end
-      
-  end
-  
-end
-
-function make_good_table(input)
-    local out = {}
-    for i,v in ipairs(input) do
-        local t = {}
-        local name = nil
-        for k,val in pairs(v) do
-            if k == 'tag' then
-                name = val
-            elseif k == 'children' then
-                for ck,cv in pairs(make_good_table(val)) do
-                    t[ck] = cv
-                end
-            elseif k == 'attrs' then
-                for ck, cv in pairs(val) do
-                    t[ck] = cv
-                end
-            else
-                t[k] = val
-            end
-        end
-        if name then
-            if name == 'properties' then
-                for i, property in ipairs(t.property) do
-                    out[property.name] = property.value
-                end
-            else
-                if not out[name] then
-                    out[name] = {}
-                end
-                table.insert(out[name], t)
-            end
-        elseif v.attrs and v.attrs.name then
-            name = v.attrs.name
-            out[name] = t
-        else
-            return t
-        end
-    end
-    return out
-end
-
-REPLAY = true
-REPLAY_FILE = 'replay-'..os.date("%y-%m-%d-%H-%M-%S", os.time())..'.txt'
-
-LOADED_DATA = false
-INPUT_FRAME = 1
-
-if not LOAD_FILE then
-    love.filesystem.write(REPLAY_FILE, '')
-    print(love.filesystem.getSaveDirectory(), REPLAY_FILE)
-else
-    LOADED_DATA = {}
-    for line in love.filesystem.lines(LOAD_FILE) do
-        print(line)
-        table.insert(LOADED_DATA, json.decode(line))
-    end
-end
-
-REPLAY_DATA = {}
-CURRENT_FRAME = {}
-
-local cur_m = {false, false}
-local old_m = {false, false}
-function clicked(b)
-    b = b or 1
-    return cur_m[b] and not old_m[b]
-end
-
-function button_down(b)
-    b = b or 1
-    return cur_m[b]
-end
-
-function update_clicked()
-    if not LOAD_FILE then
-        mouse_pos_x, mouse_pos_y = love.mouse.getPosition()
-        CURRENT_FRAME['mouse_pos_x'] = mouse_pos_x
-        CURRENT_FRAME['mouse_pos_y'] = mouse_pos_y
-    else
-        mouse_pos_x = CURRENT_FRAME['mouse_pos_x']
-        mouse_pos_y = CURRENT_FRAME['mouse_pos_y']
-    end
-    for i=1,2 do
-        old_m[i] = cur_m[i]
-        if not LOAD_FILE then
-            cur_m[i] = love.mouse.isDown(i)
-            CURRENT_FRAME['mouse_'..i] = cur_m[i]
-        else
-            cur_m[i] = CURRENT_FRAME['mouse_'..i]
-        end
-    end
-end
-
-local keys = {a=false,w=false,s=false,d=false,space=false,p=false,o=false}
-local cur_keys = {a=false,w=false,s=false,d=false,space=false,p=false,o=false}
-
-function pressed(k)
-    -- keys[k] = keys[k] or false
-    return input_details:pressed(k)
-end
-
-function released(k)
-    -- keys[k] = keys[k] or false
-    return input_details:released(k)
-end
-
-function down(k, val)
-    return input_details:down(k, val)
-end
-
-function update_pressed()
-    for k,v in pairs(keys) do
-        keys[k] = cur_keys[k]
-        if not LOAD_FILE then
-            cur_keys[k] = love.keyboard.isDown(k)
-            CURRENT_FRAME['key_'..k] = cur_keys[k]
-        else
-            cur_keys[k] = CURRENT_FRAME['key_'..k]
-        end
-    end
-end
-
-function setup_game(level)
-    GameScene:new({level=level})
-end
-
--- m = ripple.newSound(love.audio.newSource('music/loop.mp3', 'stream'), {volume=0})
--- m.loop = true
--- m:play()
-
-function love.joystickadded(j)
-    input_details.joysticks = love.joystick.getJoysticks()
-end
-
-function love.load()
-    input_details = Input()
-    update_settings()
-    love.mouse.setVisible(false)
-    -- love.window.setMode(640*SCALE, 360*SCALE, {highdpi=true})
-
-    love.graphics.setDefaultFilter('nearest')
-    love.graphics.setLineStyle('rough')
-
-    -- damageShader = love.graphics.newShader[[
-    -- extern number percent;
-    -- vec4 effect( vec4 color, Image texture, vec2 texture_coords, vec2 screen_coords ){
-    --   vec4 pixel = Texel(texture, texture_coords );//This is the current pixel color
-    --   vec4 other = vec4(0.878, 0.173, 0.173, percent);
-    --   number a = (1-other.a)*other.a+pixel.a;
-    --   return vec4(
-    --     ( (1-other.a) * pixel.a*pixel.r + other.a*other.r )/a,
-    --     ( (1-other.a) * pixel.a*pixel.g + other.a*other.g )/a,
-    --     ( (1-other.a) * pixel.a*pixel.b + other.a*other.b )/a, 
-    --     min(a, pixel.a)
-    --   );
-    -- }
-    -- ]]
-
-    -- highlightShader = love.graphics.newShader[[
-    -- extern number percent;
-    -- vec4 effect( vec4 color, Image texture, vec2 texture_coords, vec2 screen_coords ){
-    --   vec4 pixel = Texel(texture, texture_coords );//This is the current pixel color
-    --   vec4 other = vec4(52.0/255.0, 0.0, 5.0/255.0, percent);
-    --   number a = (1-other.a)*other.a+pixel.a;
-    --   return vec4(
-    --     ( (1-other.a) * pixel.a*pixel.r + other.a*other.r )/a,
-    --     ( (1-other.a) * pixel.a*pixel.g + other.a*other.g )/a,
-    --     ( (1-other.a) * pixel.a*pixel.b + other.a*other.b )/a, 
-    --     min(a, pixel.a)
-    --   );
-    -- }
-    -- ]]
-
-    -- attackShader = love.graphics.newShader[[
-    -- extern number percent;
-    -- vec4 effect( vec4 color, Image texture, vec2 texture_coords, vec2 screen_coords ){
-    --   vec4 pixel = Texel(texture, texture_coords );//This is the current pixel color
-    --   vec4 other = vec4(1, 1, 1, percent);
-    --   number a = (1-other.a)*other.a+pixel.a;
-    --   return vec4(
-    --     ( (1-other.a) * pixel.a*pixel.r + other.a*other.r )/a,
-    --     ( (1-other.a) * pixel.a*pixel.g + other.a*other.g )/a,
-    --     ( (1-other.a) * pixel.a*pixel.b + other.a*other.b )/a, 
-    --     min(a, pixel.a)
-    --   );
-    -- }
-    -- ]]
-
-    font = love.graphics.newFont('test.fnt')
-    font:setLineHeight(0.7)
-    love.graphics.setFont(font)
-
-    -- setup_game()
-    start_scene(MainMenu:new())
-
-    main_canvas = love.graphics.newCanvas(640, 360)
-    -- small_canvas = love.graphics.newCanvas(640, 360)
-
-    love.setUpdateTimestep(1/60)
-
-    -- love.profiler = require('profile') 
-    -- love.profiler.start()
-end
-
-function start_scene(s)
-  scene = s
-  scene:begin()
-end
-
-function change_scene(s)
-    next_scene = s
-    -- local str = ''
-    -- for i, f in pairs(REPLAY_DATA) do
-    --   str=str..json.encode(f)..'\n'
-    -- end
-    -- love.filesystem.append(REPLAY_FILE, str)
-    REPLAY_DATA = {}
-end
-
-function add_entity(ent)
-    return scene:add_entity(ent)
-end
-
-if pcall(function() require "imgui" end) then
-else
-    local mock = function(a, b, c, d, e, f, g, h, i, j)
-    end
-    imgui = {}
-    imgui_mt = {__index=function(table, key)
-        return mock
-    end}
-    setmetatable(imgui, imgui_mt)
-end
-
-function love.conf(t)
-    t.console = true
-end
-
-love.update = profile('love.update', function(dt)
-    SAVE.TICK_COUNT = SAVE.TICK_COUNT + 1
-
-    while next_scene do
-      scene:exit()
-      scene = next_scene
-      next_scene = nil
-      scene:begin()
-    end
-    scene:update()
-    if song then
-        song:_update(1/60)
-    end
-
-    if SAVE.TICK_COUNT%100 == 0 then
-        --profile_report()
-        profile_start()
-    end
-end)
-
-FADE = 1
-love.draw = profile('love.draw', function()
-    -- love.graphics.setCanvas(small_canvas)
-    -- love.graphics.clear()
-    --     scene:small_render()
-
-
-        love.graphics.setCanvas(main_canvas)
-
-    if not scene.no_clear then
-        love.graphics.clear(unpack(PAL_LOOKUP[PAL][0]))
-    end
-        scene:render()
-
-    love.graphics.setShader()
-    current_shader = nil
-
-
-        love.graphics.setCanvas()
-        love.graphics.clear(unpack(PAL_LOOKUP[PAL][0]))
-
-        local width, height = love.graphics.getDimensions( )
-
-        love.graphics.setColor( 255, 255, 255, FADE )
-        love.graphics.draw(main_canvas, (width - 640 * SCALE) / 2, (height - 360 * SCALE) / 2, 0, SCALE, SCALE )
-        love.graphics.setColor( 255, 255, 255, 255 )
-        love.graphics.setBlendMode('alpha')
-
-
-    -- imgui.Render()
-    -- imgui.NewFrame()
-    -- local stats = love.graphics.getStats( )
-    -- print(inspect(stats))
-end)
-
-function love.quit()
-    -- local str = ''
-    -- for i, f in pairs(REPLAY_DATA) do
-    --   str=str..json.encode(f)..'\n'
-    -- end
-    -- love.filesystem.append(REPLAY_FILE, str)
-    REPLAY_DATA = {}
-    imgui.ShutDown();
-end
-
-function love.textinput(t)
-    imgui.TextInput(t)
-end
-
-function love.keypressed(key)
-    imgui.KeyPressed(key)
-    scene.events:trigger('keypressed', {key=key})
-end
-
-function love.keyreleased(key)
-    imgui.KeyReleased(key)
-    scene.events:trigger('keyreleased', {key=key})
-end
-
-function love.mousemoved(x, y)
-    imgui.MouseMoved(x, y)
-end
-
-function love.mousepressed(x, y, button)
-    imgui.MousePressed(button)
-    if not imgui.GetWantCaptureMouse() then
-        -- cur_m[button] = true
-    end
-end
-
-function love.mousereleased(x, y, button)
-    imgui.MouseReleased(button)
-end
-
-function love.wheelmoved(x, y)
-    imgui.WheelMoved(y)
-end
+-- Performance optimizations: local caching of frequently used functions
+local pairs = pairs
+local ipairs = ipairs
+local unpack = unpack or table.unpack
+local floor = math.floor
+
+local profile_data = {}
+
+function profile_start()
+    profile_data = {}
+end
+
+function profile_report()
+    print('-----------------')
+    for k,v in pairs(profile_data) do
+        print(k, v)
+    end
+    print('-----------------')
+end
+
+local starts = {}
+
+function profile(name, fn, stop)
+    return fn
+end
+
+local sfx = {}
+local last_played = {}
+local muted = {}
+
+function mute(f)
+    muted[f] = true
+end
+
+function unmute(f)
+    muted[f] = false
+end
+
+-- OPTIMIZATION: Pre-build common sound paths to avoid string concatenation
+local sound_cache = {}
+function get_sound_path(sound)
+    if not sound_cache[sound] then
+        sound_cache[sound] = 'res/sfx/'..sound..'.wav'
+    end
+    return sound_cache[sound]
+end
+
+function play_sound(sound, pitch, vol, loop)
+    pitch = pitch or 0
+    if muted[sound] then
+        return
+    end
+    local s = sfx[sound]
+    if not s then
+        s = ripple.newSound(love.audio.newSource(get_sound_path(sound), 'static'), {
+            loop = loop,
+        })
+        sfx[sound] = s
+        last_played[sound] = -1
+    end
+    local tick = SAVE.TICK_COUNT
+    if last_played[sound] == tick then
+        return
+    end
+    last_played[sound] = tick
+    return s:play({
+        volume = SETTINGS.sfx_volume/100 * (vol or 1),
+        pitch = 2^(pitch/12),
+    })
+end
+
+song = nil
+song_name = nil
+function play_song(sound, opts)
+    if sound ~= 'TitleTheme.ogg' then
+        SAVE.SONG = sound
+    end
+    print(song_name, sound)
+    if song_name == sound then
+        return
+    end
+    song_name = sound
+    if song then
+        song:stop()
+    end
+    song = nil
+    if not sound then
+        return
+    end
+    local s = sfx[sound]
+    if not s then
+        s = ripple.newSound(love.audio.newSource('res/song/'..sound, 'static'), opts)
+        sfx[sound] = s
+        last_played[sound] = -1
+    end
+    print('huh?')
+    local tick = SAVE.TICK_COUNT
+    if last_played[sound] == tick then
+        return
+    end
+    last_played[sound] = tick
+    song = s:play({
+        volume = SETTINGS.music_volume/100
+    })
+    print(song._fadeVolume)
+end
+
+function new_game(best_times)
+    local TOP_LEVEL = 1
+    if SAVE then
+        TOP_LEVEL = SAVE.TOP_LEVEL
+    end
+    SAVE = {
+        LEVEL_NUM=1,
+        DEATHS=0,
+        COINS_MISSED=0,
+        MOTH_JUMP=MOTH_JUMP,
+        FLIP=FLIP,
+        PICK=PICK,
+        TICK_COUNT=0,
+        START_TICK=0,
+        SCORE=0,
+        SEEN_WAll_KEY=false,
+        COINS = {},
+        TIMES = {},
+        BEST_TIMES = best_times or {},
+        TOP_LEVEL=TOP_LEVEL,
+        GREED=0,
+        BEAT={},
+        OPENED={},
+        SONG='LockedUp.wav',
+    }
+
+    if set_pal then
+        set_pal(0)
+        play_song(nil)
+    end
+end
+new_game()
+
+json = require 'json'
+require 'util'
+
+local contents = love.filesystem.read('save.json')
+if contents then
+    local save = json.decode(contents)
+    for k,v in pairs(save) do
+        SAVE[k] = v
+    end
+    SAVE.OPENED = SAVE.OPENED or {}
+    print(inspect(SAVE))
+end
+
+require 'strict'
+
+
+scene = nil
+next_scene = nil
+main_canvas = nil
+small_canvas = nil
+debug_mode = nil
+_ = nil
+
+input_details = nil
+font = nil
+
+
+require 'config'
+
+require 'easing'
+require 'threads'
+
+ripple = require 'ripple'
+
+require 'components/entity'
+require 'events'
+require 'sequencer'
+require 'button'
+require 'slider'
+
+Input = require 'Input'
+
+require 'scenes/scene'
+require 'scenes/settings_scene'
+require 'scenes/game_scene'
+require 'scenes/main_menu'
+require 'scenes/break_scene'
+require 'scenes/level_editor'
+require 'scenes/level_editor_select'
+
+require 'Shake'
+
+function love.setUpdateTimestep(ts)
+  love.updateTimestep = ts
+end
+
+function love.run()
+
+  math.randomseed( os.time() )
+  math.random() math.random()
+  
+  if love.load then love.load(arg) end
+  
+  local dt = 0
+  local accumulator = 0
+  
+  -- Main loop
+  while true do
+  
+    -- Process events.
+    if love.event then
+      love.event.pump()
+      for e,a,b,c,d in love.event.poll() do
+        if e == "quit" then
+          if not love.quit or not love.quit() then
+            if love.audio then love.audio.stop() end
+            return
+          end
+        end
+        love.handlers[e](a,b,c,d)
+      end
+    end
+        
+    -- Update dt for any uses during this timestep of love.timer.getDelta
+    if love.timer then 
+      love.timer.step()
+      dt = love.timer.getDelta()
+    end
+
+    local fixedTimestep = love.updateTimestep
+    
+    if fixedTimestep then       
+      -- see http://gafferongames.com/game-physics/fix-your-timestep  
+      
+      if dt > 0.25 then      
+        dt = 0.25 -- note: max frame time to avoid spiral of death
+      end     
+      
+      accumulator = accumulator + dt
+
+      while accumulator >= fixedTimestep do
+        if love.update then love.update(fixedTimestep) end
+        accumulator = accumulator - fixedTimestep
+      end
+      
+    else
+      -- no fixed timestep in place, so just update
+      -- will pass 0 if love.timer is disabled
+      if love.update then love.update(dt) end 
+    end
+    
+    -- draw
+    if love.graphics then
+      love.graphics.clear()
+      if love.draw then love.draw() end
+      if love.timer then love.timer.sleep(0.001) end
+      love.graphics.present()
+    end
+      
+  end
+  
+end
+
+function make_good_table(input)
+    local out = {}
+    for i,v in ipairs(input) do
+        local t = {}
+        local name = nil
+        for k,val in pairs(v) do
+            if k == 'tag' then
+                name = val
+            elseif k == 'children' then
+                for ck,cv in pairs(make_good_table(val)) do
+                    t[ck] = cv
+                end
+            elseif k == 'attrs' then
+                for ck, cv in pairs(val) do
+                    t[ck] = cv
+                end
+            else
+                t[k] = val
+            end
+        end
+        if name then
+            if name == 'properties' then
+                for i, property in ipairs(t.property) do
+                    out[property.name] = property.value
+                end
+            else
+                if not out[name] then
+                    out[name] = {}
+                end
+                table.insert(out[name], t)
+            end
+        elseif v.attrs and v.attrs.name then
+            name = v.attrs.name
+            out[name] = t
+        else
+            return t
+        end
+    end
+    return out
+end
+
+REPLAY = true
+REPLAY_FILE = 'replay-'..os.date("%y-%m-%d-%H-%M-%S", os.time())..'.txt'
+
+LOADED_DATA = false
+INPUT_FRAME = 1
+
+if not LOAD_FILE then
+    love.filesystem.write(REPLAY_FILE, '')
+    print(love.filesystem.getSaveDirectory(), REPLAY_FILE)
+else
+    LOADED_DATA = {}
+    for line in love.filesystem.lines(LOAD_FILE) do
+        print(line)
+        table.insert(LOADED_DATA, json.decode(line))
+    end
+end
+
+REPLAY_DATA = {}
+CURRENT_FRAME = {}
+
+local cur_m = {false, false}
+local old_m = {false, false}
+function clicked(b)
+    b = b or 1
+    return cur_m[b] and not old_m[b]
+end
+
+function button_down(b)
+    b = b or 1
+    return cur_m[b]
+end
+
+function update_clicked()
+    if not LOAD_FILE then
+        mouse_pos_x, mouse_pos_y = love.mouse.getPosition()
+        CURRENT_FRAME['mouse_pos_x'] = mouse_pos_x
+        CURRENT_FRAME['mouse_pos_y'] = mouse_pos_y
+    else
+        mouse_pos_x = CURRENT_FRAME['mouse_pos_x']
+        mouse_pos_y = CURRENT_FRAME['mouse_pos_y']
+    end
+    for i=1,2 do
+        old_m[i] = cur_m[i]
+        if not LOAD_FILE then
+            cur_m[i] = love.mouse.isDown(i)
+            CURRENT_FRAME['mouse_'..i] = cur_m[i]
+        else
+            cur_m[i] = CURRENT_FRAME['mouse_'..i]
+        end
+    end
+end
+
+-- OPTIMIZATION: Pre-build key strings to avoid concatenation every frame
+local key_strings = {
+    a = 'key_a',
+    w = 'key_w',
+    s = 'key_s',
+    d = 'key_d',
+    space = 'key_space',
+    p = 'key_p',
+    o = 'key_o',
+    k = 'key_k'
+}
+
+local keys = {a=false,w=false,s=false,d=false,space=false,p=false,o=false,k=false}
+local cur_keys = {a=false,w=false,s=false,d=false,space=false,p=false,o=false,k=false}
+
+function pressed(k)
+    return input_details:pressed(k)
+end
+
+function released(k)
+    return input_details:released(k)
+end
+
+function down(k, val)
+    return input_details:down(k, val)
+end
+
+function update_pressed()
+    for k,v in pairs(keys) do
+        keys[k] = cur_keys[k]
+        if not LOAD_FILE then
+            cur_keys[k] = love.keyboard.isDown(k)
+            CURRENT_FRAME[key_strings[k]] = cur_keys[k]  -- No string concat!
+        else
+            cur_keys[k] = CURRENT_FRAME[key_strings[k]]
+        end
+    end
+end
+
+function setup_game(level)
+    GameScene:new({level=level})
+end
+
+function love.joystickadded(j)
+    input_details.joysticks = love.joystick.getJoysticks()
+end
+
+function love.load()
+    input_details = Input()
+    update_settings()
+    love.mouse.setVisible(false)
+
+    love.graphics.setDefaultFilter('nearest')
+    love.graphics.setLineStyle('rough')
+
+    -- OPTIMIZATION: Use TrueType font instead of bitmap font for better scaling
+    font = love.graphics.newFont('Silver.ttf', 18)
+    font:setLineHeight(0.7)
+    love.graphics.setFont(font)
+
+    start_scene(MainMenu:new())
+
+    main_canvas = love.graphics.newCanvas(640, 360)
+
+    love.setUpdateTimestep(1/60)
+end
+
+function start_scene(s)
+  scene = s
+  scene:begin()
+end
+
+function change_scene(s)
+    next_scene = s
+    REPLAY_DATA = {}
+end
+
+function add_entity(ent)
+    return scene:add_entity(ent)
+end
+
+if pcall(function() require "imgui" end) then
+else
+    local mock = function(a, b, c, d, e, f, g, h, i, j)
+    end
+    imgui = {}
+    imgui_mt = {__index=function(table, key)
+        return mock
+    end}
+    setmetatable(imgui, imgui_mt)
+end
+
+function love.conf(t)
+    t.console = true
+end
+
+-- OPTIMIZATION: Cache tick count check
+local tick_mod_100 = 0
+love.update = profile('love.update', function(dt)
+    SAVE.TICK_COUNT = SAVE.TICK_COUNT + 1
+
+    -- Reset the push flag for all pillars so they can be pushed again this frame
+    if scene and scene.entities then
+        for i = 1, #scene.entities do
+            local e = scene.entities[i]
+            if e.Pillar then
+                e.Pillar._pushed_this_tick = false
+            end
+        end
+    end
+
+    while next_scene do
+      scene:exit()
+      scene = next_scene
+      next_scene = nil
+      scene:begin()
+    end
+    
+    scene:update()
+    
+    if song then
+        song:_update(1/60)
+    end
+
+    -- Optimization: Commented out profile_start to prevent table allocation lag
+    -- if SAVE.TICK_COUNT%100 == 0 then
+    --     profile_start()
+    -- end
+end)
+
+FADE = 1
+-- OPTIMIZATION: Cache frequently accessed values
+local graphics_clear = love.graphics.clear
+local graphics_setCanvas = love.graphics.setCanvas
+local graphics_setShader = love.graphics.setShader
+local graphics_setColor = love.graphics.setColor
+local graphics_draw = love.graphics.draw
+local graphics_getDimensions = love.graphics.getDimensions
+local graphics_setBlendMode = love.graphics.setBlendMode
+
+love.draw = profile('love.draw', function()
+    graphics_setCanvas(main_canvas)
+
+    if not scene.no_clear then
+        graphics_clear(unpack(PAL_LOOKUP[PAL][0]))
+    end
+    
+    scene:render()
+
+    graphics_setShader()
+    current_shader = nil
+
+    graphics_setCanvas()
+    graphics_clear(unpack(PAL_LOOKUP[PAL][0]))
+
+    -- Get current window dimensions
+    local window_w, window_h = graphics_getDimensions()
+
+    -- Calculate the scale needed to fit the screen
+    -- This finds the ratio for both width and height and picks the smaller one
+    local scale_w = window_w / 640
+    local scale_h = window_h / 360
+    local dynamic_scale = math.min(scale_w, scale_h)
+
+    -- Calculate offsets to keep the canvas perfectly centered
+    local offset_x = (window_w - (640 * dynamic_scale)) / 2
+    local offset_y = (window_h - (360 * dynamic_scale)) / 2
+
+    graphics_setColor(255, 255, 255, FADE)
+    
+    -- Use dynamic_scale and the calculated offsets
+    graphics_draw(main_canvas, offset_x, offset_y, 0, dynamic_scale, dynamic_scale)
+    
+    graphics_setColor(255, 255, 255, 255)
+    graphics_setBlendMode('alpha')
+end)
+
+function love.quit()
+    REPLAY_DATA = {}
+    imgui.ShutDown();
+end
+
+function love.textinput(t)
+    imgui.TextInput(t)
+end
+
+function love.keypressed(key)
+    imgui.KeyPressed(key)
+    scene.events:trigger('keypressed', {key=key})
+end
+
+function love.keyreleased(key)
+    imgui.KeyReleased(key)
+    scene.events:trigger('keyreleased', {key=key})
+end
+
+function love.mousemoved(x, y)
+    imgui.MouseMoved(x, y)
+end
+
+function love.mousepressed(x, y, button)
+    imgui.MousePressed(button)
+    if not imgui.GetWantCaptureMouse() then
+        -- cur_m[button] = true
+    end
+end
+
+function love.mousereleased(x, y, button)
+    imgui.MouseReleased(button)
+end
+
+function love.wheelmoved(x, y)
+    imgui.WheelMoved(y)
+end
--- original/objects/gravity_line.lua
+++ modified/objects/gravity_line.lua
@@ -1,60 +1,64 @@
-GravityLine = {
-    last_tick = 0
-}
-
-function GravityLine:new(o)
-    o = o or {}
-
-    setmetatable(o, self)
-    self.__index = self
-
-    o:init()
-
-    return o
-end
-
-function GravityLine:init()
-    _, self.sprite = self.ent:add_comp(Sprite, {
-        file='res/images/tiles_vault.png', 
-        w=16, 
-        h=16,
-        frame=self.tile
-    })
-
-    _, self.object = self.ent:add_comp(Object, {
-        w=8,
-        h=8,
-        pass_through=true,
-    })
-
-    self.ent.tag = 'gravity_line'
-end
-
-register_tiles({}, function(scene, tile, x, y, layer)
-    return scene:add_entity(Entity:new({
-        x=x,
-        y=y,
-        layer=entity_layers[layer.name]
-    }):add_comp(GravityLine, {tile=tile, layer=layer.name}))
-end)
-
-register_collision('gravity_line', 'player', function(solid, player, solid_x, solid_y, player_x, player_y)
-    if GravityLine.last_tick > SAVE.TICK_COUNT then
-        GravityLine.last_tick = SAVE.TICK_COUNT + 4
-
-        print(GravityLine.side, player.x > solid.x)
-        if GravityLine.side ~= (player.x > solid.x) then
-            GravityLine.side = player.x > solid.x
-            player[Object].gravity = -player[Object].gravity
-            player[Player].vel_y = -player[Player].vel_y
-            player[Sprite].flip_y = not player[Sprite].flip_y
-        end
-
-        return false
-    end
-
-    GravityLine.side = player.x > solid.x
-    GravityLine.last_tick = SAVE.TICK_COUNT + 4
-
-    return false
-end)
+GravityLine = {}
+
+function GravityLine:new(o)
+    o = o or {}
+
+    setmetatable(o, self)
+    self.__index = self
+
+    o:init()
+
+    return o
+end
+
+function GravityLine:init()
+    _, self.sprite = self.ent:add_comp(Sprite, {
+        file='res/images/tiles_vault.png',
+        w=16,
+        h=16,
+        frame=self.tile
+    })
+
+    _, self.object = self.ent:add_comp(Object, {
+        w=8,
+        h=8,
+        pass_through=true,
+    })
+
+    self.ent.tag = 'gravity_line'
+
+    -- Per-instance cooldown and side state (was incorrectly on the class table,
+    -- causing all gravity lines to share a single timer and side value)
+    self.last_tick = 0
+    self.side = false
+end
+
+register_tiles({}, function(scene, tile, x, y, layer)
+    return scene:add_entity(Entity:new({
+        x=x,
+        y=y,
+        layer=entity_layers[layer.name]
+    }):add_comp(GravityLine, {tile=tile, layer=layer.name}))
+end)
+
+register_collision('gravity_line', 'player', function(solid, player, solid_x, solid_y, player_x, player_y)
+    -- FIX: read/write per-instance state instead of the shared class table
+    local gl = solid[GravityLine]
+
+    if gl.last_tick > SAVE.TICK_COUNT then
+        -- FIX: removed print() which was flushing stdout every frame during collision
+        if gl.side ~= (player.x > solid.x) then
+            gl.side = player.x > solid.x
+            player[Object].gravity = -player[Object].gravity
+            player[Player].vel_y = -player[Player].vel_y
+            player[Sprite].flip_y = not player[Sprite].flip_y
+        end
+
+        return false
+    end
+
+    gl.side = player.x > solid.x
+    gl.last_tick = SAVE.TICK_COUNT + 4
+
+    return false
+end)
--- original/objects/player.lua
+++ modified/objects/player.lua
@@ -1,971 +1,1079 @@
-Player = {}
-
-function Player:new(o)
-    o = o or {}
-
-    setmetatable(o, self)
-    self.__index = self
-
-    o:init()
-
-    return o
-end
-
-function Player:init()
-    _, self.sprite = self.ent:add_comp(Sprite, {file='res/images/reya_vault.png', w=48, h=48, offset_y=-11})
-
-    self.frames = {
-        idle = {0, 1, 2, 3, 4, 5},
-        run = {10, 11, 12, 13, 14, 15},
-
-        rope_idle = {320, 321, 322, 323, 324, 325},
-        rope_run = {310, 311, 312, 313, 314, 315},
-
-        push = {50, 51, 52, 53, 54, 55},
-        up = {21, 22},
-        down = {23, 24},
-        walljump = {31, 32, 33},
-        slide = {30},
-        super = {201, 202},
-        super_loop = {207, 208, 205, 206},
-        rebound = {42, 43},
-        rejump = {44, 45},
-        squash = {59, 60, 61, 62, 63, 64, 65, 66, 67, 68, 69, -1},
-        enter = {170, 171, 172, 173, 174, 175},
-        enter_cont = {176, 177, 178, 179, 180, 181, 182, 183},
-        exit = {160, 161, 162, 163, 164, 165, -1, -1},
-        pre_climb_jump = { 290, 291, 292, 293 },
-        pre_climb_jump_loop = { 294 },
-        climb_jump = { 300, 301 },
-        after_climb_jump = {302, 303, 304, 305, 305},
-        fancy_fall={207, 208, 205, 206},
-        fancy_flip={177, 178, 179, 180, 181, 182, 183},
-    }
-
-    self.ghost = {}
-
-    _, self.sm = self.ent:add_comp(StateManager, {
-        state=self.fancy_fall and 'fancy_fall' or 'enter',
-        states={
-            fancy_flip={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate({177, 178, 179}, function()
-                        play_song('LockedUp.wav', {
-                            loop = true,
-                        })
-                        self.sprite:animate({180, 181, 182, 183}, function()
-                            sm:change_state('idle')
-                        end)
-                    end)
-                end,
-                update=function(sm)
-                end,
-            },
-
-            fancy_fall={
-                enter=function(sm)
-                    self.sprite:animate(self.frames.fancy_fall)
-                    self.sprite.continue = true
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                exit=function(sm)
-                    if sm.state ~= 'fancy_flip' then
-                        return true
-                    end
-                end,
-                air=true
-            },
-
-            enter={
-                enter=function(sm)
-                    play_sound('DIABWalkIn')
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.enter, function()
-                        sm:change_state('enter_cont')
-                    end)
-                end,
-                update=function(sm)
-                end,
-            },
-
-            enter_fancy={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate({-1})
-                end,
-                update=function(sm)
-                end,
-            },
-
-            exit={
-                enter=function(sm)
-                    play_sound('DIABWalkOut')
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.exit, function()
-                        -- if scene.next_song then
-                        --     play_song(scene.next_song, {
-                        --         loop = true,
-                        --     })
-                        -- end
-                        scene.events:add_timer(2, function()
-                            if scene.greeding then
-                                scene:next(false, false, true)
-                            else
-                                scene:next()
-                            end
-                        end)
-                    end)
-                    sm.states.exit.fade_out = 20
-                end,
-                update=function(sm)
-                    self.object:clean_up()
-
-                    local s = sm.states.exit
-                    if s.fade_out > 0 then
-                        s.fade_out = s.fade_out - 1
-
-                        if s.fade_out == 0 then
-                            -- if scene.next_song or scene.stop_song then
-                            --     song:stop(0.5)
-                            -- end
-                        end
-                    else
-                        scene.fade = math.max(scene.fade - 2*FADE_RATE, 0)
-                        FADE = scene.fade
-                    end
-                end,
-
-                exit=function(sm)
-                    return true
-                end,
-            },
-
-            exit_fancy={
-                enter=function(sm)
-                    -- play_sound('DIABWalkOut')
-                    self.sprite.continue = false
-                    self.sprite:animate({-1})
-
-                    local offset = scene.bottle_exit and 10 * 6 or 0
-
-                    sm.states.exit.fade_out = (30-#self.frames.exit)*6 + offset
-
-                    scene.events:add_timer((30)*6 + offset, function()
-                        scene.events:add_timer(60, function()
-                            -- if scene.next_song then
-                            --     play_song(scene.next_song, {
-                            --         loop = true,
-                            --     })
-                            --     song:pause()
-                            --     song:resume(1)
-                            -- end
-                            scene:next()
-                        end)
-                    end)
-                end,
-                update=function(sm)
-                    self.object:clean_up()
-
-                    local s = sm.states.exit
-                    if s.fade_out > 0 then
-                        s.fade_out = s.fade_out - 1
-                        -- if song then
-                        --     if s.fade_out == 0 and scene.next_song then
-                        --         song:stop(1)
-                        --     end
-                        -- end
-                    else
-                        scene.fade = math.max(scene.fade - 2*FADE_RATE, 0)
-                        FADE = scene.fade
-                    end
-                end,
-
-                exit=function(sm)
-                    return true
-                end,
-            },
-
-            enter_cont={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.enter_cont, function()
-                        sm:change_state('idle')
-                    end)
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end,
-            },
-
-            idle={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.idle)
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end,
-            },
-
-            idle_wait={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.idle)
-                end,
-                update=function(sm)
-                end,
-            },
-
-
-            run={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.run)
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end
-            },
-
-            rope_idle={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.rope_idle)
-                    self.sprite.offset_y = -10
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end,
-                exit=function(sm)
-                    self.sprite.offset_y = -11
-                end,
-            },
-
-            rope_run={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.rope_run)
-                    self.sprite.offset_y = -10
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end,
-                exit=function(sm)
-                    self.sprite.offset_y = -11
-                end,
-            },
-
-            push={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.push)
-                    self.sprite.offset_x = -2
-                end,
-                update=function(sm)
-                    self:grounded_update(sm)
-                end,
-                exit=function(sm)
-                    self.sprite.offset_x = 0
-                end
-            },
-
-
-            up={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.up)
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                air=true
-            },
-
-            walljump={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.walljump, function()
-                        sm:change_state('up')
-                    end)
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                air=true
-            },
-
-            super={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.super, function()
-                        sm:change_state('super_loop')
-                    end)
-                    self.sprite.offset_y = -5
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                    if SAVE.FLIP and SAVE.TICK_COUNT % GHOST_EVERY_X_TICKS == 0 and (math.abs(self.vel_x) > 2 or self.ghosting) then
-                        self.ghosting = true
-                        table.insert(self.ghost, scene:add_entity(Entity:new({
-                            x=self.ent.x,
-                            y=self.ent.y,
-                            layer=self.ent.layer - 0.1
-                        }):add_comp(Particle, {
-                            file='res/images/reya_vault.png', w=48, h=48, offset_y=-5,
-                            frames={self.sprite.frame},
-                            anim_length=GHOST_LENGTH,
-                            alpha=GHOST_ALPHA
-                        })))
-                    end
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and (sm.state ~= 'super_loop' and sm.state ~= 'rebound')  then
-                        return true
-                    end
-                    self.sprite.offset_y = -11
-                    if sm.state ~= 'super_loop' then
-                        self.ghosting = false
-                    end
-                end,
-                air=true,
-                super=true,
-            },
-
-            super_loop={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.super_loop)
-                    self.sprite.offset_y = -5
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                    if SAVE.FLIP and SAVE.TICK_COUNT % GHOST_EVERY_X_TICKS == 0 and (math.abs(self.vel_x) > 2 or self.ghosting) then
-                        self.ghosting = true
-                        table.insert(self.ghost, scene:add_entity(Entity:new({
-                            x=self.ent.x,
-                            y=self.ent.y,
-                            layer=self.ent.layer - 0.1
-                        }):add_comp(Particle, {
-                            file='res/images/reya_vault.png', w=48, h=48, offset_y=-5,
-                            frames={self.sprite.frame},
-                            anim_length=GHOST_LENGTH,
-                            alpha=GHOST_ALPHA
-                        })))
-                    end
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and sm.state ~= 'rebound' then
-                        return true
-                    end
-                    self.sprite.offset_y = -11
-                    self.ghosting = false
-                end,
-                air=true,
-                super=true,
-            },
-
-            rebound={
-                enter=function(sm)
-                    self.vel_y = 0
-                    if self.hit_left_wall then
-                        self.object.facing = -1
-                    else
-                        self.object.facing = 1
-                    end
-
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.rebound, function()
-                        self:walljump(-4.8, 4, scene.heavy_walls and 0.75 or 0.85, 25, 'rejump')
-                        self.object.facing = -self.object.facing
-                        
-                        if self.buffer_super then
-                            sm:change_state('super')
-                        end
-                    end)
-
-                    self.sprite.offset_x = -4
-                    self.buffer_super = false
-                    self.cancel = false
-                end,
-                update=function(sm)
-                    self.vel_y = 0
-                    self:move_update(sm)
-                    if not (self.hit_left_wall or self.hit_right_wall) and self.sprite.f < 10 then
-                        sm:change_state('down')
-                        self.object.facing = -self.object.facing
-                    end
-
-                    if pressed('space') then
-                        self.buffer_super = true
-                    end
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and sm.state ~= 'rejump' and (self.hit_left_wall or self.hit_right_wall) then
-                        return true
-                    end
-                    self.sprite.offset_x = 0
-                end,
-                air=true,
-                super=true,
-                wall=true
-            },
-
-            rejump={
-                enter=function(sm)
-                    self.cancel = false
-                    self.cancel_count = self.cancel_count + 1
-                    local cancel_count = self.cancel_count
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.rejump, function()
-                        sm:change_state('up')
-                    end)
-                    if not SAVE.FLIP then
-                        scene.events:add_timer(#self.frames.rejump*6, function()
-                            if self.cancel_count == cancel_count then
-                                self.cancel = true
-                            end
-                        end)
-                    end
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                air=true,
-                super=true,
-            },
-
-            pre_climb_jump={
-                enter=function(sm)
-                    play_sound('deploy_pick', nil, 0.5)
-                    if self.hit_wall then
-                        sm:change_state("climb_jump")
-                        return
-                    end
-
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.pre_climb_jump, function()
-                        sm:change_state("pre_climb_jump_loop")
-                    end)
-                    if down('a') then
-                        self.vel_x = -math.abs(self.vel_x)
-                        self.object.facing = -1
-                    elseif down('d') then
-                        self.vel_x = math.abs(self.vel_x)
-                        self.object.facing = 1
-                    end
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                    if self.hit_wall then
-                        if self.hit_left_wall and self.object.facing == 1 then
-                            return
-                        elseif self.hit_right_wall and self.object.facing == -1 then
-                            return
-                        end
-                        sm:change_state('climb_jump')
-                    end
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and sm.state ~= 'pre_climb_jump_loop' and sm.state ~= 'climb_jump' then
-                        return true
-                    end
-                end,
-                air=true,
-                super=true
-            },
-
-            pre_climb_jump_loop={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.pre_climb_jump_loop)
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                    if self.hit_wall then
-                        if self.hit_left_wall and self.object.facing == 1 then
-                            return
-                        elseif self.hit_right_wall and self.object.facing == -1 then
-                            return
-                        end
-                        sm:change_state('climb_jump')
-                    end
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and sm.state ~= 'climb_jump' then
-                        return true
-                    end
-                end,
-                air=true,
-                super=true,
-            },
-
-            climb_jump={
-                enter=function(sm)
-                    play_sound('pick_sound', nil, 0.5)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.climb_jump, function()
-                        self.vel_y = -CLIMB_STRENGTH
-                        sm:change_state("after_climb_jump")
-
-                        if self.wall then
-                            scene.events:trigger('climb', {entity=self.ent, off=self.wall.ent})
-                        end
-                    end)
-                    self.vel_y = 0
-                    self.vel_x = self.wall_direction * CLIMB_PUSHOFF
-                    if self.hit_left_wall then
-                        self.right_lock = CLIMB_OFF_LOCK
-                        self.left_lock = CLIMB_LOCK
-                        self.object.facing = -1
-                    else
-                        self.left_lock = CLIMB_OFF_LOCK
-                        self.right_lock = CLIMB_LOCK
-                        self.object.facing = 1
-                    end
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                exit=function(sm)
-                    if sm:attr('air') and sm.state ~= 'after_climb_jump' and sm.state ~= 'walljump' then
-                        return true
-                    end
-                end,
-                air=true,
-                super=true,
-                wall=true
-            },
-
-            after_climb_jump={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.after_climb_jump, function()
-                        if self.hit_wall then
-                            sm.done = true
-                            sm:change_state("slide")
-                        else
-                            sm:change_state("up")
-                        end
-                    end)
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                    -- if not self.hit_wall then
-                    --     sm:change_state("up")
-                    -- end
-                end,
-                exit=function(sm)
-                    if sm.state == 'slide' and not sm.done then
-                        return true
-                    end
-                    sm.done = nil
-                end,
-                air=true,
-                super=true
-            },
-
-            down={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.down)
-                end,
-                update=function(sm)
-                    self:air_update(sm)
-                end,
-                air=true
-            },
-
-            slide={
-                enter=function(sm)
-                    self.sprite.continue = true
-                    self.sprite:animate(self.frames.slide)
-                    if self.hit_left_wall then
-                        self.object.facing = 1
-                    else
-                        self.object.facing = -1
-                    end
-                    self.sprite.offset_x = -3
-                end,
-                update=function(sm)
-                    if self.hit_left_wall then
-                        self.object.facing = 1
-                    else
-                        self.object.facing = -1
-                    end
-                    self:air_update(sm)
-                end,
-                exit=function(sm)
-                    self.sprite.offset_x = 0
-                end,
-                air=true,
-                wall=true,
-            },
-
-            squash={
-                enter=function(sm)
-                    self.sprite.continue = false
-                    self.sprite:animate(self.frames.squash, function()
-                        scene:again()
-                    end)
-                    sm.states.squash.count_down = 12
-                    sm.states.squash.explode_count = 36
-                    sm.states.squash.fade_out = 42
-                end,
-                update=function(sm)
-                    self.object:clean_up()
-                    local s = sm.states.squash
-                    if s.count_down > 0 then
-                        self.object:move_x(s.x_dir * s.count_down / 12 * 4)
-                        self.object:move_y(s.y_dir * s.count_down / 12 * 2)
-                        s.count_down = s.count_down - 1
-                    end
-                    if s.explode_count > 0 then
-                        s.explode_count = s.explode_count - 1
-                        if s.explode_count == 6 then
-                            play_sound("DIABExplosion")
-                        elseif s.explode_count == 0 then
-                            scene.shake = 2
-                        end
-                    end
-
-                    if s.fade_out > 0 then
-                        s.fade_out = s.fade_out - 1
-                    else
-                        scene.fade = math.max(scene.fade - 2*FADE_RATE, 0)
-                        FADE = scene.fade
-                    end
-                end,
-                exit=function(sm)
-                    return true
-                end,
-            }
-        }
-    })
-    _, self.object = self.ent:add_comp(Object, {
-        w=14,
-        h=26,
-        object_list=Object.player_list,
-        is_riding=Player.is_riding,
-    })
-    -- self.ent:add_comp(Rectangle, {
-    --     width=14,
-    --     height=26
-    -- })
-    self.object.facing = self.fancy_fall and -1 or 1
-
-    self.vel_x = 0
-    self.vel_y = 0
-
-    self.right_lock = 0
-    self.left_lock = 0
-    self.cancel_count = 0
-
-    self.ent.tag = 'player'
-end
-
-function Player:kill(x_dir, y_dir, no_sound)
-    if self.sm.state == 'squash' or self.sm.state == 'exit' then
-        return
-    end
-
-    local s = self.sm.states.squash
-    s.x_dir = x_dir
-    s.y_dir = y_dir
-
-    if not no_sound then
-        play_sound("DIABHitBySpikes")
-    end
-    self.sm:change_state('squash')
-end
-
-function Player:win(fancy)
-    if self.sm.state == 'exit' then
-        return
-    end
-    self.sm:change_state(fancy and 'exit_fancy' or 'exit')
-end
-
-function Player:walljump(vel_y, vel_x, vel_wall, lock, state)
-    if not self.wall then
-        return
-    end
-    self.vel_y = vel_y + math.min(self.wall.move_y_amount * 0.75/2, 0)
-    self.vel_x = vel_x * self.wall_direction + self.wall.move_x_amount
-    self.sm:change_state(state or 'walljump')
-    if state then
-        play_sound('DIABHyperJump')
-
-        scene:add_entity(Entity:new({
-            x=self.ent.x,
-            y=self.ent.y,
-            layer=self.ent.layer
-        }):add_comp(Particle, {
-            file='res/images/particles.png', 
-            w=80, 
-            h=80,
-            frames={28, 29, 30, 31, 32, 33, 34},
-            flip=self.object.facing == -1,
-            other=self.wall.ent
-        }))
-
-        if self.object.facing == -1 then
-            scene.shake_right = 1
-        else
-            scene.shake_left = 1
-        end
-    else
-        play_sound('DIABJump')
-
-        scene:add_entity(Entity:new({
-            x=self.ent.x,
-            y=self.ent.y,
-            layer=self.ent.layer
-        }):add_comp(Particle, {
-            file='res/images/particles.png', 
-            w=80, 
-            h=80,
-            frames={56, 57},
-            flip=self.object.facing == 1,
-            other=self.wall.ent
-        }))
-    end
-
-    scene.events:trigger('walljump', {entity=self.ent, off=self.wall.ent, push=-self.wall_direction * vel_wall})
-    if self.wall2 then
-        scene.events:trigger('walljump', {entity=self.ent, off=self.wall2.ent, push=-self.wall_direction * vel_wall})
-    end
-    if self.wall3 then
-        scene.events:trigger('walljump', {entity=self.ent, off=self.wall3.ent, push=-self.wall_direction * vel_wall})
-    end
-    if self.wall.ent.left or self.wall.ent.right then
-        scene:push(self.wall.ent.left, self.wall.ent.left and -vel_wall or vel_wall)
-        -- play_sound("jump_off_wall", nil, state and 0.5 or 0.2)
-    end
-
-    if self.hit_left_wall then
-        self.left_lock = lock
-        self.right_lock = 0
-    else
-        self.right_lock = lock
-        self.left_lock = 0
-    end
-    self.hit_wall = false
-    self.hit_left_wall = false
-    self.hit_right_wall = false
-end
-
-function Player.is_riding(self, other, axis)
-    local s = other.ent[Sword]
-    if s then
-        if s.vel_x ~= 0 then
-            return false
-        end
-    end
-
-    if math.abs(other.ent.x - self.ent.x) == (self.w + other.w)/2 then
-        if self.ent[Player].sm:attr('air') or (axis == 'x' and self.ent[Player].sm.state == 'push') then
-            return self
-        else
-            return false
-        end
-    end
-
-    if self.ent.y < other.ent.y then
-        return self
-    end
-end
-
-function Player:grounded_update(sm)
-    local target = 0
-    local max_vel = 2
-
-    local wall = self.object:touch_wall()
-
-    local run = self.on_rope and 'rope_run' or 'run'
-    local idle = self.on_rope and 'rope_idle' or 'idle'
-
-    if down('a') and down('d') then
-        self.vel_x = approach(self.vel_x, 0, 0.4)
-        if sm.state ~= 'enter_cont' then
-            sm:change_state(idle)
-        end
-    elseif down('a') then
-        sm:change_state(wall and 'push' or run)
-        self.vel_x = approach(self.vel_x, -max_vel * (self.on_rope and 0.5 or 1), 0.4 * (self.on_rope and 0.5 or 1))
-        self.object.facing = -1
-    elseif down('d') then
-        sm:change_state(wall and 'push' or run)
-        self.vel_x = approach(self.vel_x, max_vel * (self.on_rope and 0.5 or 1), 0.4 * (self.on_rope and 0.5 or 1))
-        self.object.facing = 1
-    else
-        self.vel_x = approach(self.vel_x, 0, 0.4)
-        if sm.state ~= 'enter_cont' then
-            sm:change_state(idle)
-        end
-    end
-
-    if pressed('space') then
-        local res = self.object:touch_floor()
-        if res then
-            self.vel_y = -4 + math.min(res.move_y_amount * 0.75/2, 0)
-            self.vel_x = self.vel_x + res.move_x_amount
-            sm:change_state('up')
-            
-            scene.events:trigger('jump', {entity=self.ent, off=res.ent})
-            play_sound('DIABJump')
-
-            scene:add_entity(Entity:new({
-                x=self.ent.x,
-                y=self.ent.y-12,
-                layer=self.ent.layer
-            }):add_comp(Particle, {
-                file='res/images/particles.png', 
-                w=80, 
-                h=80,
-                frames={7, 8},
-                flip=self.object.facing == -1
-            }))
-        end
-    end
-
-    self:move_update(sm)
-end
-
-function Player:air_update(sm)
-    if self.sm.state ~= 'fancy_fall' then
-        local target = 0
-        local max_vel = sm:attr('super') and not self.cancel and 3 or 2
-        local target_vel = 2
-        if self.vel_x > max_vel then
-            self.vel_x = approach(self.vel_x, max_vel, 0.1)
-        elseif self.vel_x < -max_vel then
-            self.vel_x = approach(self.vel_x, -max_vel, 0.1)
-        elseif down('a') and down('d') then
-        elseif down('a') and self.left_lock <= 0 then
-            if self.vel_x > -target_vel then
-                self.vel_x = approach(self.vel_x, -target_vel, 0.3)
-            end
-        elseif down('d') and self.right_lock <= 0 then
-            if self.vel_x < target_vel then
-                self.vel_x = approach(self.vel_x, target_vel, 0.3)
-            end
-        elseif (self.vel_x > 0 and self.left_lock <= 0) or (self.vel_x < 0 and self.right_lock <= 0) then
-        end
-
-        if pressed('space') then
-            if self.hit_wall then
-                self:walljump(-3.6, 2, scene.heavy_walls and 0.5 or 0.65, 20)
-            elseif SAVE.MOTH_JUMP then
-                sm:change_state('super')
-            end
-        elseif pressed('k') and SAVE.PICK and self.sm.state ~= 'after_climb_jump' then
-            sm:change_state('pre_climb_jump')
-        end
-    end
-
-    self:move_update(sm)
-end
-
-function Player:move_update(sm)
-    if self.sm.state == 'rebound' or self.sm.state == 'climb_jump' then
-    elseif self.sm.state == 'slide' and self.vel_y >= 0 then
-        if SAVE.TICK_COUNT % 12 == 0 then
-            scene:add_entity(Entity:new({
-                x=self.ent.x,
-                y=self.ent.y,
-                layer=self.ent.layer
-            }):add_comp(Particle, {
-                file='res/images/particles.png', 
-                w=80, 
-                h=80,
-                frames={59, 60},
-                flip=self.object.facing == -1,
-                other=self.wall.ent
-            }))
-        end
-        self.vel_y = self.vel_y + 0.15/2
-    else
-        self.vel_y = self.vel_y + 0.15
-    end
-
-    if self.vel_y + self.ent.y < -10 then
-        self.vel_y = -10 - self.ent.y
-    end
-
-    self.hit_wall = false
-    self.hit_left_wall = false
-    self.hit_right_wall = false
-
-    local old_y = self.ent.y
-
-    if not self.roping then
-        self.object:move_x(self.vel_x)
-    end
-    self.on_rope = false
-    if self.roping then
-        self.object:move_y(2)
-    else
-        self.object:move_y(self.vel_y)
-    end
-
-    self:check_wall()
-    if self.state == 'rebound' then
-        return
-    end
-
-    if self.object.move_y_amount ~= 0 then
-        if (0 < (self.ent.y - old_y) * self.object.gravity and self.sm.state ~= 'slide') then
-            self.sm:change_state('down')
-        elseif 0 > (self.ent.y - old_y) * self.object.gravity and not self.sm:attr('air') then
-            self.sm:change_state('up')
-        end
-    end
-end
-
-function Player:check_wall()
-    local obj, obj2, obj3 = self.object:touch_wall()
-    if obj then
-        self.hit_left_wall = obj.ent.x < self.ent.x
-        self.hit_right_wall = obj.ent.x > self.ent.x
-        self.hit_wall = true
-        self.wall_direction = self.hit_left_wall and 1 or -1
-        self.wall = obj
-        self.wall2 = obj2
-        self.wall3 = obj3
-
-        if self.sm:attr('air') then
-            if self.sm.state == 'super' or self.sm.state == 'super_loop' then
-                self.sm:change_state('rebound')
-            else
-                self.sm:change_state('slide')
-            end
-        end
-    else
-        if self.sm.state == 'slide' then
-            self.sm:change_state(self.vel_y > 0 and 'down' or 'up')
-        end
-    end
-end
-
-function Player:update()
-    self.left_lock = self.left_lock - 1
-    self.right_lock = self.right_lock - 1
-
-    if self.ent.y > scene.kill_height and self.sm.state ~= 'squash' and self.sm.state ~= 'exit' then
-        -- play_sound('Fall_Off_'..math.random(2))
-        scene:restart(0, -1, true)
-    end
-
-    for i,p in ipairs(self.ghost) do
-        p[Sprite].frame = self.sprite.frame
-    end
-end
-
-function Player:render()
-
-end
-
-return Player
+Player = {}
+
+-- Maximum simultaneous ghost images. Must be >= GHOST_LENGTH / GHOST_EVERY_X_TICKS.
+-- 8 is generous for typical values and costs nothing when ghosts are inactive.
+local GHOST_POOL_SIZE = 8
+
+-- Localize frequently-called math globals.
+-- In LuaJIT, global table lookups (math.abs etc.) in hot per-frame paths
+-- cost more than upvalue reads. These run every frame during air movement.
+local math_abs   = math.abs
+local math_min   = math.min
+local math_max   = math.max
+local math_floor = math.floor
+
+-- Pre-computed constants — avoid re-doing these divisions every frame.
+local GRAVITY_SLIDE = 0.15 / 2   -- was 0.15/2 in move_update
+local WALL_VEL_MULT = 0.75 / 2   -- was 0.75/2 in walljump / grounded_update
+
+function Player:new(o)
+    o = o or {}
+
+    setmetatable(o, self)
+    self.__index = self
+
+    o:init()
+
+    return o
+end
+
+function Player:init()
+    _, self.sprite = self.ent:add_comp(Sprite, {file='res/images/reya_vault.png', w=48, h=48, offset_y=-11})
+
+    self.frames = {
+        idle = {0, 1, 2, 3, 4, 5},
+        run = {10, 11, 12, 13, 14, 15},
+
+        rope_idle = {320, 321, 322, 323, 324, 325},
+        rope_run = {310, 311, 312, 313, 314, 315},
+
+        push = {50, 51, 52, 53, 54, 55},
+        up = {21, 22},
+        down = {23, 24},
+        walljump = {31, 32, 33},
+        slide = {30},
+        super = {201, 202},
+        super_loop = {207, 208, 205, 206},
+        rebound = {42, 43},
+        rejump = {44, 45},
+        squash = {59, 60, 61, 62, 63, 64, 65, 66, 67, 68, 69, -1},
+        enter = {170, 171, 172, 173, 174, 175},
+        enter_cont = {176, 177, 178, 179, 180, 181, 182, 183},
+        exit = {160, 161, 162, 163, 164, 165, -1, -1},
+        pre_climb_jump = { 290, 291, 292, 293 },
+        pre_climb_jump_loop = { 294 },
+        climb_jump = { 300, 301 },
+        after_climb_jump = {302, 303, 304, 305, 305},
+        fancy_fall={207, 208, 205, 206},
+        fancy_flip={177, 178, 179, 180, 181, 182, 183},
+    }
+
+    -- OPTIMIZATION: Fixed-size ring buffer replaces the old self.ghost table.
+    -- The old system created a new Entity + Particle every few ticks, added it to
+    -- scene.entities, and never cleaned up dead entries from self.ghost — causing
+    -- unbounded table growth, GC pressure, and growing per-frame loop costs.
+    -- The pool allocates 8 plain data tables once and reuses them forever.
+    -- Ghosts are drawn directly in Player:render() — zero scene.entities overhead.
+    self.ghost_pool         = {}
+    self.ghost_pool_head    = 1
+    self.ghost_active_count = 0    -- skip pool loop entirely when no ghosts alive
+    self.flip_enabled       = SAVE.FLIP  -- cached at init; refreshed at super entry
+    self.wall_coyote_ticks  = 0  -- counts down after leaving a wall; allows late walljump
+    for i = 1, GHOST_POOL_SIZE do
+        self.ghost_pool[i] = {
+            active   = false,
+            x        = 0,
+            y        = 0,
+            frame    = 0,
+            alpha    = 0,
+            life     = 0,
+            flip     = false,
+            flip_y   = false,
+            offset_x = 0,
+            offset_y = 0,
+        }
+    end
+
+    _, self.sm = self.ent:add_comp(StateManager, {
+        state=self.fancy_fall and 'fancy_fall' or 'enter',
+        states={
+            fancy_flip={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate({177, 178, 179}, function()
+                        play_song('LockedUp.wav', {
+                            loop = true,
+                        })
+                        self.sprite:animate({180, 181, 182, 183}, function()
+                            sm:change_state('idle')
+                        end)
+                    end)
+                end,
+                update=function(sm)
+                end,
+            },
+
+            fancy_fall={
+                enter=function(sm)
+                    self.sprite:animate(self.frames.fancy_fall)
+                    self.sprite.continue = true
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                exit=function(sm)
+                    if sm.state ~= 'fancy_flip' then
+                        return true
+                    end
+                end,
+                air=true
+            },
+
+            enter={
+                enter=function(sm)
+                    play_sound('DIABWalkIn')
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.enter, function()
+                        sm:change_state('enter_cont')
+                    end)
+                end,
+                update=function(sm)
+                end,
+            },
+
+            enter_fancy={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate({-1})
+                end,
+                update=function(sm)
+                end,
+            },
+
+            exit={
+                enter=function(sm)
+                    play_sound('DIABWalkOut')
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.exit, function()
+                        -- if scene.next_song then
+                        --     play_song(scene.next_song, {
+                        --         loop = true,
+                        --     })
+                        -- end
+                        scene.events:add_timer(2, function()
+                            if scene.greeding then
+                                scene:next(false, false, true)
+                            else
+                                scene:next()
+                            end
+                        end)
+                    end)
+                    sm.states.exit.fade_out = 20
+                end,
+                update=function(sm)
+                    self.object:clean_up()
+
+                    local s = sm.states.exit
+                    if s.fade_out > 0 then
+                        s.fade_out = s.fade_out - 1
+
+                        if s.fade_out == 0 then
+                            -- if scene.next_song or scene.stop_song then
+                            --     song:stop(0.5)
+                            -- end
+                        end
+                    else
+                        scene.fade = math_max(scene.fade - 2*FADE_RATE, 0)
+                        FADE = scene.fade
+                    end
+                end,
+
+                exit=function(sm)
+                    return true
+                end,
+            },
+
+            exit_fancy={
+                enter=function(sm)
+                    -- play_sound('DIABWalkOut')
+                    self.sprite.continue = false
+                    self.sprite:animate({-1})
+
+                    local offset = scene.bottle_exit and 10 * 6 or 0
+
+                    sm.states.exit.fade_out = (30-#self.frames.exit)*6 + offset
+
+                    scene.events:add_timer((30)*6 + offset, function()
+                        scene.events:add_timer(60, function()
+                            -- if scene.next_song then
+                            --     play_song(scene.next_song, {
+                            --         loop = true,
+                            --     })
+                            --     song:pause()
+                            --     song:resume(1)
+                            -- end
+                            scene:next()
+                        end)
+                    end)
+                end,
+                update=function(sm)
+                    self.object:clean_up()
+
+                    local s = sm.states.exit
+                    if s.fade_out > 0 then
+                        s.fade_out = s.fade_out - 1
+                        -- if song then
+                        --     if s.fade_out == 0 and scene.next_song then
+                        --         song:stop(1)
+                        --     end
+                        -- end
+                    else
+                        scene.fade = math_max(scene.fade - 2*FADE_RATE, 0)
+                        FADE = scene.fade
+                    end
+                end,
+
+                exit=function(sm)
+                    return true
+                end,
+            },
+
+            enter_cont={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.enter_cont, function()
+                        sm:change_state('idle')
+                    end)
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end,
+            },
+
+            idle={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.idle)
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end,
+            },
+
+            idle_wait={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.idle)
+                end,
+                update=function(sm)
+                end,
+            },
+
+
+            run={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.run)
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end
+            },
+
+            rope_idle={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.rope_idle)
+                    self.sprite.offset_y = -10
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end,
+                exit=function(sm)
+                    self.sprite.offset_y = -11
+                end,
+            },
+
+            rope_run={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.rope_run)
+                    self.sprite.offset_y = -10
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end,
+                exit=function(sm)
+                    self.sprite.offset_y = -11
+                end,
+            },
+
+            push={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.push)
+                    self.sprite.offset_x = -2
+                end,
+                update=function(sm)
+                    self:grounded_update(sm)
+                end,
+                exit=function(sm)
+                    self.sprite.offset_x = 0
+                end
+            },
+
+
+            up={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.up)
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                air=true
+            },
+
+            walljump={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.walljump, function()
+                        sm:change_state('up')
+                    end)
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                air=true
+            },
+
+            super={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.super, function()
+                        sm:change_state('super_loop')
+                    end)
+                    self.sprite.offset_y = -5
+                    -- Refresh all SAVE caches in case they changed since init
+                    self.flip_enabled      = SAVE.FLIP
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                    if self.flip_enabled and SAVE.TICK_COUNT % GHOST_EVERY_X_TICKS == 0 and (math_abs(self.vel_x) > 2 or self.ghosting) then
+                        self.ghosting = true
+                        local g = self.ghost_pool[self.ghost_pool_head]
+                        self.ghost_pool_head = self.ghost_pool_head % GHOST_POOL_SIZE + 1
+                        if not g.active then
+                            self.ghost_active_count = self.ghost_active_count + 1
+                        end
+                        g.active   = true
+                        g.x        = self.ent.x
+                        g.y        = self.ent.y
+                        g.frame    = self.sprite.frame
+                        g.alpha    = GHOST_ALPHA
+                        g.life     = GHOST_LENGTH
+                        g.flip     = self.sprite.flip
+                        g.flip_y   = self.sprite.flip_y
+                        g.offset_x = self.sprite.offset_x
+                        g.offset_y = self.sprite.offset_y
+                    end
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and (sm.state ~= 'super_loop' and sm.state ~= 'rebound')  then
+                        return true
+                    end
+                    self.sprite.offset_y = -11
+                    if sm.state ~= 'super_loop' then
+                        self.ghosting = false
+                    end
+                end,
+                air=true,
+                super=true,
+            },
+
+            super_loop={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.super_loop)
+                    self.sprite.offset_y = -5
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                    if self.flip_enabled and SAVE.TICK_COUNT % GHOST_EVERY_X_TICKS == 0 and (math_abs(self.vel_x) > 2 or self.ghosting) then
+                        self.ghosting = true
+                        local g = self.ghost_pool[self.ghost_pool_head]
+                        self.ghost_pool_head = self.ghost_pool_head % GHOST_POOL_SIZE + 1
+                        if not g.active then
+                            self.ghost_active_count = self.ghost_active_count + 1
+                        end
+                        g.active   = true
+                        g.x        = self.ent.x
+                        g.y        = self.ent.y
+                        g.frame    = self.sprite.frame
+                        g.alpha    = GHOST_ALPHA
+                        g.life     = GHOST_LENGTH
+                        g.flip     = self.sprite.flip
+                        g.flip_y   = self.sprite.flip_y
+                        g.offset_x = self.sprite.offset_x
+                        g.offset_y = self.sprite.offset_y
+                    end
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and sm.state ~= 'rebound' then
+                        return true
+                    end
+                    self.sprite.offset_y = -11
+                    self.ghosting = false
+                end,
+                air=true,
+                super=true,
+            },
+
+            rebound={
+                enter=function(sm)
+                    self.vel_y = 0
+                    if self.hit_left_wall then
+                        self.object.facing = -1
+                    else
+                        self.object.facing = 1
+                    end
+
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.rebound, function()
+                        self:walljump(-4.8, 4, scene.heavy_walls and 0.75 or 0.85, 25, 'rejump')
+                        self.object.facing = -self.object.facing
+                        
+                        if self.buffer_super then
+                            sm:change_state('super')
+                        end
+                    end)
+
+                    self.sprite.offset_x = -4
+                    self.buffer_super = false
+                    self.cancel = false
+                end,
+                update=function(sm)
+                    self.vel_y = 0
+                    self:move_update(sm)
+                    if not (self.hit_left_wall or self.hit_right_wall) and self.sprite.f < 10 then
+                        sm:change_state('down')
+                        self.object.facing = -self.object.facing
+                    end
+
+                    if pressed('space') then
+                        self.buffer_super = true
+                    end
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and sm.state ~= 'rejump' and (self.hit_left_wall or self.hit_right_wall) then
+                        return true
+                    end
+                    self.sprite.offset_x = 0
+                end,
+                air=true,
+                super=true,
+                wall=true
+            },
+
+            rejump={
+                enter=function(sm)
+                    self.cancel = false
+                    self.cancel_count = self.cancel_count + 1
+                    local cancel_count = self.cancel_count
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.rejump, function()
+                        sm:change_state('up')
+                    end)
+                    if not SAVE.FLIP then
+                        scene.events:add_timer(#self.frames.rejump*6, function()
+                            if self.cancel_count == cancel_count then
+                                self.cancel = true
+                            end
+                        end)
+                    end
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                air=true,
+                super=true,
+            },
+
+            pre_climb_jump={
+                enter=function(sm)
+                    play_sound('deploy_pick', nil, 0.5)
+                    if self.hit_wall then
+                        sm:change_state("climb_jump")
+                        return
+                    end
+
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.pre_climb_jump, function()
+                        sm:change_state("pre_climb_jump_loop")
+                    end)
+                    if down('a') then
+                        self.vel_x = -math_abs(self.vel_x)
+                        self.object.facing = -1
+                    elseif down('d') then
+                        self.vel_x = math_abs(self.vel_x)
+                        self.object.facing = 1
+                    end
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                    if self.hit_wall then
+                        if self.hit_left_wall and self.object.facing == 1 then
+                            return
+                        elseif self.hit_right_wall and self.object.facing == -1 then
+                            return
+                        end
+                        sm:change_state('climb_jump')
+                    end
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and sm.state ~= 'pre_climb_jump_loop' and sm.state ~= 'climb_jump' then
+                        return true
+                    end
+                end,
+                air=true,
+                super=true
+            },
+
+            pre_climb_jump_loop={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.pre_climb_jump_loop)
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                    if self.hit_wall then
+                        if self.hit_left_wall and self.object.facing == 1 then
+                            return
+                        elseif self.hit_right_wall and self.object.facing == -1 then
+                            return
+                        end
+                        sm:change_state('climb_jump')
+                    end
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and sm.state ~= 'climb_jump' then
+                        return true
+                    end
+                end,
+                air=true,
+                super=true,
+            },
+
+            climb_jump={
+                enter=function(sm)
+                    play_sound('pick_sound', nil, 0.5)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.climb_jump, function()
+                        self.vel_y = -CLIMB_STRENGTH
+                        sm:change_state("after_climb_jump")
+
+                        if self.wall then
+                            scene.events:trigger('climb', {entity=self.ent, off=self.wall.ent})
+                        end
+                    end)
+                    self.vel_y = 0
+                    self.vel_x = self.wall_direction * CLIMB_PUSHOFF
+                    if self.hit_left_wall then
+                        self.right_lock = CLIMB_OFF_LOCK
+                        self.left_lock = CLIMB_LOCK
+                        self.object.facing = -1
+                    else
+                        self.left_lock = CLIMB_OFF_LOCK
+                        self.right_lock = CLIMB_LOCK
+                        self.object.facing = 1
+                    end
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                exit=function(sm)
+                    if sm:attr('air') and sm.state ~= 'after_climb_jump' and sm.state ~= 'walljump' then
+                        return true
+                    end
+                end,
+                air=true,
+                super=true,
+                wall=true
+            },
+
+            after_climb_jump={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.after_climb_jump, function()
+                        if self.hit_wall then
+                            sm.done = true
+                            sm:change_state("slide")
+                        else
+                            sm:change_state("up")
+                        end
+                    end)
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                    -- if not self.hit_wall then
+                    --     sm:change_state("up")
+                    -- end
+                end,
+                exit=function(sm)
+                    if sm.state == 'slide' and not sm.done then
+                        return true
+                    end
+                    sm.done = nil
+                end,
+                air=true,
+                super=true
+            },
+
+            down={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.down)
+                end,
+                update=function(sm)
+                    self:air_update(sm)
+                end,
+                air=true
+            },
+
+            slide={
+                enter=function(sm)
+                    self.sprite.continue = true
+                    self.sprite:animate(self.frames.slide)
+                    if self.hit_left_wall then
+                        self.object.facing = 1
+                    else
+                        self.object.facing = -1
+                    end
+                    self.sprite.offset_x = -3
+                end,
+                update=function(sm)
+                    if self.hit_left_wall then
+                        self.object.facing = 1
+                    else
+                        self.object.facing = -1
+                    end
+                    self:air_update(sm)
+                end,
+                exit=function(sm)
+                    self.sprite.offset_x = 0
+                end,
+                air=true,
+                wall=true,
+            },
+
+            squash={
+                enter=function(sm)
+                    self.sprite.continue = false
+                    self.sprite:animate(self.frames.squash, function()
+                        scene:again()
+                    end)
+                    sm.states.squash.count_down = 12
+                    sm.states.squash.explode_count = 36
+                    sm.states.squash.fade_out = 42
+                end,
+                update=function(sm)
+                    self.object:clean_up()
+                    local s = sm.states.squash
+                    if s.count_down > 0 then
+                        self.object:move_x(s.x_dir * s.count_down / 12 * 4)
+                        self.object:move_y(s.y_dir * s.count_down / 12 * 2)
+                        s.count_down = s.count_down - 1
+                    end
+                    if s.explode_count > 0 then
+                        s.explode_count = s.explode_count - 1
+                        if s.explode_count == 6 then
+                            play_sound("DIABExplosion")
+                        elseif s.explode_count == 0 then
+                            scene.shake = 2
+                        end
+                    end
+
+                    if s.fade_out > 0 then
+                        s.fade_out = s.fade_out - 1
+                    else
+                        scene.fade = math_max(scene.fade - 2*FADE_RATE, 0)
+                        FADE = scene.fade
+                    end
+                end,
+                exit=function(sm)
+                    return true
+                end,
+            }
+        }
+    })
+    _, self.object = self.ent:add_comp(Object, {
+        w=14,
+        h=26,
+        object_list=Object.player_list,
+        is_riding=Player.is_riding,
+    })
+    -- self.ent:add_comp(Rectangle, {
+    --     width=14,
+    --     height=26
+    -- })
+    self.object.facing = self.fancy_fall and -1 or 1
+
+    self.vel_x = 0
+    self.vel_y = 0
+
+    self.right_lock = 0
+    self.left_lock = 0
+    self.cancel_count = 0
+
+    self.ent.tag = 'player'
+end
+
+function Player:kill(x_dir, y_dir, no_sound)
+    if self.sm.state == 'squash' or self.sm.state == 'exit' then
+        return
+    end
+
+    local s = self.sm.states.squash
+    s.x_dir = x_dir
+    s.y_dir = y_dir
+
+    if not no_sound then
+        play_sound("DIABHitBySpikes")
+    end
+    self.sm:change_state('squash')
+end
+
+function Player:win(fancy)
+    if self.sm.state == 'exit' then
+        return
+    end
+    self.sm:change_state(fancy and 'exit_fancy' or 'exit')
+end
+
+function Player:walljump(vel_y, vel_x, vel_wall, lock, state)
+    if not self.wall then
+        return
+    end
+    self.vel_y = vel_y + math_min(self.wall.move_y_amount * WALL_VEL_MULT, 0)
+    self.vel_x = vel_x * self.wall_direction + self.wall.move_x_amount
+    self.sm:change_state(state or 'walljump')
+    if state then
+        play_sound('DIABHyperJump')
+
+        scene:add_entity(Entity:new({
+            x=self.ent.x,
+            y=self.ent.y,
+            layer=self.ent.layer
+        }):add_comp(Particle, {
+            file='res/images/particles.png', 
+            w=80, 
+            h=80,
+            frames={28, 29, 30, 31, 32, 33, 34},
+            flip=self.object.facing == -1,
+            other=self.wall.ent
+        }))
+
+        if self.object.facing == -1 then
+            scene.shake_right = 1
+        else
+            scene.shake_left = 1
+        end
+    else
+        play_sound('DIABJump')
+
+        scene:add_entity(Entity:new({
+            x=self.ent.x,
+            y=self.ent.y,
+            layer=self.ent.layer
+        }):add_comp(Particle, {
+            file='res/images/particles.png', 
+            w=80, 
+            h=80,
+            frames={56, 57},
+            flip=self.object.facing == 1,
+            other=self.wall.ent
+        }))
+    end
+
+    scene.events:trigger('walljump', {entity=self.ent, off=self.wall.ent, push=-self.wall_direction * vel_wall})
+    if self.wall2 then
+        scene.events:trigger('walljump', {entity=self.ent, off=self.wall2.ent, push=-self.wall_direction * vel_wall})
+    end
+    if self.wall3 then
+        scene.events:trigger('walljump', {entity=self.ent, off=self.wall3.ent, push=-self.wall_direction * vel_wall})
+    end
+    if self.wall.ent.left or self.wall.ent.right then
+        scene:push(self.wall.ent.left, self.wall.ent.left and -vel_wall or vel_wall)
+        -- play_sound("jump_off_wall", nil, state and 0.5 or 0.2)
+    end
+
+    if self.hit_left_wall then
+        self.left_lock = lock
+        self.right_lock = 0
+    else
+        self.right_lock = lock
+        self.left_lock = 0
+    end
+    self.hit_wall = false
+    self.hit_left_wall = false
+    self.hit_right_wall = false
+end
+
+function Player.is_riding(self, other, axis)
+    local s = other.ent[Sword]
+    if s then
+        if s.vel_x ~= 0 then
+            return false
+        end
+    end
+
+    if math_abs(other.ent.x - self.ent.x) == (self.w + other.w)/2 then
+        if self.ent[Player].sm:attr('air') or (axis == 'x' and self.ent[Player].sm.state == 'push') then
+            return self
+        else
+            return false
+        end
+    end
+
+    if self.ent.y < other.ent.y then
+        return self
+    end
+end
+
+function Player:grounded_update(sm)
+    local target = 0
+    local max_vel = 2
+
+    local wall = self.object:touch_wall()
+
+    local run = self.on_rope and 'rope_run' or 'run'
+    local idle = self.on_rope and 'rope_idle' or 'idle'
+
+    if down('a') and down('d') then
+        self.vel_x = approach(self.vel_x, 0, 0.4)
+        if sm.state ~= 'enter_cont' then
+            sm:change_state(idle)
+        end
+    elseif down('a') then
+        sm:change_state(wall and 'push' or run)
+        self.vel_x = approach(self.vel_x, -max_vel * (self.on_rope and 0.5 or 1), 0.4 * (self.on_rope and 0.5 or 1))
+        self.object.facing = -1
+    elseif down('d') then
+        sm:change_state(wall and 'push' or run)
+        self.vel_x = approach(self.vel_x, max_vel * (self.on_rope and 0.5 or 1), 0.4 * (self.on_rope and 0.5 or 1))
+        self.object.facing = 1
+    else
+        self.vel_x = approach(self.vel_x, 0, 0.4)
+        if sm.state ~= 'enter_cont' then
+            sm:change_state(idle)
+        end
+    end
+
+    if pressed('space') then
+        local res = self.object:touch_floor()
+        if res then
+            self.vel_y = -4 + math_min(res.move_y_amount * WALL_VEL_MULT, 0)
+            self.vel_x = self.vel_x + res.move_x_amount
+            sm:change_state('up')
+            
+            scene.events:trigger('jump', {entity=self.ent, off=res.ent})
+            play_sound('DIABJump')
+
+            scene:add_entity(Entity:new({
+                x=self.ent.x,
+                y=self.ent.y-12,
+                layer=self.ent.layer
+            }):add_comp(Particle, {
+                file='res/images/particles.png', 
+                w=80, 
+                h=80,
+                frames={7, 8},
+                flip=self.object.facing == -1
+            }))
+        end
+    end
+
+    self:move_update(sm)
+end
+
+function Player:air_update(sm)
+    if self.sm.state ~= 'fancy_fall' then
+        local target = 0
+        local max_vel = sm:attr('super') and not self.cancel and 3 or 2
+        local target_vel = 2
+        if self.vel_x > max_vel then
+            self.vel_x = approach(self.vel_x, max_vel, 0.1)
+        elseif self.vel_x < -max_vel then
+            self.vel_x = approach(self.vel_x, -max_vel, 0.1)
+        elseif down('a') and down('d') then
+        elseif down('a') and self.left_lock <= 0 then
+            if self.vel_x > -target_vel then
+                self.vel_x = approach(self.vel_x, -target_vel, 0.3)
+            end
+        elseif down('d') and self.right_lock <= 0 then
+            if self.vel_x < target_vel then
+                self.vel_x = approach(self.vel_x, target_vel, 0.3)
+            end
+        elseif (self.vel_x > 0 and self.left_lock <= 0) or (self.vel_x < 0 and self.right_lock <= 0) then
+        end
+
+        if pressed('space') then
+            -- wall_coyote_ticks > 0 gives a short grace window after leaving a wall,
+            -- so a walljump input that arrives 1-6 frames late still fires correctly.
+            -- Walljump always takes priority over moth jump during the coyote window.
+            if self.hit_wall or self.wall_coyote_ticks > 0 then
+                self:walljump(-3.6, 2, scene.heavy_walls and 0.5 or 0.65, 20)
+                self.wall_coyote_ticks = 0  -- consume the coyote window immediately
+            elseif SAVE.MOTH_JUMP then
+                sm:change_state('super')
+            end
+        elseif pressed('k') and SAVE.PICK and self.sm.state ~= 'after_climb_jump' then
+            sm:change_state('pre_climb_jump')
+        end
+    end
+
+    self:move_update(sm)
+end
+
+function Player:move_update(sm)
+    if self.sm.state == 'rebound' or self.sm.state == 'climb_jump' then
+    elseif self.sm.state == 'slide' and self.vel_y >= 0 then
+        if SAVE.TICK_COUNT % 12 == 0 then
+            scene:add_entity(Entity:new({
+                x=self.ent.x,
+                y=self.ent.y,
+                layer=self.ent.layer
+            }):add_comp(Particle, {
+                file='res/images/particles.png', 
+                w=80, 
+                h=80,
+                frames={59, 60},
+                flip=self.object.facing == -1,
+                other=self.wall.ent
+            }))
+        end
+        self.vel_y = self.vel_y + GRAVITY_SLIDE
+    else
+        self.vel_y = self.vel_y + 0.15
+    end
+
+    if self.vel_y + self.ent.y < -10 then
+        self.vel_y = -10 - self.ent.y
+    end
+
+    self.hit_wall = false
+    self.hit_left_wall = false
+    self.hit_right_wall = false
+
+    local old_y = self.ent.y
+
+    if not self.roping then
+        self.object:move_x(self.vel_x)
+    end
+    self.on_rope = false
+    if self.roping then
+        self.object:move_y(2)
+    else
+        self.object:move_y(self.vel_y)
+    end
+
+    self:check_wall()
+    if self.state == 'rebound' then
+        return
+    end
+
+    if self.object.move_y_amount ~= 0 then
+        if (0 < (self.ent.y - old_y) * self.object.gravity and self.sm.state ~= 'slide') then
+            self.sm:change_state('down')
+        elseif 0 > (self.ent.y - old_y) * self.object.gravity and not self.sm:attr('air') then
+            self.sm:change_state('up')
+        end
+    end
+end
+
+function Player:check_wall()
+    local obj, obj2, obj3 = self.object:touch_wall()
+    if obj then
+        self.hit_left_wall = obj.ent.x < self.ent.x
+        self.hit_right_wall = obj.ent.x > self.ent.x
+        self.hit_wall = true
+        self.wall_direction = self.hit_left_wall and 1 or -1
+        self.wall = obj
+        self.wall2 = obj2
+        self.wall3 = obj3
+        self.wall_coyote_ticks = 0  -- touching wall resets coyote window
+
+        if self.sm:attr('air') then
+            if self.sm.state == 'super' or self.sm.state == 'super_loop' then
+                self.sm:change_state('rebound')
+            else
+                self.sm:change_state('slide')
+            end
+        end
+    else
+        if self.sm.state == 'slide' then
+            -- Start coyote window: player just left the wall, give 6 ticks
+            -- to still fire a walljump (matches original wall_cling_duration intent)
+            self.wall_coyote_ticks = 6
+            self.sm:change_state(self.vel_y > 0 and 'down' or 'up')
+        end
+    end
+end
+
+function Player:update()
+    self.left_lock = self.left_lock - 1
+    self.right_lock = self.right_lock - 1
+
+    if self.wall_coyote_ticks > 0 then
+        self.wall_coyote_ticks = self.wall_coyote_ticks - 1
+    end
+
+    if self.ent.y > scene.kill_height and self.sm.state ~= 'squash' and self.sm.state ~= 'exit' then
+        -- play_sound('Fall_Off_'..math.random(2))
+        scene:restart(0, -1, true)
+    end
+
+    -- Tick down active ghost pool slots — skipped entirely when none are active
+    if self.ghost_active_count > 0 then
+        local pool = self.ghost_pool
+        for i = 1, GHOST_POOL_SIZE do
+            local g = pool[i]
+            if g.active then
+                g.life = g.life - 1
+                if g.life <= 0 then
+                    g.active = false
+                    self.ghost_active_count = self.ghost_active_count - 1
+                end
+            end
+        end
+    end
+end
+
+function Player:render()
+    -- Skip entirely when no ghosts are alive — avoids all loop and state overhead
+    if self.ghost_active_count == 0 then return end
+
+    local pool  = self.ghost_pool
+    local drawn = false
+
+    for i = 1, GHOST_POOL_SIZE do
+        local g = pool[i]
+        if g.active and g.frame >= 0 then
+            local spr = self.sprite
+            if not drawn then
+                if current_shader ~= spr.shader then
+                    love.graphics.setShader(spr.shader)
+                    current_shader = spr.shader
+                end
+                drawn = true
+            end
+            love.graphics.setColor(1, 1, 1, g.alpha)
+            love.graphics.draw(
+                spr.image,
+                spr.quads[g.frame + 1],
+                g.x + g.offset_x * (g.flip   and -1 or 1),
+                g.y + g.offset_y * (g.flip_y  and -1 or 1),
+                0,
+                g.flip   and -1 or 1,
+                g.flip_y and -1 or 1,
+                spr.w/2, spr.h/2
+            )
+        end
+    end
+
+    if drawn then
+        love.graphics.setColor(1, 1, 1, 1)
+    end
+end
+
+return Player
\ No newline at end of file
--- original/objects/push.lua
+++ modified/objects/push.lua
@@ -1,3 +1,9 @@
+-- OPTIMIZATION: Local caching for performance
+local floor = math.floor
+local insert = table.insert
+local remove = table.remove
+local abs = math.abs
+
 Push = {}
 
 local looks = {
@@ -10,7 +16,7 @@
 local actual_looks = {}
 for tiles, dir in pairs(looks) do
     for i, t in ipairs(tiles) do
-        table.insert(all_tiles, t)
+        insert(all_tiles, t)
         actual_looks[t] = dir
     end
 end
@@ -32,42 +38,65 @@
     local closed = {}
     local res = {}
 
-    if e and not e.taken and not scene.loaded_push[math.floor((x) / 16)..'-'..math.floor((y) / 16)] then
+    -- OPTIMIZATION: Pre-calculate initial tile coordinates
+    local init_tx = floor(x / 16)
+    local init_ty = floor(y / 16)
+    local init_key = init_tx * 10000 + init_ty  -- Use numeric key instead of string
+    
+    if e and not e.taken and not scene.loaded_push[init_key] then
         open = {{e, 0, 0}}
-        table.insert(res, e)
+        insert(res, e)
     end
 
+    -- OPTIMIZATION: Pre-define direction offsets to avoid recreating table
+    local dirs = {{16, 0}, {-16, 0}, {0, 16}, {0, -16}}
+    
     while open[1] do
-        local e, off_x, off_y = unpack(table.remove(open))
+        local curr = remove(open, 1)
+        local e, off_x, off_y = curr[1], curr[2], curr[3]
         e.taken = true
 
-        local t = scene:get_tile(math.floor((x + off_x) / 16), math.floor((y + off_y) / 16), 'Foreground')
+        -- OPTIMIZATION: Cache tile coordinates to avoid repeated floor calculations
+        local curr_tx = floor((x + off_x) / 16)
+        local curr_ty = floor((y + off_x) / 16)
+        local t = scene:get_tile(curr_tx, curr_ty, 'Foreground')
         local dir = actual_looks[t] or {}
-        for i, v in ipairs({{16, 0}, {-16, 0}, {0, 16}, {0, -16}}) do
-            local n = scene:lookup('Foreground', x + off_x + v[1], y + off_y + v[2])
-            if n and not closed[n] and not n.taken then
-                if not scene.loaded_push[math.floor((x + off_x + v[1]) / 16)..'-'..math.floor((y + off_y + v[2]) / 16)] then
-                    local t2 = scene:get_tile(math.floor((x + off_x + v[1]) / 16), math.floor((y + off_y + v[2]) / 16), 'Foreground')
-                    if false then--actual_looks[t2] then
-                        local good_lookup = false
-                        for j, d in ipairs(dir) do
-                            if d[1] * 16 == v[1] and d[2] * 16 == v[2] then
-                                good_lookup = true
-                                break
-                            end
+        
+        for i=1, 4 do
+            local v = dirs[i]
+            local new_off_x = off_x + v[1]
+            local new_off_y = off_y + v[2]
+            
+            -- OPTIMIZATION: Calculate tile coords once
+            local new_tx = floor((x + new_off_x) / 16)
+            local new_ty = floor((y + new_off_y) / 16)
+            local new_key = new_tx * 10000 + new_ty  -- Numeric key instead of string
+            
+            local n = scene:lookup('Foreground', x + new_off_x, y + new_off_y)
+            
+            if n and not closed[n] and not n.taken and not scene.loaded_push[new_key] then
+                local t2 = scene:get_tile(new_tx, new_ty, 'Foreground')
+                
+                if false then  -- actual_looks[t2] then
+                    local good_lookup = false
+                    for j=1, #dir do
+                        local d = dir[j]
+                        if d[1] * 16 == v[1] and d[2] * 16 == v[2] then
+                            good_lookup = true
+                            break
                         end
-                        if good_lookup then
-                            table.insert(open, {n, off_x + v[1], off_y + v[2]})
-                            scene.loaded_push[math.floor((x + off_x + v[1]) / 16)..'-'..math.floor((y + off_y + v[2]) / 16)] = true
-                            closed[n] = true
-                            table.insert(res, n)
-                        end
-                    else
-                        table.insert(open, {n, off_x + v[1], off_y + v[2]})
-                        scene.loaded_push[math.floor((x + off_x + v[1]) / 16)..'-'..math.floor((y + off_y + v[2]) / 16)] = true
+                    end
+                    if good_lookup then
+                        insert(open, {n, new_off_x, new_off_y})
+                        scene.loaded_push[new_key] = true
                         closed[n] = true
-                        table.insert(res, n)
+                        insert(res, n)
                     end
+                else
+                    insert(open, {n, new_off_x, new_off_y})
+                    scene.loaded_push[new_key] = true
+                    closed[n] = true
+                    insert(res, n)
                 end
             end
         end
@@ -92,8 +121,12 @@
             return
         end
 
-        for i,e in ipairs(self.moving_ents) do
-            if math.abs(e.x - data.ent.x) <= 16 and math.abs(e.y - data.ent.y) <= 16 then
+        -- OPTIMIZATION: Cache length and entity position
+        local moving_count = #self.moving_ents
+        local data_x, data_y = data.ent.x, data.ent.y
+        for i=1, moving_count do
+            local e = self.moving_ents[i]
+            if abs(e.x - data_x) <= 16 and abs(e.y - data_y) <= 16 then
                 self:activate()
                 break
             end
@@ -105,8 +138,12 @@
             return
         end
 
-        for i,e in ipairs(self.moving_ents) do
-            if math.abs(e.x - data.ent.x) <= 16 and math.abs(e.y - data.ent.y) <= 16 then
+        -- OPTIMIZATION: Cache length and entity position
+        local moving_count = #self.moving_ents
+        local data_x, data_y = data.ent.x, data.ent.y
+        for i=1, moving_count do
+            local e = self.moving_ents[i]
+            if abs(e.x - data_x) <= 16 and abs(e.y - data_y) <= 16 then
                 self:activate()
                 break
             end
@@ -118,8 +155,12 @@
             return
         end
 
-        for i,e in ipairs(self.moving_ents) do
-            if math.abs(e.x - data.ent.x) <= 16 and math.abs(e.y - data.ent.y) <= 16 then
+        -- OPTIMIZATION: Cache length and entity position
+        local moving_count = #self.moving_ents
+        local data_x, data_y = data.ent.x, data.ent.y
+        for i=1, moving_count do
+            local e = self.moving_ents[i]
+            if abs(e.x - data_x) <= 16 and abs(e.y - data_y) <= 16 then
                 self:activate()
                 break
             end
@@ -159,8 +200,10 @@
         end
 
         local joint = {}
-        for i, p in ipairs(self.moving_ents) do
-            local ent = p
+        -- OPTIMIZATION: Cache array length
+        local moving_count = #self.moving_ents
+        for i=1, moving_count do
+            local ent = self.moving_ents[i]
             ent.master = self
             if not ent[Object] then
                 local solid = not tablefind(nothing, ent[Sprite].frame)
@@ -173,7 +216,7 @@
                 })
                 ent.tag = solid and 'push' or 'DO-NOTHING'
             end
-            table.insert(joint, ent[Object])
+            insert(joint, ent[Object])
             ent[Object].joint = joint
             ent[Object].check_list = Object.object_and_player_list
         end
@@ -191,9 +234,12 @@
         local dir = SAVE.TICK_COUNT % 4 < 2 and 1 or -1
         self.offset_x = self.offset_x * (1-self.rumble) + math.cos(x) * 2 * self.rumble
         self.offset_y = self.offset_y * (1-self.rumble) + math.sin(x) * 2 * self.rumble
-        for i, e in ipairs(self.moving_ents) do
-            e[Sprite].offset_x = math.floor(self.offset_x)
-            e[Sprite].offset_y = math.floor(self.offset_y)
+        -- OPTIMIZATION: Cache array length
+        local moving_count = #self.moving_ents
+        for i=1, moving_count do
+            local e = self.moving_ents[i]
+            e[Sprite].offset_x = floor(self.offset_x)
+            e[Sprite].offset_y = floor(self.offset_y)
         end
     end
 
@@ -201,9 +247,11 @@
         self.rumble = math.min(1, self.rumble + self.rumble_acc/10)
         if self.rumble <= 0 then
             self.rumble = false
-            for i, e in ipairs(self.moving_ents) do
-                e[Sprite].offset_x = 0
-                e[Sprite].offset_y = 0
+            -- OPTIMIZATION: Cache array length
+            local moving_count = #self.moving_ents
+            for i=1, moving_count do
+                self.moving_ents[i][Sprite].offset_x = 0
+                self.moving_ents[i][Sprite].offset_y = 0
             end
         end
     end
@@ -246,7 +294,12 @@
 
 register_tiles(all_tiles, function(scene, tile, x, y, layer)
     scene.loaded_push = scene.loaded_push or {}
-    if scene.loaded_push[math.floor(x/16)..'-'..math.floor(y/16)] then
+    -- OPTIMIZATION: Use numeric key instead of string concatenation
+    local tile_x = floor(x/16)
+    local tile_y = floor(y/16)
+    local key = tile_x * 10000 + tile_y
+    
+    if scene.loaded_push[key] then
         return
     end
 
@@ -257,8 +310,8 @@
     }):add_comp(Push, {
         layer=layer.name,
         tile=tile,
-        tile_x=math.floor(x/16),
-        tile_y=math.floor(y/16),
+        tile_x=tile_x,
+        tile_y=tile_y,
         scene=scene
     }))
 
@@ -343,4 +396,4 @@
     end
 
     return true
-end)
+end)
\ No newline at end of file
--- original/objects/saw.lua
+++ modified/objects/saw.lua
@@ -1,65 +1,69 @@
-Saw = {}
-
-function Saw:new(o)
-    o = o or {}
-
-    setmetatable(o, self)
-    self.__index = self
-
-    o:init()
-
-    return o
-end
-
-function Saw:init()
-    _, self.sprite = self.ent:add_comp(Sprite, {
-        file='res/usable_files/saw.png', 
-        w=32, 
-        h=32,
-        frame=0,
-        continue=true
-    })
-    self.sprite:animate({0, 1})
-    -- self.sprite.f = math.random(0,35)
-
-    local w = 12
-    local obj = self.scene:lookup(self.layer, self.ent.x - 40, self.ent.y - 8)
-    if obj and obj[Saw] then
-        w = 32
-    end
-
-    _, self.object = self.ent:add_comp(Object, {
-        w=w,
-        h=12,
-        pass_through=true,
-        check_list=Object.player_list,
-    })
-
-    self.ent.tag = 'saw'
-end
-
-register_tiles({64, 65, 74, 75}, function(scene, tile, x, y, layer)
-    if tile ~= 64 then
-        return
-    end
-
-    return scene:add_entity(Entity:new({
-        x=x+8,
-        y=y+8,
-        layer=entity_layers[layer.name]
-    }):add_comp(Saw, {scene=scene, layer=layer.name}))
-end)
-
-register_collision('saw', 'player', function(solid, player, solid_x, solid_y, player_x, player_y)
-    local difx = player.x - solid.x
-    if difx > 12 then
-        return false
-    end
-
-    local dify = player.y - solid.y
-    local dis = math.max(math.sqrt(difx * difx + dify * dify), 1)
-
-    scene:restart(difx / dis, dify / dis)
-
-    return false
-end)
+Saw = {}
+
+function Saw:new(o)
+    o = o or {}
+
+    setmetatable(o, self)
+    self.__index = self
+
+    o:init()
+
+    return o
+end
+
+function Saw:init()
+    _, self.sprite = self.ent:add_comp(Sprite, {
+        file='res/usable_files/saw.png', 
+        w=32, 
+        h=32,
+        frame=0,
+        continue=true
+    })
+    self.sprite:animate({0, 1})
+
+    local w = 12
+    local obj = self.scene:lookup(self.layer, self.ent.x - 40, self.ent.y - 8)
+    if obj and obj[Saw] then
+        w = 32
+    end
+
+    _, self.object = self.ent:add_comp(Object, {
+        w=w,
+        h=12,
+        pass_through=true,
+        check_list=Object.player_list,
+    })
+
+    self.ent.tag = 'saw'
+end
+
+register_tiles({64, 65, 74, 75}, function(scene, tile, x, y, layer)
+    if tile ~= 64 then
+        return
+    end
+
+    -- FIX: spawn at (x+8, y) not (x+8, y+8).
+    -- track_piece looks up the saw in lookup_table using the tile's pixel centre
+    -- (x, y) and then moves it toward TrackPiece.ent.y, which is also ty*16+8 = y.
+    -- The original +8 on y placed the saw 8px below where track_piece expected it,
+    -- so the movement logic could never reach its target and the saw stayed static.
+    return scene:add_entity(Entity:new({
+        x=x+8,
+        y=y,
+        layer=entity_layers[layer.name]
+    }):add_comp(Saw, {scene=scene, layer=layer.name}))
+end)
+
+register_collision('saw', 'player', function(solid, player, solid_x, solid_y, player_x, player_y)
+    local difx = player.x - solid.x
+    if difx > 12 then
+        return false
+    end
+
+    local dify = player.y - solid.y
+    local dis = math.max(math.sqrt(difx * difx + dify * dify), 1)
+
+    scene:restart(difx / dis, dify / dis)
+
+    return false
+end)
--- original/objects/skeleton.lua
+++ modified/objects/skeleton.lua
@@ -1,350 +1,415 @@
-Skeleton = {}
-
-lines = {
-    "Actually, I've been working on a tight five. Tell you what, I'll tell you how to use those boots if you listen to my set.",
-    "So fellas, you might be wondering why I'm not wearing any pants.||| Well, lets just say I didn't think it'd be THIS kind of dungeon, if you catch my drift!||| Where my boys at? YOU know what I'm talking about.",
-    "Its not a total bust though, I met a fossil on the way in.||| I said, hey girl, are you feelin okay, cause it looks like jur ass SICK.||| Am I right fellas?",
-    "I'd tell you to tip your waitress, but my drink hasn't gotten here yet,||| and I ordered it when I was alive.",
-    "But seriously, if you see her,||| tell her to change my death in the afternoon to a life in as soon as possible.",
-    "and before you ask me what I died of, I bet it was less nasty than what you were BORN of. That is to say, your mother!",
-    "Thank you, thank you! That's my time!|| I'll be here...|| well, forever, probably!"
-}
-
-function range(n, shift)
-    shift = shift or 0
-    local r = {}
-    for i=shift,n do
-        table.insert(r, i)
-    end
-    for i=0,shift-1 do
-        table.insert(r, i)
-    end
-    return r
-end
-
-local idles = {
-    [355] = {80},
-    [365] = {60},
-    [356] = {120}
-}
-
-local post_idles = {
-    [365] = {50},
-    [355] = {70},
-    [356] = {110}
-}
-
-local steal = {
-    [365] = range(45),
-    [355] = range(69),
-    [356] = range(103),
-}
-
-local sfx = {
-    [355] = {
-        [22]='DIABEatFloor',
-        [26]='DIABEatFloor',
-        [16]='Reya_Land',
-        [63]='Reya_Land',
-    },
-    [356] = {},
-    [365] = {}
-}
-
-local talking = {
-    [365] = {50, 51, 52},
-    [355] = {70, 71, 72, 73},
-    [356] = {110, 111, 112, 113},
-}
-
-local files = {
-    [365] = {file='res/images/rob_pick_sheet.png', w=64, h=48, offset_y=-16, offset_x=7},
-    [356] = {file='res/images/steal_ring_sheet.png', w=80, h=48, offset_y=-16, offset_x=0},
-    [355] = {file='res/images/boot_skeleton_sheet.png', w=80, h=48, offset_y=-16, offset_x=0},
-}
-
-local offset = {
-    [365] = 3,
-    [355] = 8,
-    [356] = 14,
-}
-
-local power = {
-    [355] = 'MOTH_JUMP',
-    [356] = 'FLIP',
-    [365] = 'PICK',
-}
-
-local function ring_question(response)
-    print(response)
-    if response then
-        scene.story = true
-        scene.skeleton.lines = {
-            {reya=true, text="Hey Ravioli, how do you use this ring again?"},
-            "It's Rivolo.",
-            {reya=true, text="Right! Sorry. How do you use this Rivolo Ring again?"},
-            "...",
-            {reya=true, text="Oh nevermind. I just remembered. If I flip right after doing a moth jump, I can maintain my maximum speed during the entirety of the jump, helping me to cross wider gaps."},
-            "What's a moth jump?",
-            {reya=true, text="Farewell, Ravioli!"},
-        }
-
-        return {
-            "Countless years ago, the people of this town lived in luxury. There were two bus stops, and garbage was collected every other week instead once a year as is customary. It was easy pickin' for the likes of us.",
-            "On a particularly hot evening, I found my way into an old wizard's shop. Rummaging through all of the curios, one chest stood out to me.",
-            "It had an extremely elaborate lock. I knew it would take me until morning to pick it, but I had to have whatever was inside. You know how it is.",
-            {reya=true, text="Sure!"},
-            "Well, I cracked the lock just before dawn. Inside was that ring. I snatched it up, and made my escape, hoping to make it back to the guild before morning.  But that night... morning never came. No morning like I've seen.",
-            "I was struck with a feeling like the wind twisted out of my body. As I fled the shop, I saw crowds of people, all reeling, as if recovering from the same feeling I just shook.",
-            "As I ran back here, no one stopped me, no one batted an eye. Everyone was looking up, but there was no sun, there were no stars. Just a blanket of dim green light. I stumbled into an empty guild, and I've been hiding here ever since.",
-            {reya=true, text="So.|.|.| you're NOT the Chief Thief?"},
-            "Well, no, but do you see any OTHER animated skeletons around here?",
-            {reya=true, text="Whatever, bones. How do I use this ring?"},
-            "That ring makes you more aerodynamic. While flipping in the air, you won't lose any speed due to drag. It really helps cover more distance. Also, if you land on a rope while going that fast, it will launch you upward.", 
-            "Try jumping as hard as you can off of that wall, then immediately flipping.",
-            {reya=true, text="Neat, thanks!"},
-            "When you escape from here, make sure everyone in the land knows my story. The story of Rivolo Darikin.",
-            {reya=true, text="The Story of Ravioli Dairy-King. Got it."},
-            "No wai-",
-            {reya=true, text="Farewell Ravioli!"},
-        }
-    else
-        -- print('here1')
-        scene.skeleton.lines = {
-            {reya=true, text="Will you PLEASE just tell me how this ring works?"},
-            {text="Only if you listen to my tale!#", question=ring_question},
-        }
-        -- print('here2')
-        return {
-            {reya=true, text="Sorry, Bones. I'm in a hurry."},
-            "You'll be back!"
-        }
-    end
-    print('here3')
-end
-
-local function boot_question(response)
-    if response then
-        scene.comedy = true
-        scene.skeleton.lines = {
-            {text="How do you use these again?", reya=true},
-            "If you try to jump while you're in the air already, you'll start flipping. If you run into a wall while flipping, you'll do what I like to call a 'Moth Jump'. Give it a try!"
-        }
-        return {
-            "Ah great! Thanks a bunch. Okay here it goes...",
-            "AHEM. Hey everybody, tonight we have a very special guest who's going to really rattle your cage! Your ribcage, that is! Give it up for Clint Ivory!",
-{text="...", reya=true},
-            "So fellas, you might be wondering why I'm not wearing any pants.||| Well, lets just say when I came here I didn't think it'd be THIS kind of dungeon, if you catch my drift!||| Where my boys at? YOU know what I'm talking about.",
-{text="Hmm...", reya=true},
-            "Its not a total bust though, I met a fossil on the way in.||| I said, hey girl, are you feelin okay, cause it looks like jur ass SICK.|||",
-{text="(sigh...)", reya=true},
-            "and before you ask me what I died of, I bet it was less nasty than what you were BORN of.",
-            "They're shining the light in my eye! That's my time!|| I'll be here...|| well, forever, probably!",
-            "So, what'd you think? Did you get that last one? It's a \"your mother is ugly\" joke.",
-            {text="Yeah I caught that.||| Some of that seemed a little.|.|.| dated.|.|.| but I guess it's the best comedy I've ever heard in a dungeon.", reya=true},
-            "I will take that.",
-            {text="So about these boots...",reya=true},
-            "Oh, right! Those are my old 'Moth Boots'! If you try to jump while you're in the air already, you'll start flipping. If you run into a wall while flipping, you'll do what I like to call a 'Moth Jump'. Give it a try!",  {text="Thanks! And uh,||| keep working on it!", reya=true},
-        }
-    else
-        scene.skeleton.lines = {text="Not so easy, huh? Ready to listen to my set?#", question=boot_question}
-        return {"Fine! Good luck getting outta here on your own."}
-    end
-end
-
-local function pick_question(response)
-    if response then
-        local helped = false
-        scene.events:add_handle('land_block_trigger_interal', function(data)
-            if not helped then
-                helped = true
-                make_text({"Wait! I've changed my mind!"}, true, scene.skeleton.ent, nil, nil)
-            end
-        end)
-
-        scene.events:add_handle('win', function(data)
-            if not helped then
-                helped = true
-                make_text({"Next time I'm getting crushed upfront..."}, true, scene.skeleton.ent, nil, nil)
-            end
-        end)
-        scene.skeleton.lines = {
-            {text='Would you mind going over how to use this thing one more time?', reya=true},
-            {text="Sure. To use that pick, hit 'k' while you're in the air. Once you hit a wall, you'll climb straight up. You can use the pick to change direction while jumping, too."},
-        }
-        return {
-            {reya=true, text="Sure. I can do that for ya."},
-            {text="Thank you, stranger. To use that pick, hit 'k' or the left face button while you're in the air. Once you hit a wall, you'll climb straight up. You can use the pick to change direction while jumping, too."},
-            {reya=true, text="Cool! Thank you! I've always wanted one of these."}
-        }
-    else
-        scene.skeleton.lines = {
-            {text="Would you mind telling me anyway?", reya=true},
-            {text="Sorry. The deal stands.#", question=pick_question}
-        }
-        return {
-{reya=true, text="Sorry. I'm not really comfortable with that."},
-                {text="That's disappointing."},
-{reya=true, text="Sorry..."}}
-    end
-end
-
-local lines = {
-    [355] = {
-        "Hey! Those are mine!",
-        {text="Oh! I didn't think you were using them.", reya=true},
-        "Well I'm not really using my feet either. You want those?",
-        {text="You think you're pretty funny, huh?", reya=true},
-        {
-            text="Actually, I HAVE been working on a tight five|.|.|. Tell you what, I'll tell you how to use those boots if you listen to my set.#",
-            question=boot_question
-        },
-    },
-    [365] = {
-        "Just when I thought it couldn't get worse,|| my last friend is taken from me.",
-        {text="Who? This switch-pick?", reya=true},
-        "It was a good listener,||| now I have no one to talk to.",
-        {text="You could always chat with that piston in front of you. Doesn't look like it's going anywhere.", reya=true},
-        "I would, but I'm mad at it. There truly is nothing left for me.",
-        {text=".|.|.|", reya=true},
-        {text="I'll tell you what,|| if you promise to drop that huge block on me and end my misery,|| I'll tell you how the pick works.#",
-            question=pick_question
-        }
-    },
-    [356] = {
-        "As Chief Thief of this guild, I welcome you, newcommer.",
-        {reya=true, text="This is a thieve's guild?"},
-        "Well, it was, back when this was a town. Can't really have a guild without marks.",
-        {reya=true, text="Pretty rough town."},
-        {
-            text="It wasn't always this way. Seeing as how you stole that ring right off my corpse, let me tell you my tale so that my memory may live on... should you ever escape.#",
-            question=ring_question
-        },
-    },
-}
-
-function Skeleton:new(o)
-    o = o or {}
-
-    setmetatable(o, self)
-    self.__index = self
-
-    o:init()
-
-    return o
-end
-
-function Skeleton:init()
-    _, self.sprite = self.ent:add_comp(Sprite, {
-        file=files[self.tile].file,
-        w=files[self.tile].w, 
-        h=files[self.tile].h,
-        offset_y=files[self.tile].offset_y,
-        continue=true
-    })
-    self.sprite:animate(idles[self.tile])
-    local s = self.sprite
-    local tile = self.tile
-    self.scene.skeleton = self.ent
-    self.scene.skeleton.lines = lines[self.tile]
-    local old_frame = s.frame
-
-    if self.tile == 356 and SAVE.FLIP then
-        self.scene.skeleton.lines = {
-            {reya=true, text="Hey Ravioli, how do you use this ring again?"},
-            "It's Rivolo.",
-            {reya=true, text="Right! Sorry. How do you use this Rivolo Ring again?"},
-            "...",
-            {reya=true, text="Oh nevermind. I just remembered. If I flip right after doing a moth jump, I can maintain my maximum speed during the entirety of the jump, helping me to cross wider gaps."},
-            "What's a moth jump?",
-            {reya=true, text="Farewell, Ravioli!"},
-        }
-
-        self.talked = false
-        s:animate(post_idles[tile])
-    end
-
-    self.arrow = self.scene:add_entity(Entity:new({
-        x=self.ent.x,
-        y=self.ent.y-16,
-        layer=entity_layers.Foreground,
-        tag='text',
-        scene=self.scene,
-        update=function(self)
-            if s.frame ~= old_frame then
-                local sound = sfx[tile][s.frame]
-                if sound then
-                    play_sound(sound)
-                end
-                old_frame = s.frame
-            end
-            if inside(self, self.scene.player) and not self.done then
-                self[Sprite].frame = 63
-                if not self.scene.paused and pressed('w') then
-                    self.done = true
-                    self[Sprite].frame = -1
-                    runProcess(function()
-                        local save = scene.player[Player].sprite.frame
-                        scene.player[Player].sprite.frame = -1
-                        if not self.talked then
-                            self.talked = true
-                            local up = scene.player.update 
-                            scene.player.update = function()end
-
-                            TEXT_DONE = false
-                            s:animate_wait(steal[tile])
-                            s:animate(post_idles[tile])
-
-                            scene.player.update = up
-                            SAVE[power[tile]] = true
-                        end
-                        scene.player.x = s.ent.x - offset[tile]
-                        scene.player.y = s.ent.y - 5
-
-                        make_text(scene.skeleton.lines, true, s.ent, nil, save)
-                    end)
-                end
-            else
-                self[Sprite].frame = -1
-            end
-        end
-    }):add_comp(Object, {
-        w=16,
-        h=16,
-        pass_through=true
-    }):add_comp(Sprite, {
-        file='res/usable_files/diab_final_tileset.png',
-        w=16,
-        h=16,
-        frame=-1
-    }))
-end
-
-function Skeleton:talk()
-    self.sprite:animate(talking[self.tile])
-end
-
-function Skeleton:listen()
-    -- self.sprite.callback = function()
-        self.sprite:animate(post_idles[self.tile])
-    -- end
-end
-
-function Skeleton:update()
-    if TEXT_DONE then
-        self.arrow.done = false
-        if self.sprite.frames and #self.sprite.frames > 1 then
-            self.sprite.callback = function()
-                self.sprite:animate(post_idles[self.tile])
-            end
-        end
-    end
-end
-
-register_tiles({355, 356, 365}, function(scene, tile, x, y, layer)
-    return scene:add_entity(Entity:new({
-        x=x+files[tile].offset_x,
-        y=y,
-        layer=entity_layers[layer.name],
-    }):add_comp(Skeleton, {tile=tile, scene=scene}))
-end)
+Skeleton = {}
+
+-- OPTIMIZED: Split long dialogue lines to fit in textbox with 18pt font
+-- Max ~100-110 characters per line for readability
+lines = {
+    "Actually, I've been working on a tight five.",
+    "Tell you what, I'll tell you how to use those boots if you listen to my set.",
+    "So fellas, you might be wondering why I'm not wearing any pants.||| Well, lets just say I didn't think it'd be THIS kind of dungeon, if you catch my drift!||| Where my boys at? YOU know what I'm talking about.",
+    "Its not a total bust though, I met a fossil on the way in.||| I said, hey girl, are you feelin okay, cause it looks like jur ass SICK.||| Am I right fellas?",
+    "I'd tell you to tip your waitress, but my drink hasn't gotten here yet,||| and I ordered it when I was alive.",
+    "But seriously, if you see her,||| tell her to change my death in the afternoon to a life in as soon as possible.",
+    "and before you ask me what I died of,",
+    "I bet it was less nasty than what you were BORN of.",
+    "That is to say, your mother!",
+    "Thank you, thank you! That's my time!|| I'll be here...|| well, forever, probably!"
+}
+
+function range(n, shift)
+    shift = shift or 0
+    local r = {}
+    for i=shift,n do
+        table.insert(r, i)
+    end
+    for i=0,shift-1 do
+        table.insert(r, i)
+    end
+    return r
+end
+
+local idles = {
+    [355] = {80},
+    [365] = {60},
+    [356] = {120}
+}
+
+local post_idles = {
+    [365] = {50},
+    [355] = {70},
+    [356] = {110}
+}
+
+local steal = {
+    [365] = range(45),
+    [355] = range(69),
+    [356] = range(103),
+}
+
+local sfx = {
+    [355] = {
+        [22]='DIABEatFloor',
+        [26]='DIABEatFloor',
+        [16]='Reya_Land',
+        [63]='Reya_Land',
+    },
+    [356] = {},
+    [365] = {}
+}
+
+local talking = {
+    [365] = {50, 51, 52},
+    [355] = {70, 71, 72, 73},
+    [356] = {110, 111, 112, 113},
+}
+
+local files = {
+    [365] = {file='res/images/rob_pick_sheet.png', w=64, h=48, offset_y=-16, offset_x=7},
+    [356] = {file='res/images/steal_ring_sheet.png', w=80, h=48, offset_y=-16, offset_x=0},
+    [355] = {file='res/images/boot_skeleton_sheet.png', w=80, h=48, offset_y=-16, offset_x=0},
+}
+
+local offset = {
+    [365] = 3,
+    [355] = 8,
+    [356] = 14,
+}
+
+local power = {
+    [355] = 'MOTH_JUMP',
+    [356] = 'FLIP',
+    [365] = 'PICK',
+}
+
+local function ring_question(response)
+    print(response)
+    if response then
+        scene.story = true
+        scene.skeleton.lines = {
+            {reya=true, text="Hey Ravioli, how do you use this ring again?"},
+            "It's Rivolo.",
+            {reya=true, text="Right! Sorry. How do you use this Rivolo Ring again?"},
+            "...",
+            {reya=true, text="Oh nevermind. I just remembered."},
+            {reya=true, text="If I flip right after doing a moth jump, I can maintain my maximum speed during the jump,"},
+            {reya=true, text="helping me to cross wider gaps."},
+            "What's a moth jump?",
+            {reya=true, text="Farewell, Ravioli!"},
+        }
+
+        return {
+            "Countless years ago, the people of this town lived in luxury.",
+            "There were two bus stops, and garbage was collected every other week",
+            "instead once a year as is customary.",
+            "It was easy pickin' for the likes of us.",
+            "On a particularly hot evening, I found my way into an old wizard's shop.",
+            "Rummaging through all of the curios, one chest stood out to me.",
+            "It had an extremely elaborate lock.",
+            "I knew it would take me until morning to pick it, but I had to have whatever was inside.",
+            "You know how it is.",
+            {reya=true, text="Sure!"},
+            "Well, I cracked the lock just before dawn. Inside was that ring.",
+            "I snatched it up, and made my escape,",
+            "hoping to make it back to the guild before morning.",
+            "But that night... morning never came. No morning like I've seen.",
+            "I was struck with a feeling like the wind twisted out of my body.",
+            "As I fled the shop, I saw crowds of people, all reeling,",
+            "as if recovering from the same feeling I just shook.",
+            "As I ran back here, no one stopped me, no one batted an eye.",
+            "Everyone was looking up, but there was no sun, there were no stars.",
+            "Just a blanket of dim green light.",
+            "I stumbled into an empty guild, and I've been hiding here ever since.",
+            {reya=true, text="So.|.|.| you're NOT the Chief Thief?"},
+            "Well, no, but do you see any OTHER animated skeletons around here?",
+            {reya=true, text="Whatever, bones. How do I use this ring?"},
+            "That ring makes you more aerodynamic.",
+            "While flipping in the air, you won't lose any speed due to drag.",
+            "It really helps cover more distance.",
+            "Also, if you land on a rope while going that fast, it will launch you upward.", 
+            "Try jumping as hard as you can off of that wall, then immediately flipping.",
+            {reya=true, text="Neat, thanks!"},
+            "When you escape from here, make sure everyone in the land knows my story.",
+            "The story of Rivolo Darikin.",
+            {reya=true, text="The Story of Ravioli Dairy-King. Got it."},
+            "No wai-",
+            {reya=true, text="Farewell Ravioli!"},
+        }
+    else
+        scene.skeleton.lines = {
+            {reya=true, text="Will you PLEASE just tell me how this ring works?"},
+            {text="Only if you listen to my tale!#", question=ring_question},
+        }
+        return {
+            {reya=true, text="Sorry, Bones. I'm in a hurry."},
+            "You'll be back!"
+        }
+    end
+end
+
+local function boot_question(response)
+    if response then
+        scene.comedy = true
+        scene.skeleton.lines = {
+            {text="How do you use these again?", reya=true},
+            "If you try to jump while you're in the air already, you'll start flipping.",
+            "If you run into a wall while flipping, you'll do what I like to call a 'Moth Jump'.",
+            "Give it a try!"
+        }
+        return {
+            "Ah great! Thanks a bunch. Okay here it goes...",
+            "AHEM. Hey everybody, tonight we have a very special guest",
+            "who's going to really rattle your cage! Your ribcage, that is!",
+            "Give it up for Clint Ivory!",
+            {text="...", reya=true},
+            "So fellas, you might be wondering why I'm not wearing any pants.",
+            "Well, lets just say when I came here I didn't think it'd be THIS kind of dungeon,",
+            "if you catch my drift!||| Where my boys at? YOU know what I'm talking about.",
+            {text="Hmm...", reya=true},
+            "Its not a total bust though, I met a fossil on the way in.",
+            "I said, hey girl, are you feelin okay, cause it looks like jur ass SICK.|||",
+            {text="(sigh...)", reya=true},
+            "and before you ask me what I died of,",
+            "I bet it was less nasty than what you were BORN of.",
+            "They're shining the light in my eye! That's my time!",
+            "I'll be here...|| well, forever, probably!",
+            "So, what'd you think? Did you get that last one?",
+            "It's a \"your mother is ugly\" joke.",
+            {text="Yeah I caught that.||| Some of that seemed a little.|.|.| dated.|.|.|", reya=true},
+            {text="but I guess it's the best comedy I've ever heard in a dungeon.", reya=true},
+            "I will take that.",
+            {text="So about these boots...",reya=true},
+            "Oh, right! Those are my old 'Moth Boots'!",
+            "If you try to jump while you're in the air already, you'll start flipping.",
+            "If you run into a wall while flipping, you'll do what I like to call a 'Moth Jump'.",
+            "Give it a try!",
+            {text="Thanks! And uh,||| keep working on it!", reya=true},
+        }
+    else
+        scene.skeleton.lines = {text="Not so easy, huh? Ready to listen to my set?#", question=boot_question}
+        return {"Fine! Good luck getting outta here on your own."}
+    end
+end
+
+local function pick_question(response)
+    if response then
+        local helped = false
+        scene.events:add_handle('land_block_trigger_interal', function(data)
+            if not helped then
+                helped = true
+                make_text({"Wait! I've changed my mind!"}, true, scene.skeleton.ent, nil, nil)
+            end
+        end)
+
+        scene.events:add_handle('win', function(data)
+            if not helped then
+                helped = true
+                make_text({"Next time I'm getting crushed upfront..."}, true, scene.skeleton.ent, nil, nil)
+            end
+        end)
+        scene.skeleton.lines = {
+            {text='Would you mind going over how to use this thing one more time?', reya=true},
+            {text="Sure. To use that pick, hit 'k' while you're in the air."},
+            {text="Once you hit a wall, you'll climb straight up."},
+            {text="You can use the pick to change direction while jumping, too."},
+        }
+        return {
+            {reya=true, text="Sure. I can do that for ya."},
+            {text="Thank you, stranger."},
+            {text="To use that pick, hit 'k' or the left face button while you're in the air."},
+            {text="Once you hit a wall, you'll climb straight up."},
+            {text="You can use the pick to change direction while jumping, too."},
+            {reya=true, text="Cool! Thank you! I've always wanted one of these."}
+        }
+    else
+        scene.skeleton.lines = {
+            {text="Would you mind telling me anyway?", reya=true},
+            {text="Sorry. The deal stands.#", question=pick_question}
+        }
+        return {
+            {reya=true, text="Sorry. I'm not really comfortable with that."},
+            {text="That's disappointing."},
+            {reya=true, text="Sorry..."}
+        }
+    end
+end
+
+local lines = {
+    [355] = {
+        "Hey! Those are mine!",
+        {text="Oh! I didn't think you were using them.", reya=true},
+        "Well I'm not really using my feet either. You want those?",
+        {text="You think you're pretty funny, huh?", reya=true},
+        {
+            text="Actually, I HAVE been working on a tight five|.|.|.",
+            question=boot_question
+        },
+        {
+            text="Tell you what, I'll tell you how to use those boots if you listen to my set.#",
+            question=boot_question
+        },
+    },
+    [365] = {
+        "Just when I thought it couldn't get worse,",
+        "my last friend is taken from me.",
+        {text="Who? This switch-pick?", reya=true},
+        "It was a good listener,||| now I have no one to talk to.",
+        {text="You could always chat with that piston in front of you.", reya=true},
+        {text="Doesn't look like it's going anywhere.", reya=true},
+        "I would, but I'm mad at it. There truly is nothing left for me.",
+        {text=".|.|.|", reya=true},
+        {text="I'll tell you what,|| if you promise to drop that huge block on me",
+            question=pick_question
+        },
+        {text="and end my misery,|| I'll tell you how the pick works.#",
+            question=pick_question
+        }
+    },
+    [356] = {
+        "As Chief Thief of this guild, I welcome you, newcommer.",
+        {reya=true, text="This is a thieve's guild?"},
+        "Well, it was, back when this was a town.",
+        "Can't really have a guild without marks.",
+        {reya=true, text="Pretty rough town."},
+        {
+            text="It wasn't always this way.",
+            question=ring_question
+        },
+        {
+            text="Seeing as how you stole that ring right off my corpse,",
+            question=ring_question
+        },
+        {
+            text="let me tell you my tale so that my memory may live on...",
+            question=ring_question
+        },
+        {
+            text="should you ever escape.#",
+            question=ring_question
+        },
+    },
+}
+
+function Skeleton:new(o)
+    o = o or {}
+
+    setmetatable(o, self)
+    self.__index = self
+
+    o:init()
+
+    return o
+end
+
+function Skeleton:init()
+    _, self.sprite = self.ent:add_comp(Sprite, {
+        file=files[self.tile].file,
+        w=files[self.tile].w, 
+        h=files[self.tile].h,
+        offset_y=files[self.tile].offset_y,
+        continue=true
+    })
+    self.sprite:animate(idles[self.tile])
+    local s = self.sprite
+    local tile = self.tile
+    self.scene.skeleton = self.ent
+    self.scene.skeleton.lines = lines[self.tile]
+    local old_frame = s.frame
+
+    if self.tile == 356 and SAVE.FLIP then
+        self.scene.skeleton.lines = {
+            {reya=true, text="Hey Ravioli, how do you use this ring again?"},
+            "It's Rivolo.",
+            {reya=true, text="Right! Sorry. How do you use this Rivolo Ring again?"},
+            "...",
+            {reya=true, text="Oh nevermind. I just remembered."},
+            {reya=true, text="If I flip right after doing a moth jump, I can maintain my maximum speed"},
+            {reya=true, text="during the entirety of the jump, helping me to cross wider gaps."},
+            "What's a moth jump?",
+            {reya=true, text="Farewell, Ravioli!"},
+        }
+
+        self.talked = false
+        s:animate(post_idles[tile])
+    end
+
+    self.arrow = self.scene:add_entity(Entity:new({
+        x=self.ent.x,
+        y=self.ent.y-16,
+        layer=entity_layers.Foreground,
+        tag='text',
+        scene=self.scene,
+        update=function(self)
+            if s.frame ~= old_frame then
+                local sound = sfx[tile][s.frame]
+                if sound then
+                    play_sound(sound)
+                end
+                old_frame = s.frame
+            end
+            if inside(self, self.scene.player) and not self.done then
+                self[Sprite].frame = 63
+                if not self.scene.paused and pressed('w') then
+                    self.done = true
+                    self[Sprite].frame = -1
+                    runProcess(function()
+                        local save = scene.player[Player].sprite.frame
+                        scene.player[Player].sprite.frame = -1
+                        if not self.talked then
+                            self.talked = true
+                            local up = scene.player.update 
+                            scene.player.update = function()end
+
+                            TEXT_DONE = false
+                            s:animate_wait(steal[tile])
+                            s:animate(post_idles[tile])
+
+                            scene.player.update = up
+                            SAVE[power[tile]] = true
+                        end
+                        scene.player.x = s.ent.x - offset[tile]
+                        scene.player.y = s.ent.y - 5
+
+                        make_text(scene.skeleton.lines, true, s.ent, nil, save)
+                    end)
+                end
+            else
+                self[Sprite].frame = -1
+            end
+        end
+    }):add_comp(Object, {
+        w=16,
+        h=16,
+        pass_through=true
+    }):add_comp(Sprite, {
+        file='res/usable_files/diab_final_tileset.png',
+        w=16,
+        h=16,
+        frame=-1
+    }))
+end
+
+function Skeleton:talk()
+    self.sprite:animate(talking[self.tile])
+end
+
+function Skeleton:listen()
+    self.sprite:animate(post_idles[self.tile])
+end
+
+function Skeleton:update()
+    if TEXT_DONE then
+        self.arrow.done = false
+        if self.sprite.frames and #self.sprite.frames > 1 then
+            self.sprite.callback = function()
+                self.sprite:animate(post_idles[self.tile])
+            end
+        end
+    end
+end
+
+register_tiles({355, 356, 365}, function(scene, tile, x, y, layer)
+    return scene:add_entity(Entity:new({
+        x=x+files[tile].offset_x,
+        y=y,
+        layer=entity_layers[layer.name],
+    }):add_comp(Skeleton, {tile=tile, scene=scene}))
+end)
--- original/objects/track.lua
+++ modified/objects/track.lua
@@ -1,41 +1,32 @@
-Track = {}
+local floor = math.floor
 
-local track_tiles = {120, 121, 122, 123, 124, 125, 126, 127, 128, 129, 130}
+_G.Track = {} -- Explicit global declaration
 
+local track_tiles = {
+    [120]=true, [121]=true, [122]=true, [123]=true, [124]=true, 
+    [125]=true, [126]=true, [127]=true, [128]=true, [129]=true, [130]=true
+}
+
 function Track:new(o)
     o = o or {}
-
     setmetatable(o, self)
     self.__index = self
-
     o:init()
-
     return o
 end
 
 function Track:init()
     self.ent.tag = 'track'
+    local x_dir, y_dir, bx_dir, by_dir = 0, 0, 0, 0
+    local t = self.tile
+    if t == 120 or t == 129 then y_dir, by_dir = 1, -1
+    elseif t == 122 then x_dir, bx_dir = -1, 1
+    elseif t == 128 or t == 121 then x_dir, bx_dir = 1, -1 end
 
-    local x_dir = 0
-    local y_dir = 0
-
-    local bx_dir = 0
-    local by_dir = 0
-
-    if self.tile == 422 - 302 or self.tile == 431 - 302 then
-        y_dir = 1
-        by_dir = -1
-    elseif self.tile == 424 - 302 then
-        x_dir = -1
-        bx_dir = 1
-    elseif self.tile == 430 - 302 or self.tile == 423 - 302 then
-        x_dir = 1
-        bx_dir = -1
-    end
-
     local ent = self:next_piece(self.tile_x, self.tile_y, x_dir, y_dir)
     local back_ent = self:next_piece(self.tile_x + bx_dir, self.tile_y + by_dir, bx_dir, by_dir, true)
-    if back_ent then
+    
+    if back_ent and ent then
         ent[TrackPiece].last_piece = back_ent[TrackPiece]
         back_ent[TrackPiece].next_piece = ent[TrackPiece]
     end
@@ -43,61 +34,45 @@
 
 function Track:next_piece(x, y, x_dir, y_dir, backwards)
     local tile = self.scene:get_tile(x, y, self.layer)
-    if not tablefind(track_tiles, tile) then
-        return
-    end
-    if self.scene.loaded_tracks[x..'-'..y] then
-        return self.scene.loaded_tracks[x..'-'..y]
-    end
-    local ent = self.scene:add_entity(Entity:new({x=x * 16 + 16, y=y * 16 + 8,layer=self.ent.layer}):add_comp(TrackPiece, {
-        tile=tile,
-        tile_x=x,
-        tile_y=y,
-    }))
-    self.scene.loaded_tracks[x..'-'..y] = ent
+    if not track_tiles[tile] then return end
+    
+    -- OPTIMIZATION: Use numeric key instead of string concatenation
+    local key = x * 10000 + y
+    if self.scene.loaded_tracks[key] then return self.scene.loaded_tracks[key] end
 
-    local n = nil
+    local ent = self.scene:add_entity(Entity:new({
+        x = x * 16 + 16, y = y * 16 + 8, layer = self.ent.layer
+    }):add_comp(TrackPiece, { tile = tile, tile_x = x, tile_y = y }))
+    self.scene.loaded_tracks[key] = ent
 
-    if tile == 422 - 302 or tile == 431 - 302 or tile == 423 - 302 or tile == 430 - 302 then
-        n = self:next_piece(x + x_dir, y + y_dir, x_dir, y_dir, backwards)
-    elseif tile == 426 - 302 then
-        n = self:next_piece(x + y_dir, y + x_dir, y_dir, x_dir, backwards)
-    elseif tile == 427 - 302 then
-        n = self:next_piece(x - y_dir, y - x_dir, -y_dir, -x_dir, backwards)
-    elseif tile == 424 - 302 then
-        n = self:next_piece(x - y_dir, y - x_dir, -y_dir, -x_dir, backwards)
-    elseif tile == 425 - 302 then
-        n = self:next_piece(x + y_dir, y + x_dir, y_dir, x_dir, backwards)
+    local nx, ny, nxd, nyd = x, y, x_dir, y_dir
+    if tile == 120 or tile == 129 or tile == 121 or tile == 128 then
+        nx, ny = x + x_dir, y + y_dir
+    elseif tile == 124 or tile == 123 then
+        nx, ny, nxd, nyd = x + y_dir, y + x_dir, y_dir, x_dir
+    elseif tile == 125 or tile == 122 then
+        nx, ny, nxd, nyd = x - y_dir, y - x_dir, -y_dir, -x_dir
     end
 
+    local n = self:next_piece(nx, ny, nxd, nyd, backwards)
     if n then
-        if backwards then
-            ent[TrackPiece].last_piece = n[TrackPiece]
-            n[TrackPiece].next_piece = ent[TrackPiece]
-        else
-            ent[TrackPiece].next_piece = n[TrackPiece]
-            n[TrackPiece].last_piece = ent[TrackPiece]
-        end
+        local target = backwards and "last_piece" or "next_piece"
+        local reverse = backwards and "next_piece" or "last_piece"
+        ent[TrackPiece][target] = n[TrackPiece]
+        n[TrackPiece][reverse] = ent[TrackPiece]
     end
-
     return ent
 end
 
-register_tiles({120, 121, 122, 123, 124, 125, 126, 127, 128, 129, 130}, function(scene, tile, x, y, layer)
+-- Explicit array passed to register_tiles, which now builds a flat
+-- tile_to_handler hash in game_scene. No need for table.insert anymore.
+register_tiles({120,121,122,123,124,125,126,127,128,129,130}, function(scene, tile, x, y, layer)
     scene.loaded_tracks = scene.loaded_tracks or {}
-    if scene.loaded_tracks[math.floor(x/16)..'-'..math.floor(y/16)] then
-        return
-    end
-
-    return scene:add_entity(Entity:new({
-        x=x,
-        y=y,
-        layer=entity_layers[layer.name]
-    }):add_comp(Track, {
-        layer=layer.name,
-        tile=tile,
-        tile_x=math.floor(x/16),
-        tile_y=math.floor(y/16),
-        scene=scene
+    local tx, ty = floor(x/16), floor(y/16)
+    -- loaded_tracks uses tile coords; key formula matches next_piece() below
+    local key = tx * 10000 + ty
+    if scene.loaded_tracks[key] then return end
+    return scene:add_entity(Entity:new({x=x, y=y}):add_comp(Track, {
+        layer=layer.name, tile=tile, tile_x=tx, tile_y=ty, scene=scene
     }))
-end)
+end)
\ No newline at end of file
--- original/objects/track_piece.lua
+++ modified/objects/track_piece.lua
@@ -1,129 +1,221 @@
-TrackPiece = {}
-
-function TrackPiece:new(o)
-    o = o or {}
-
-    setmetatable(o, self)
-    self.__index = self
-
-    o:init()
-
-    return o
-end
-
-function TrackPiece:init()
-    _, self.sprite = self.ent:add_comp(Sprite, {
-        file='res/usable_files/diab_final_tileset.png', 
-        w=16, 
-        h=16,
-        frame=self.tile
-    })
-
-    self.ent.tag = 'track_piece'
-end
-
-local function get_all(x, y)
-    local e = scene:lookup('Foreground', x, y)
-    local open = {}
-    local closed = {}
-    local res = {}
-
-    if e and not e.taken then
-        open = {{e, 0, 0}}
-        table.insert(res, {e, 0, 0})
-    end
-
-    while open[1] do
-        local e, off_x, off_y = unpack(table.remove(open))
-        e.taken = true
-
-        for i, v in ipairs({{16, 0}, {-16, 0}, {0, 16}, {0, -16}}) do
-            local n = scene:lookup('Foreground', x + off_x + v[1], y + off_y + v[2])
-            if n and not closed[n] and not n.taken then
-                closed[n] = true
-                table.insert(res, {n, off_x + v[1], off_y + v[2]})
-                table.insert(open, {n, off_x + v[1], off_y + v[2]})
-            end
-        end
-    end
-
-    return res
-end
-
-function TrackPiece:update()
-    if not self.started then
-        self.started = true
-
-        self.moving_ents = get_all(self.ent.x - 8, self.ent.y)
-        if not self.moving_ents[1] then
-            return
-        end
-        local max_left = nil
-        local max_right = nil
-
-        for i, p in ipairs(self.moving_ents) do
-            local ent = p[1]
-            if not ent[Object] then
-                ent:add_comp(Object, {
-                    w=16,
-                    h=16,
-                    solid=true
-                })
-            end
-            if not ent[Object].pass_through or #self.moving_ents == 1 then
-                if max_left then max_left = math.min(max_left, ent.x) else max_left = ent.x end
-                if max_right then max_right = math.max(max_right, ent.x) else max_right = ent.x end
-            end
-            ent[Object].check_list = Object.player_list
-        end
-
-        local columns = (max_right-max_left)/16
-
-        for i, p in ipairs(self.moving_ents) do
-            p[2] = p[2] - columns * 8
-        end
-
-        self.direction = 'next_piece'
-        self.current_piece = self
-
-        -- self.moving_ent.x = self.current_piece.ent.x
-        -- self.moving_ent.y = self.current_piece.ent.y
-
-        local n = self.current_piece[self.direction]
-        if not n then
-            self.direction = (self.direction == 'next_piece' and 'last_piece' or 'next_piece')
-            n = self.current_piece[self.direction]
-        end
-        self.current_piece = n
-    end
-
-    if self.moving_ents[1] then
-        for i, p in ipairs(self.moving_ents) do
-            local ent = p[1]
-            local offx = p[2]
-            local offy = p[3]
-            
-            local target_x = approach(ent.x, self.current_piece.ent.x + offx, 1)
-            local target_y = approach(ent.y, self.current_piece.ent.y + offy, 1)
-
-            ent[Object]:move_x(target_x - ent.x)
-            ent[Object]:move_y(target_y - ent.y)
-        end
-
-        local p = self.moving_ents[1]
-        local ent = p[1]
-        local offx = p[2]
-        local offy = p[3]
-        
-        if ent.x == self.current_piece.ent.x + offx and ent.y == self.current_piece.ent.y + offy then
-            local n = self.current_piece[self.direction]
-            if not n then
-                self.direction = (self.direction == 'next_piece' and 'last_piece' or 'next_piece')
-                n = self.current_piece[self.direction]
-            end
-            self.current_piece = n
-        end
-    end
-end
-
-return TrackPiece
+_G.TrackPiece = {} -- Explicit global declaration for strict.lua
+
+local math_min = math.min
+local math_max = math.max
+local L_Object = Object
+
+-- Direction constants (faster than strings)
+local DIR_NEXT = 1
+local DIR_LAST = 2
+
+function TrackPiece:new(o)
+    o = o or {}
+    setmetatable(o, self)
+    self.__index = self
+    o:init()
+    return o
+end
+
+function TrackPiece:init()
+    _, self.sprite = self.ent:add_comp(Sprite, {
+        file = 'res/usable_files/diab_final_tileset.png',
+        w = 16, h = 16,
+        frame = self.tile
+    })
+    self.ent.tag = 'track_piece'
+
+    -- internal state
+    self.update_counter = 0
+    self.started = false
+end
+
+local function get_all(scene, x, y)
+    local start = scene:lookup('Foreground', x, y)
+    if not start or start.taken then return nil end
+
+    local ents = {}
+    local ox   = {}
+    local oy   = {}
+
+    local i = 1
+    local n = 1
+
+    ents[1] = start
+    ox[1] = 0
+    oy[1] = 0
+    start.taken = true
+
+    while i <= n do
+        local ent = ents[i]
+        local offx = ox[i]
+        local offy = oy[i]
+        i = i + 1
+
+        -- Unrolled directions (faster than table loop)
+        local nx, ny, e
+
+        ny = y + offy  -- set once; shared by the +x and -x checks on this row
+
+        nx = x + offx + 16
+        e = scene:lookup('Foreground', nx, ny)
+        if e and not e.taken then
+            n = n + 1
+            ents[n] = e
+            ox[n] = offx + 16
+            oy[n] = offy
+            e.taken = true
+        end
+
+        nx = x + offx - 16
+        e = scene:lookup('Foreground', nx, ny)  -- BUG FIX: was using stale ny from previous iteration
+        if e and not e.taken then
+            n = n + 1
+            ents[n] = e
+            ox[n] = offx - 16
+            oy[n] = offy
+            e.taken = true
+        end
+
+        nx = x + offx
+        ny = y + offy + 16
+        e = scene:lookup('Foreground', nx, ny)
+        if e and not e.taken then
+            n = n + 1
+            ents[n] = e
+            ox[n] = offx
+            oy[n] = offy + 16
+            e.taken = true
+        end
+
+        ny = y + offy - 16
+        e = scene:lookup('Foreground', nx, ny)
+        if e and not e.taken then
+            n = n + 1
+            ents[n] = e
+            ox[n] = offx
+            oy[n] = offy - 16
+            e.taken = true
+        end
+    end
+
+    return ents, ox, oy
+end
+
+function TrackPiece:update()
+    -- Frame skipping (no modulo)
+    local c = self.update_counter + 1
+    if c < 4 then
+        self.update_counter = c
+        return
+    end
+    self.update_counter = 0
+
+    local ent_ref = self.ent
+
+    if not self.started then
+        if not scene then return end
+
+        local lookup_result = scene:lookup('Foreground', ent_ref.x - 8, ent_ref.y)
+        if not lookup_result then
+            return
+        end
+
+        self.started = true
+
+        local ents, ox, oy = get_all(scene, ent_ref.x - 8, ent_ref.y)
+        if not ents then return end
+
+        self.moving_ents = ents
+        self.moving_ox   = ox
+        self.moving_oy   = oy
+
+        local max_left, max_right
+        local single = (#ents == 1)
+
+        for i = 1, #ents do
+            local ent = ents[i]
+            local obj = ent[L_Object] or ent:add_comp(L_Object, {
+                w = 16, h = 16, solid = true
+            })
+
+            if single or not obj.pass_through then
+                local x = ent.x
+                max_left  = max_left  and math_min(max_left, x) or x
+                max_right = max_right and math_max(max_right, x) or x
+            end
+
+            obj.check_list = L_Object.player_list
+        end
+
+        local columns_offset = ((max_right - max_left) / 16) * 8
+        for i = 1, #ox do
+            ox[i] = ox[i] - columns_offset
+        end
+
+        self.direction = DIR_NEXT
+        self.current_piece = self.next_piece or self.last_piece
+        if not self.current_piece then
+            self.direction = DIR_LAST
+            self.current_piece = self.last_piece
+        end
+
+        return
+    end
+
+    local movers = self.moving_ents
+    if not movers or not movers[1] then return end
+
+    local target = self.current_piece
+    if not target then return end
+    local tp_ent = target.ent
+    local base_x = tp_ent.x
+    local base_y = tp_ent.y
+
+    local ox = self.moving_ox
+    local oy = self.moving_oy
+
+    local reached_all = true
+
+    for i = 1, #movers do
+        local ent = movers[i]
+        local obj = ent[L_Object]
+
+        local tx = base_x + ox[i]
+        local ty = base_y + oy[i]
+
+        local cx = ent.x
+        local cy = ent.y
+
+        local dx = tx - cx
+        if dx ~= 0 then
+            obj:move_x(dx > 0 and 1 or -1)
+            reached_all = false
+        end
+
+        local dy = ty - cy
+        if dy ~= 0 then
+            obj:move_y(dy > 0 and 1 or -1)
+            reached_all = false
+        end
+    end
+
+    if reached_all then
+        local n
+        if self.direction == DIR_NEXT then
+            n = target.next_piece
+            if not n then
+                self.direction = DIR_LAST
+                n = target.last_piece
+            end
+        else
+            n = target.last_piece
+            if not n then
+                self.direction = DIR_NEXT
+                n = target.next_piece
+            end
+        end
+        self.current_piece = n
+    end
+end
+
+return TrackPiece
\ No newline at end of file
--- original/scenes/game_scene.lua
+++ modified/scenes/game_scene.lua
@@ -1,1248 +1,1474 @@
-local tile_handles = {}
-function register_tiles(tiles, fn)
-    table.insert(tile_handles, {tiles, fn})
-end
-
-_ = nil
-
-entity_layers = {
-    Background2 = -5,
-    Background = -3,
-    Right = -1,
-    Left = -1,
-    Collision = 0,
-    player = -2,
-    Foreground = 10,
-    Objects = -1,
-}
-
-local function ends_with(str, ending)
-   return ending == "" or str:sub(-#ending) == ending
-end
-
-function requireDirectory( dir )
-    dir = dir or ""
-    local entities = love.filesystem.getDirectoryItems(dir)
-
-    for k, ents in ipairs(entities) do
-        if ends_with(ents, '.lua') then
-            local trim = string.gsub( ents, ".lua", "")
-            require(dir .. "/" .. trim)
-        end
-    end
-end
-requireDirectory('objects')
-
-GameScene = Scene:new()
-
--- SAVE.LEVEL_NUM = 1
-levels = require ("scenes/level_sections/"..LEVEL_FILE)
-
-
-local greed_up = 0.01
-
-local total_coins = 0
-
--- print(#levels)
--- LEVEL_TOTAL = #levels
-
-function GameScene:init()
-    Scene.init(self)
-end
-
-function titleCase( first, rest )
-   return first:upper()..rest:lower()
-end
-
-SAVE_SCORE = nil
-PERFECT = true
-function GameScene:begin()
-    print('begin?')
-    SAVE_SCORE = SAVE.SCORE
-    print(inspect(SAVE.BEST_TIMES))
-    print(self.back, 'backerino')
-
-    local level_title = self.lt or levels[SAVE.LEVEL_NUM]--{name='test.json', powerups={MOTH_JUMP=true, FLIP=true, PICK=true}}--
-    local level = level_title
-
-    if level_title.name then
-        self.next_song = level_title.song or level_title.next_song
-        self.bottle_exit = level_title.bottle_exit and not (SAVE.GREED > 0)
-        self.stop_song = level_title.stop_song
-        self.fancy_exit = self.bottle_exit or level_title.fancy_exit
-        self.fancy_enter = level_title.fancy_enter
-        self.greed_number = level_title.greed_number
-        self.greed_cost = level_title.greed_cost
-        self.heavy_walls = not level_title.light_walls
-        if level_title.pal then
-            set_pal(tonumber(level_title.pal))
-        end
-
-        print(SAVE.LEVEL_NUM)
-        if SAVE.LEVEL_NUM ~= 2 and level_title.current_song ~= nil then
-            print('song', level_title.song)
-            play_song(level_title.current_song, {
-                loop = true,
-            })
-        end
-
-        if level_title.name == "res/new_levels/the_choice.json" and SAVE.GREED == 0 then
-            play_song(nil, {
-                loop = true,
-            })
-            self.lightning = true
-            runProcess(function()
-                while true do
-                    waitSeconds(math.random() * 6 + 6)
-                    if not self.lightning then
-                        return
-                    end
-                    play_sound('thunderclap'..math.random(1,3))
-                end
-            end)
-        end
-
-        level_title = level_title.name
-    end
-
-    if level_title:sub(1, 1) == 'e' then
-        self:next(true, false)
-        return
-    end
-
-    -- if level_title:sub(1, 1) == 'p' then
-    --     set_pal(tonumber(level_title:sub(2)))
-    --     self:next(false, true)
-    --     return
-    -- end
-
-    if level_title:sub(1, 1) == 's' then
-        play_song(level_title:sub(2), {
-            loop = true,
-        })
-        self:next(false, true)
-        return
-    end
-
-    if level_title:sub(1, 1) == 'b' then
-        local break_number = level_title:sub(6)
-        change_scene(BreakScene:new({break_number=break_number}))
-        SAVE.LEVEL_NUM = SAVE.LEVEL_NUM + 1
-        return
-    end
-
-    SAVE.MOTH_JUMP = level.powerups.MOTH_JUMP
-    SAVE.PICK = level.powerups.PICK
-    SAVE.FLIP = level.powerups.FLIP
-
-    if SAVE.GREED ~= 0 and level.greed then
-        level_title = level.greed[SAVE.GREED]
-    end
-
-    Object._reset()
-
-
-    local level_name = level_title:gsub('.*/', ''):gsub('.json', ''):gsub('_', ' '):gsub("(%a)([%w_']*)", titleCase)
-    
-    self.player = self:add_entity(Entity:new({
-        x=150,
-        y=100,
-        layer=entity_layers.player
-    }):add_comp(Player, {fancy_fall=(level_name=='Break In')}))
-    self.key_count = 0
-    self.wall_key_count = 0
-
-    self.coins = {}
-    for k,v in pairs(not self.lt and SAVE.COINS[''..SAVE.LEVEL_NUM+SAVE.GREED * 0.01] or {}) do
-        self.coins[k] = v
-    end
-
-    self.coin_count = 0
-    self.left_ents = {}
-    self.right_ents = {}
-    self.left_objs = {}
-    self.right_objs = {}
-
-    print(level_title)
-    self:load_level(level_title)
-
-    if self.wall_key_count == 0 then
-        self.events:trigger('wallkey', {})
-    end
-
-    self.left_vel = -0.5
-    self.right_vel = 0.5
-
-    self.left_freeze = 0
-    self.right_freeze = 0
-
-    self.fade = 0
-    self.shake = 0
-    self.shake_left = 0
-    self.shake_right = 0
-    self.sleep = 50
-
-    self.edge = Entity:new({
-        x=-24+16,
-        y=0
-    }):add_comp(Sprite, {
-        file='res/images/frame_tiles.png', 
-        w=48,
-        h=16
-    })
-
-    self.edge = Entity:new({
-        x=-24+16,
-        y=0
-    }):add_comp(Sprite, {
-        file='res/images/frame_tiles.png', 
-        w=48,
-        h=16
-    })
-
-    self.goop = Entity:new({
-        x=-24+16,
-        y=0
-    }):add_comp(Sprite, {
-        file='res/usable_files/soup.png', 
-        w=32,
-        h=64
-    })
-
-    self.chest = Entity:new({
-        x=640-35-16,
-        y=21,
-    }):add_comp(Sprite, {
-        file='res/images/chest_sheet.png',
-        w=48,
-        h=42,
-    })
-
-    self.skull = Entity:new({
-        x=640-35-16,
-        y=71,
-    }):add_comp(Sprite, {
-        file='res/images/aghast_skull_sheet.png',
-        w=32,
-        h=32,
-        flip=true
-    })
-
-    self.ui_ents = {
-        Entity:new({
-            x=105,
-            y=0,
-        }):add_comp(Label, {
-            w=200,
-            text = (SAVE.LEVEL_NUM - 1)..(SAVE.GREED ~= 0 and ('.'..SAVE.GREED) or '')..". "..(level_name),
-            align = "left",
-        }),
-
-        Entity:new({
-            x=640-104-16,
-            y=15,
-        }):add_comp(Label, {
-            w=200,
-            binding = function() return string.format("%02d", SAVE.SCORE) end,
-            align = "right",
-        }),
-
-        Entity:new({
-            x=640-104-16,
-            y=65,
-        }):add_comp(Label, {
-            w=200,
-            binding = function() return string.format("%02d", SAVE.DEATHS) end,
-            align = "right",
-        }),
-
-        self.chest,
-
-        -- Entity:new({
-        --     x=640-104-16,
-        --     y=105,
-        -- }):add_comp(Label, {
-        --     w=200,
-        --     binding = function() return time_string(SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)) end,
-        --     align = "right",
-        -- }),
-
-        self.skull,
-    }
-
-    if SETTINGS.speedrun then
-        for i=0,0 do
-            local ent = Entity:new({
-                x=104,
-                y=10*i+100,
-            }):add_comp(Label, {
-                w=200,
-                binding = function()
-                    local best_time = (SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM] or 0)
-                    local compare_time = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
-
-                    return {
-                        {1,1,1,1},
-                        ' Level\n '..time_string(best_time)..'\n '..time_string(compare_time)..'\n',
-                        (compare_time < best_time) and {0,1,0,1} or ((compare_time == best_time) and {1,1,1,1} or {1,0,0,1}),
-                        (compare_time < best_time and '-' or '+')..time_string(math.abs(best_time-compare_time))
-                    }
-                end,
-                align = "left",
-            })
-            table.insert(self.ui_ents, ent)
-        end
-
-        local ent = Entity:new({
-            x=104,
-            y=200,
-        }):add_comp(Label, {
-            w=200,
-            binding = function()
-                local best_time = SAVE.BEST_TIMES['overall'] or 0
-                local compare_time = SAVE.TICK_COUNT - SAVE.START_TICK
-
-                return {
-                    {1,1,1,1},
-                    ' Overall\n '..time_string(best_time)..'\n '..time_string(compare_time)..'\n',
-                    (compare_time < best_time) and {0,1,0,1} or ((compare_time == best_time) and {1,1,1,1} or {1,0,0,1}),
-                    (compare_time < best_time and '-' or '+')..time_string(math.abs(best_time-compare_time))
-                }            end,
-            align = "left",
-        })
-        table.insert(self.ui_ents, ent)
-    end
-
-    self.pause_ents = Collection:new({
-        start_y = 120,
-        ents={
-            Button:new({
-                w=200,
-                text = 'Resume',
-                action = function()
-                    self.pause_menu = false
-                    self.paused = false
-                end,
-                align = "center",
-            }),
-
-            Button:new({
-                w=200,
-                text = 'Level Select',
-                action = function()
-                    SAVE.SCORE = SAVE_SCORE
-                    change_scene(LevelSelect:new())
-                end,
-                align = "center",
-            }),
-
-            Button:new({
-                w=200,
-                text = 'Main Menu',
-                action = function()
-                    SAVE.SCORE = SAVE_SCORE
-                    change_scene(MainMenu:new())
-                end,
-                align = "center",
-            }),
-
-            -- Button:new({
-            --     w=200,
-            --     text = 'Edit',
-            --     action = function()
-            --         change_scene(self.back)
-            --     end,
-            --     align = "center",
-            -- }),
-
-            Button:new({
-                w=200,
-                text = 'Quit Game',
-                action = function()
-                    love.event.quit()
-                end,
-                align = "center",
-            }),
-        }
-    })
-
-    self.pause_menu = false
-
-    collectgarbage('stop')
-    collectgarbage()
-end
-
-function GameScene:next(fancy_exit, fancy_enter, greed)
-    if self.back then
-        change_scene(self.back)
-        return
-    end
-
-    if greed then
-        SAVE.BEAT[''..SAVE.LEVEL_NUM] = 'greed'
-    elseif SAVE.GREED ~= 0 then
-        SAVE.BEAT[''..(SAVE.LEVEL_NUM+SAVE.GREED*0.01)] = true
-    else
-        SAVE.BEAT[''..SAVE.LEVEL_NUM] = true
-    end
-    SAVE.COINS[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = self.coins
-    SAVE.TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
-    if not SAVE[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] or SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK) < SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] then
-        SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
-    end
-
-    local cur, section = find_section_index(SAVE.LEVEL_NUM, SAVE.GREED)
-    local i = cur
-
-    print(cur, section)
-    while type(sections[section][i]) ~= 'table' and i < #sections[section] do
-        i = i + 1
-    end
-
-    local t = i - 1
-    local count = 0
-    while t > 0 and type(sections[section][t]) == 'number' do
-        if SAVE.BEAT[''..sections[section][t]] then
-            count = count + 1
-        end
-        t = t - 1
-    end
-
-    print(section, i, sections[section][i])
-    if not SAVE.OPENED[''..section..'-'..i] and ((type(sections[section][i]) == 'table' and sections[section][i].beat and sections[section][i].beat <= count) or i == cur + 1) then
-        save_game()
-        change_scene(LevelSelect:new())
-        return
-    end
-
-    if SAVE.GREED ~= 0 then
-        SAVE.GREED = SAVE.GREED + 1
-        print(inspect(levels[SAVE.LEVEL_NUM]))
-        if #levels[SAVE.LEVEL_NUM].greed < SAVE.GREED then
-            SAVE.GREED = 0
-        end
-    end
-    if greed then
-        print('greeding?')
-        SAVE.GREED = 1
-    end
-    if SAVE.GREED == 0 then
-        SAVE.LEVEL_NUM = SAVE.LEVEL_NUM + 1
-    end
-    SAVE.LEVEL_TICK = SAVE.TICK_COUNT
-    SAVE.COINS_MISSED = SAVE.COINS_MISSED + (self.coin_count or 0)
-    SAVE.TOP_LEVEL = math.max(SAVE.LEVEL_NUM, SAVE.TOP_LEVEL)
-    PERFECT = true
-
-    save_game()
-
-    local gs = GameScene:new()
-    gs.fancy_enter = fancy_enter
-    gs.fancy_exit = fancy_exit
-    change_scene(gs)
-end
-
-function save_game()
-    love.filesystem.write('save.json', json.encode(SAVE))
-end
-
-function GameScene:last()
-    SAVE.LEVEL_NUM = SAVE.LEVEL_NUM - 1
-    change_scene(GameScene:new())
-end
-
-function GameScene:restart(x, y, no_sound)
-    if self.killed then
-        return
-    end
-    self.killed = true
-    self.player[Player]:kill(x or 0, y or -1, no_sound)
-    self.skull[Sprite]:animate({1, 2, 3, 4, 0})
-    SAVE.DEATHS = SAVE.DEATHS + 1
-    if math.random() < 0.1 then
-        save_game()
-    end
-    -- change_scene(GameScene:new())
-end
-
-function GameScene:again()
-    SAVE.SCORE = SAVE_SCORE
-    PERFECT = false
-    change_scene(GameScene:new({back=self.back, lt=self.lt}))
-end
-
-function GameScene:push(is_left, amount)
-    if is_left then
-        self.left_vel = amount
-    else
-        self.right_vel = amount
-    end
-end
-
-function GameScene:move_wall(is_left, amount)
-    if is_left then
-        for i, ent in ipairs(self.left_ents) do
-            ent[Object]:move_x(amount, false)
-            ent[Object].x_remainder = 0
-        end
-        self.left_freeze = 8
-    else
-        for i, ent in ipairs(self.right_ents) do
-            ent[Object]:move_x(amount, false)
-            ent[Object].x_remainder = 0
-        end
-        self.right_freeze = 8
-    end
-end
-
-GameScene.game_update = profile("game_update", function(self)
-    local stop = true
-    self.sleep = self.sleep - 1
-
-    if self.left_ent and self.wall_key_count <= 0 then
-        self.left_vel = math.min(self.left_vel + 0.01, self.heavy_walls and 0.25 or (0.4))
-        self.right_vel = math.max(self.right_vel - 0.01, self.heavy_walls and -0.25 or (-0.4))
-
-        if self.sleep < 0 then
-            if self.left_freeze <= 0 then
-                local move = math.min(self.left_ent and self.right_ent.x - self.left_ent.x - 16 or 1, self.left_vel)
-                move = math.max(move, move)
-                if move == 0 then
-                    self.left_vel = math.max(-1, self.left_vel)
-                end
-                if move < 0 and self.player[Player].sm.state == 'push' then
-                    if not self.sound then
-                        self.sound = play_sound('push_wall', nil, 0.5, true)
-                    end
-                    if SAVE.TICK_COUNT % 4 == 0 then
-                        self.shake = 0.4
-                    end
-                    stop = false
-                end
-                for i, ent in ipairs(self.left_ents) do
-                    ent[Object]:move_x(move)
-                    break
-                end
-            else
-                self.left_freeze = self.left_freeze - 1
-            end
-
-            if self.right_freeze <= 0 then
-                local move = math.max(self.left_ent and self.left_ent.x - self.right_ent.x + 16 or -1, self.right_vel)
-                move = math.min(move, move)
-                if move == 0 then
-                    self.right_vel = math.min(1, self.right_vel)
-                end
-                if move > 0 and self.player[Player].sm.state == 'push' then
-                    if not self.sound then
-                        self.sound = play_sound('push_wall', nil, 0.5, true)
-                    end
-                    if SAVE.TICK_COUNT % 4 == 0 then
-                        self.shake = 0.4
-                    end
-                    stop = false
-                end
-                for i, ent in ipairs(self.right_ents) do
-                    ent[Object]:move_x(move)
-                    break
-                end
-            else
-                self.right_freeze = self.right_freeze - 1
-            end
-        end
-    end
-
-    if stop then
-        if self.sound then
-            self.sound:stop()
-            self.sound = nil
-        end
-    end
-
-    if self.has_goop then
-        self.kill_height_offset = self.kill_height_offset - 0.5
-    end
-end)
-
-FADE_RATE = 1/30
-GameScene.update = profile("GameScene.update", function(self)
-    
-    if pressed('pause') then
-        self.pause_menu = not self.pause_menu
-        self.paused = self.pause_menu
-    end
-
-    -- if pressed('q') then
-    --     love.graphics.captureScreenshot('diab' .. os.time() .. '.png')
-    -- end
-
-    self.fade = math.min(self.fade + FADE_RATE, 1)
-    FADE = self.fade
-
-    if not NO_SKIP then
-        if pressed('p') then
-            self:next()
-        elseif pressed('o') then
-            self:last()
-        end
-    end
-
-    if not self.paused then
-        self:game_update()
-    end
-
-    Scene.update(self)
-
-    for i, ent in ipairs(self.ui_ents) do
-        ent:update()
-    end
-    local i = 1
-    while i <= #self.ui_ents do
-        if self.ui_ents[i].dead then
-            _ = self.ui_ents[i].clean_up and self.ui_ents[i]:clean_up()
-            self.events:remove(self.ui_ents[i])
-            table.remove(self.ui_ents, i)
-        else
-            i = i + 1
-        end
-    end
-
-    if self.pause_menu then
-        self.pause_ents:update()
-    end
-end)
-
-function GameScene:calc_width()
-    return (self.max_right - self.max_left)
-end
-
-function GameScene:calc_height()
-    return (self.max_bot - self.max_top)
-end
-
-function GameScene:render(scale, translate_x, translate_y, sc)
-    scale = scale or 1
-    translate_x = translate_x or 0
-    translate_y = translate_y or 0
-    love.graphics.push()
-
-    if self:calc_height() > 30 * 16 then
-        if self.player.y < self.scroll_height - TOP_WINDOW_SCROLL then
-            self.scroll_height = self.player.y + TOP_WINDOW_SCROLL
-        elseif self.player.y > self.scroll_height - BOTTOM_WINDOW_SCROLL then
-            self.scroll_height = self.player.y + BOTTOM_WINDOW_SCROLL
-        end
-
-        if not self.player[StateManager]:attr('air') then
-            self.scroll_to = self.player.y
-        end
-
-        if self.scroll_to then
-            if self.player.y > self.scroll_to then
-                self.scroll_to = nil
-            else
-                self.scroll_height = math.min(math.max(self.scroll_height - 1, self.scroll_to + BOTTOM_WINDOW_SCROLL), self.scroll_height)
-            end
-        end
-
-        self.kill_height = math.min(self.max_top + self:calc_height() + self.kill_height_offset, self.kill_height)
-        self.scroll_height = math.min(self.scroll_height, self.max_bot + 30)
-        self.scroll_height = math.max(self.scroll_height, self.max_top + 360 + 40)
-    end
-
-    local xs = 0
-    local ys = 0
-
-    if self.shake > 0 or self.shake_left > 0 or self.shake_right > 0 then
-        self.shake = math.max(self.shake - 0.1, 0)
-        self.shake_left = math.max(self.shake_left - 0.1, 0)
-        self.shake_right = math.max(self.shake_right - 0.1, 0)
-
-        local angle = math.pi * math.random() * 2
-
-        xs = math.cos(angle) * (self.shake + self.shake_left - self.shake_right) + (self.shake_left - self.shake_right)
-        ys = math.sin(angle) * (self.shake + self.shake_left - self.shake_right)
-    end
-
-    love.graphics.translate(
-        xs + ((320 - self:calc_width()/2 - self.max_left)), 
-        ys -(self.scroll_height - 30 - math.min(23, self:calc_height()) * 16)
-    )
-
-    if not sc then
-        love.graphics.setScissor((-self:calc_width()/2 + 320 + translate_x) * scale, translate_y * 0.5, self:calc_width() * scale, self:calc_height() * scale)
-    else
-        love.graphics.setScissor(math.max(374, (-self:calc_width()/2 + 320 + translate_x) * scale), math.max(20, translate_y * 0.5), math.min(250, self:calc_width() * scale), math.min(200, self:calc_height() * scale))
-    end
-
-    Scene.render(self)
-
-    if self:calc_height() > 30 * 16 and scale == 1 then
-            love.graphics.setColor(161/255, 161/255, 161/255)
-            love.graphics.rectangle('fill', self.max_left, self.kill_height, self:calc_width(), 400)
-            love.graphics.setColor(1, 1, 1)
-
-        for i=1,self:calc_width()/32 do
-            self.goop.x = i * 32 + self.max_left
-            self.goop.y = self.kill_height
-            self.goop[Sprite].frame = math.floor(SAVE.TICK_COUNT / 6) % 18
-            self.goop:render()
-        end
-    end
-
-    self.edge[Sprite].flip = false
-    for i=1,self.level_data.height do
-        self.edge.x = self.max_left + math.floor(math.sin(SAVE.TICK_COUNT/CYCLE_LENGTH) * CYCLE_SIZE) - CYCLE_OFFSET
-        self.edge.y = i * 16 - 8
-        self.edge:render()
-    end
-
-    self.edge[Sprite].flip = true
-    for i=1,self.level_data.height do
-        self.edge.x = self.max_right - (math.floor(math.sin(SAVE.TICK_COUNT/CYCLE_LENGTH) * CYCLE_SIZE) - CYCLE_OFFSET)
-        self.edge.y = i * 16 - 8
-        self.edge:render()
-    end
-    -- Object.draw()
-
-    love.graphics.setScissor( )
-    love.graphics.pop()
-
-    if scale ~= 1 then
-        return
-    end
-
-    love.graphics.push()
-
-    for i, ent in ipairs(self.ui_ents) do
-        ent:render()
-    end
-
-    if self.pause_menu then
-        love.graphics.setShader()
-        current_shader = nil
-
-        love.graphics.setColor(0, 0, 0, 0.7)
-        love.graphics.rectangle('fill', 0, 0, 640, 360)
-        love.graphics.setColor(1, 1, 1, 1)
-
-        self.pause_ents:render()
-    end
-
-    love.graphics.pop()
-end
-
-function GameScene:exit()
-    self.lightning = false
-    local i = 1
-    while i <= #self.entities do
-        if true then
-            _ = self.entities[i].clean_up and self.entities[i]:clean_up()
-            self.events:remove(self.entities[i])
-            table.remove(self.entities, i)
-        else
-            i = i + 1
-        end
-    end
-
-    if self.sound then
-        self.sound:stop()
-        self.sound = nil
-    end
-end
-
-function GameScene:find_closest_ents()
-    self.left_ent = self.left_ents[1]
-    self.left_ent_start = self.left_ent and self.left_ent.x or -100000
-    self.right_ent = self.right_ents[1]
-    self.right_ent_start = self.right_ent and self.right_ent.x or 100000
-    local dis = 100000000
-    for i,le in ipairs(self.left_ents) do
-        for i,re in ipairs(self.right_ents) do
-            if re.y == le.y and re.x - le.x < dis and not le[Object].pass_through and not re[Object].pass_through then
-                self.left_ent = le
-                self.left_ent_start = le.x
-                self.right_ent = re
-                self.right_ent_start = re.x
-                dis = re.x - le.x
-            end
-        end
-    end
-
-    if not self.left_ent  then
-        self.left_ent = self:add_entity(Entity:new({x=-100000, y=0}))
-        self.left_ent_start = -100000
-    end
-
-    if not self.right_ent then
-        self.right_ent = self:add_entity(Entity:new({x=100000, y=0}))
-        self.right_ent_start = 100000
-    end
-end
-
-function GameScene:lookup(layer, x, y)
-    return self.lookup_table[layer .. '-' .. tostring(x) .. '-' .. tostring(y)]
-end
-
-function GameScene:get_tile(x, y, layer_name)
-    local layer = self.layers[layer_name]
-    return layer.data[x + y * layer.width + 1] - 1
-end
-
-function GameScene:set_tile(x, y, layer_name, t)
-    local layer = self.layers[layer_name]
-    layer.data[x + y * layer.width + 1] = t + 1
-end
-
-function GameScene:load_level(name)
-    self.lookup_table = {}
-
-    local content = love.filesystem.read(name)
-    local level_data = json.decode(content)
-    self.level_data = level_data
-    self.layers = {}
-
-    for i, layer in ipairs(level_data.layers) do
-        if layer.name == "Text" then
-            self:load_text_layer(layer)
-        elseif layer.data then
-            self:load_tile_layer(layer)
-        else
-            for i, obj in ipairs(layer.objects) do
-                local ent = nil
-                local done = false
-                local tile = obj.gid - 1
-
-                local x = obj.x + 8
-                local y = obj.y - 8
-
-                for i,pair in ipairs(tile_handles) do
-                    if tablefind(pair[1], tile) then
-                        done = true
-                        ent = pair[2](self, tile, obj.x + 8, obj.y - 8, layer)
-                        break
-                    end
-                end
-
-                if not done then
-                    ent = self:add_entity(Entity:new({
-                        x=x,
-                        y=y,
-                        layer=entity_layers[layer.name],
-                        tag='solid'
-                    }):add_comp(Sprite, {
-                        file='res/images/tiles_vault.png',
-                        w=16,
-                        h=16,
-                        frame=tile
-                    }))
-
-                    ent:add_comp(Object, {
-                        w=16,
-                        h=16,
-                        solid=true
-                    })
-                end
-            end
-        end
-    end
-
-    self:find_closest_ents()
-
-    if not self.max_left then
-        self.max_left = 0
-    end
-
-    if not self.max_right then
-        self.max_right = self.level_data.width * 16
-    end
-
-    if not self.max_top then
-        self.max_top = 0
-    end
-
-    if not self.max_bot then
-        self.max_bot = self.level_data.height * 16
-    end
-
-    self.kill_height = self.max_bot + 30
-    self.scroll_height = math.min(self.kill_height, self:calc_height() > 28*16 and (self.player.y + BOTTOM_WINDOW_SCROLL) or self.kill_height)
-    if self.kill_height_offset then
-        self.kill_height_offset = self.kill_height_offset - self:calc_height()
-    else
-        self.kill_height_offset = 90
-    end
-end
-
-function GameScene:load_text_layer(layer)
-    for i, o in ipairs(layer.objects) do
-        self:add_entity(Entity:new({
-            x=o.x+8,
-            y=o.y-8,
-            layer=entity_layers.Foreground,
-            tag='text',
-            update=function(self)
-                if inside(self, scene.player) then
-                    self[Sprite].frame = 63
-                    if pressed('w') then
-                        make_text(o.properties[1].value, true)
-                    end
-                else
-                    self[Sprite].frame = -1
-                end
-            end
-        }):add_comp(Object, {
-            w=16,
-            h=16,
-            pass_through=true
-        }):add_comp(Sprite, {
-            file='res/usable_files/diab_final_tileset.png',
-            w=16,
-            h=16,
-            frame=-1
-        }))
-    end
-end
-
-function GameScene:load_tile_layer(layer)
-    if layer.name == 'RIght' then
-        layer.name = 'Right'
-    end
-    if layer.name == 'Background 2' then
-        layer.name = 'Background2'
-    end
-    self.layers[layer.name] = layer
-
-    local sprite_batch = Sprite:new({
-        file='res/usable_files/diab_final_tileset.png',
-        w=16,
-        h=16,
-    })
-
-    self.main_batch = self:add_entity(Entity:new({
-        x=0,
-        y=0,
-        layer=entity_layers[layer.name]
-    }):add_comp(SpriteBatch, {
-        image=sprite_batch.image,
-        shader=change_shader
-    }))
-    local batch = self.main_batch[SpriteBatch]
-
-    for t, tile in ipairs(layer.data) do
-        tile = tile - 1
-        if tile >= 0 then
-            local tile_x = ((t-1)%layer.width)
-            local tile_y = math.floor((t-1)/layer.width)
-            local x = ((t-1)%layer.width)*16 + 8
-            local y = math.floor((t-1)/layer.width)*16 + 8
-
-            local ent = nil
-            local done = false
-            for i,pair in ipairs(tile_handles) do
-                if tablefind(pair[1], tile) then
-                    done = true
-                    local skip = false
-                    ent, skip = pair[2](self, tile, x, y, layer)
-                    if skip then
-                        done = false
-                    end
-                    break
-                end
-            end
-
-            if not done then
-                ent = self:add_entity(Entity:new({
-                    x=x,
-                    y=y,
-                    layer=entity_layers[layer.name],
-                    tag='solid'
-                }))
-
-                if layer.name == 'Left' or layer.name == 'Right' then
-                    ent:add_comp(Sprite, {
-                        file='res/usable_files/diab_final_tileset.png',
-                        w=16,
-                        h=16,
-                        frame=tile
-                    })
-                else
-                    batch.batch:add(sprite_batch.quads[tile + 1], x, y, 0, 1, 1, 8, 8)
-                end
-
-                if layer.name == 'Collision' then
-                    -- if (tile_x == 0 or self:get_tile(tile_x-1, tile_y, layer.name) == tile) and
-                    --    (tile_y == 0 or self:get_tile(tile_x, tile_y - 1, layer.name) == tile) and
-                    --    (tile_x == layer.width - 1 or self:get_tile(tile_x + 1, tile_y, layer.name) == tile) and
-                    --    (tile_y == layer.height - 1 or self:get_tile(tile_x, tile_y + 1, layer.name) == tile) then
-                    -- else
-
-                    local done = false
-                    if tile_x > 0 and tile_x < layer.width - 1 and
-                       tile_y > 0 and tile_y < layer.height - 1 then
-
-                        local vals = {}
-
-                        table.insert(vals, self:get_tile(tile_x, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x - 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x + 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y - 1, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y + 1, layer.name))
-
-                        local t_val = nil
-                        done = false
-                        for i, t in ipairs(vals) do
-                            if i == 1 then
-                                t_val = t
-                            elseif t ~= t_val then
-                                done = false
-                                break
-                            end
-                        end
-                    end
-
-                    if not done then
-                        ent:add_comp(Object, {
-                            w=16,
-                            h=16,
-                            solid=true
-                        })
-                    end
-                    -- end
-                elseif layer.name == 'Right' then
-                    local done = false
-                    if tile_x > 0 and tile_x < layer.width - 1 and
-                       tile_y > 0 and tile_y < layer.height - 1 then
-
-                        local vals = {}
-
-                        table.insert(vals, self:get_tile(tile_x, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x - 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x + 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y - 1, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y + 1, layer.name))
-
-                        local t_val = nil
-                        done = false
-                        for i, t in ipairs(vals) do
-                            if i == 1 then
-                                t_val = t
-                            elseif t ~= t_val then
-                                done = false
-                                break
-                            end
-                        end
-                    end
-
-                    if not done then
-                        ent:add_comp(Object, {
-                            w=16,
-                            h=16,
-                            solid=true
-                        })
-                    else
-                        ent:add_comp(Object, {
-                            w=16,
-                            h=16,
-                            solid=true,
-                            no_check=true,
-                        })
-                    end
-                elseif layer.name == 'Left' then
-                    local done = false
-                    if tile_x > 0 and tile_x < layer.width - 1 and
-                       tile_y > 0 and tile_y < layer.height - 1 then
-
-                        local vals = {}
-
-                        table.insert(vals, self:get_tile(tile_x, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x - 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x + 1, tile_y, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y - 1, layer.name))
-                        table.insert(vals, self:get_tile(tile_x, tile_y + 1, layer.name))
-
-                        local t_val = nil
-                        done = true
-                        for i, t in ipairs(vals) do
-                            if i == 1 then
-                                t_val = t
-                            elseif t ~= t_val then
-                                done = false
-                                break
-                            end
-                        end
-                    end
-
-                    if not done then
-                        ent:add_comp(Object, {
-                            w=16,
-                            h=16,
-                            solid=true
-                        })
-                    else
-                        ent:add_comp(Object, {
-                            w=16,
-                            h=16,
-                            solid=true,
-                            no_check=true,
-                        })
-                    end
-                end
-            end
-
-            if ent then
-                self.lookup_table[layer.name .. '-' .. tostring(x) .. '-' .. tostring(y)] = ent
-                if layer.name == 'Right' then
-                    if ent[Object] then
-                        ent[Object]:set_position(ent.x, ent.y)
-                        ent.right = true
-                        ent[Object].check_list = Object.object_and_player_list
-                        ent[Object].joint = self.right_objs
-                        table.insert(self.right_ents, ent)
-                        table.insert(self.right_objs, ent[Object])
-                    end
-                elseif layer.name == 'Left' then
-                    if ent[Object] then
-                        ent[Object]:set_position(ent.x, ent.y)
-                        ent.left = true
-                        ent[Object].check_list = Object.object_and_player_list
-                        ent[Object].joint = self.left_objs
-                        table.insert(self.left_ents, ent)
-                        table.insert(self.left_objs, ent[Object])
-                    end
-                end
-            end
-        end
-    end
-end
-
-register_tiles({90}, function(scene, tile, x, y, layer)
-    scene.has_goop = true
-    scene.kill_height_offset = y
-end)
-
-register_tiles({77, 78, 76, 79}, function(scene, tile, x, y, layer)
-    if tile == 76 then
-        scene.max_bot = y + 8
-    elseif tile == 79 then
-        scene.max_top = y - 8
-    elseif tile == 77 then
-        scene.max_left = x - 8
-    else
-        scene.max_right = x + 8
-    end
-end)
-
-
-sections = {[1]={}}
-section_map = {}
-
-function find_section_index(level, greed)
-    greed = greed or 0
-    local section = section_map[level+greed*0.01]
-    print(level, greed, section)
-    print(level, greed, section)
-    for i=1,#sections[section] do
-        if sections[section][i] == level+greed*0.01 then
-            return i, section
-        end
-    end
-end
-
-local c_pal = 0
-local c_song = 'LockedUp.wav'
-local powerups = {}
-local c_section_name = ''
-
-function generate_lvl_info()
-    local section = 1
-    local lvl_count = 0
-    local setup_level_picker = nil
-    setup_level_picker = function(level, i)
-        if type(level) == 'string' and level:sub(1, 1) == 'b' then
-            return level
-        end
-       
-        if level.pal and level.pal ~= c_pal then
-            table.insert(sections[section], {'lock', beat=level.beat})
-            print(section, i)
-        end
-
-        if level.pal and math.floor(level.pal) ~= c_pal then
-            table.insert(sections[section], 'end')
-            section = section + 1
-            sections[section] = {}
-            table.insert(sections[section], 'start')
-        end
-        table.insert(sections[section], i)
-        section_map[i] = section
-        
-        if not level.name then
-            level = {name=level, pal=c_pal, current_song=c_song, powerups=powerups, section_name=c_section_name}
-        else
-            level.current_song = c_song
-
-            c_section_name = level.section_name or c_section_name
-            level.section_name = c_section_name
-            c_pal = level.pal or c_pal
-            c_pal = math.floor(c_pal)
-            level.pal = c_pal
-            c_song = level.next_song or level.song or c_song
-            if level.powerups then
-                levels[i-1].bag_level = true
-                powerups = {}
-                for k, v in pairs(level.powerups) do
-                    powerups[k] = v
-                end
-            else
-                level.powerups = powerups
-            end
-
-            if level.greed then
-                level.greed_up = greed_up
-                section = section + greed_up
-                sections[section] = {}
-                table.insert(sections[section], 'start')
-                for t,l in ipairs(level.greed) do
-                    levels[i+t*0.01] = setup_level_picker(l, i+t*0.01)
-                end
-                table.insert(sections[section], #sections[section], {'lock', beat=#sections[section] - 2})
-                print(sections[section][#sections[section]])
-                table.insert(sections[section], 'end')
-                greed_up = greed_up + 0.01
-                section = math.floor(section)
-            end
-        end
-
-        -- local gs = GameScene:new({lt=level})
-        -- gs:begin()
-        -- print(inspect(gs))
-        -- print(gs.coin_count)
-        -- total_coins = total_coins + gs.coin_count
-        -- lvl_count = lvl_count + 1
-
-        return level
-    end
-
-    local LEVEL_TOTAL = #levels
-    for i=1,LEVEL_TOTAL do
-        levels[i] = setup_level_picker(levels[i], i)
-    end
-
-    print(levels[2])
-    print('TOTAL ', total_coins)
-    print(lvl_count)
-end
-generate_lvl_info()
-
-function coins_gotten()
-    local gotten_coins = 0
-    for key, coins in pairs(SAVE.COINS) do
-        gotten_coins = gotten_coins + tablelength(coins)
-    end
-    print('GOTTEN COINS', gotten_coins)
-
-    return gotten_coins
-end
-
-
+local table_insert = table.insert
+local table_remove = table.remove
+local string_gsub = string.gsub
+local string_format = string.format
+local math_random = math.random
+local math_floor = math.floor
+local math_min = math.min
+local math_max = math.max
+local math_abs = math.abs
+local math_sin = math.sin
+local math_cos = math.cos
+local pairs = pairs
+local ipairs = ipairs
+local type = type
+
+-- OPTIMIZATION: Flat hash map from tile id -> handler function.
+-- Replaces the old tile_handles array which required a linear tablefind()
+-- scan across ALL registered tiles for EVERY tile in the level on load.
+-- register_tiles() is called at file-load time so this is built once at startup.
+local tile_to_handler = {}
+
+function register_tiles(tiles, fn)
+    for i = 1, #tiles do
+        tile_to_handler[tiles[i]] = fn
+    end
+end
+
+-- OPTIMIZATION: Numeric IDs for each lookup-able layer name.
+-- Used by GameScene:lookup() and load_tile_layer() to form a pure-integer key,
+-- avoiding 3 string concatenations + a heap allocation on every lookup call.
+-- Only layers that are actually written into lookup_table need an entry here.
+local layer_ids = {
+    Foreground  = 1,
+    Collision   = 2,
+    Background  = 3,
+    Background2 = 4,
+    Right       = 5,
+    Left        = 6,
+    Objects     = 7,
+}
+
+_ = nil
+
+entity_layers = {
+    Background2 = -5,
+    Background = -3,
+    Right = -1,
+    Left = -1,
+    Collision = 0,
+    player = -2,
+    Foreground = 10,
+    Objects = -1,
+}
+
+local function ends_with(str, ending)
+   return ending == "" or str:sub(-#ending) == ending
+end
+
+function requireDirectory( dir )
+    dir = dir or ""
+    local entities = love.filesystem.getDirectoryItems(dir)
+
+    for k, ents in ipairs(entities) do
+        if ends_with(ents, '.lua') then
+            local trim = string_gsub( ents, ".lua", "")
+            require(dir .. "/" .. trim)
+        end
+    end
+end
+requireDirectory('objects')
+
+GameScene = Scene:new()
+
+-- SAVE.LEVEL_NUM = 1
+levels = require ("scenes/level_sections/"..LEVEL_FILE)
+
+
+local greed_up = 0.01
+
+local total_coins = 0
+
+-- print(#levels)
+-- LEVEL_TOTAL = #levels
+
+function GameScene:init()
+    Scene.init(self)
+end
+
+function titleCase( first, rest )
+   return first:upper()..rest:lower()
+end
+
+SAVE_SCORE = nil
+PERFECT = true
+function GameScene:begin()
+    print('begin?')
+    SAVE_SCORE = SAVE.SCORE
+    print(inspect(SAVE.BEST_TIMES))
+    print(self.back, 'backerino')
+
+    local level_title = self.lt or levels[SAVE.LEVEL_NUM]--{name='test.json', powerups={MOTH_JUMP=true, FLIP=true, PICK=true}}--
+    local level = level_title
+
+    if level_title.name then
+        self.next_song = level_title.song or level_title.next_song
+        self.bottle_exit = level_title.bottle_exit and not (SAVE.GREED > 0)
+        self.stop_song = level_title.stop_song
+        self.fancy_exit = self.bottle_exit or level_title.fancy_exit
+        self.fancy_enter = level_title.fancy_enter
+        self.greed_number = level_title.greed_number
+        self.greed_cost = level_title.greed_cost
+        self.heavy_walls = not level_title.light_walls
+        if level_title.pal then
+            set_pal(tonumber(level_title.pal))
+        end
+
+        print(SAVE.LEVEL_NUM)
+        if SAVE.LEVEL_NUM ~= 2 and level_title.current_song ~= nil then
+            print('song', level_title.song)
+            play_song(level_title.current_song, {
+                loop = true,
+            })
+        end
+
+        if level_title.name == "res/new_levels/the_choice.json" and SAVE.GREED == 0 then
+            play_song(nil, {
+                loop = true,
+            })
+            self.lightning = true
+            runProcess(function()
+                while true do
+                    waitSeconds(math_random() * 6 + 6)
+                    if not self.lightning then
+                        return
+                    end
+                    play_sound('thunderclap'..math_random(1,3))
+                end
+            end)
+        end
+
+        level_title = level_title.name
+    end
+
+    if level_title:sub(1, 1) == 'e' then
+        self:next(true, false)
+        return
+    end
+
+    -- if level_title:sub(1, 1) == 'p' then
+    --     set_pal(tonumber(level_title:sub(2)))
+    --     self:next(false, true)
+    --     return
+    -- end
+
+    if level_title:sub(1, 1) == 's' then
+        play_song(level_title:sub(2), {
+            loop = true,
+        })
+        self:next(false, true)
+        return
+    end
+
+    if level_title:sub(1, 1) == 'b' then
+        local break_number = level_title:sub(6)
+        change_scene(BreakScene:new({break_number=break_number}))
+        SAVE.LEVEL_NUM = SAVE.LEVEL_NUM + 1
+        return
+    end
+
+    SAVE.MOTH_JUMP = level.powerups.MOTH_JUMP
+    SAVE.PICK = level.powerups.PICK
+    SAVE.FLIP = level.powerups.FLIP
+
+    if SAVE.GREED ~= 0 and level.greed then
+        level_title = level.greed[SAVE.GREED]
+    end
+
+    Object._reset()
+
+
+    local level_name = level_title:gsub('.*/', ''):gsub('.json', ''):gsub('_', ' '):gsub("(%a)([%w_']*)", titleCase)
+    
+    self.player = self:add_entity(Entity:new({
+        x=150,
+        y=100,
+        layer=entity_layers.player
+    }):add_comp(Player, {fancy_fall=(level_name=='Break In')}))
+    self.key_count = 0
+    self.wall_key_count = 0
+
+    self.coins = {}
+    for k,v in pairs(not self.lt and SAVE.COINS[''..SAVE.LEVEL_NUM+SAVE.GREED * 0.01] or {}) do
+        self.coins[k] = v
+    end
+
+    self.coin_count = 0
+    self.left_ents = {}
+    self.right_ents = {}
+    self.left_objs = {}
+    self.right_objs = {}
+
+    print(level_title)
+    self:load_level(level_title)
+
+    if self.wall_key_count == 0 then
+        self.events:trigger('wallkey', {})
+    end
+
+    self.left_vel = -0.5
+    self.right_vel = 0.5
+
+    self.left_freeze = 0
+    self.right_freeze = 0
+
+    self.fade = 0
+    self.shake = 0
+    self.shake_left = 0
+    self.shake_right = 0
+    self.sleep = 50
+
+    self.edge = Entity:new({
+        x=-24+16,
+        y=0
+    }):add_comp(Sprite, {
+        file='res/images/frame_tiles.png', 
+        w=48,
+        h=16
+    })
+
+    self.edge = Entity:new({
+        x=-24+16,
+        y=0
+    }):add_comp(Sprite, {
+        file='res/images/frame_tiles.png', 
+        w=48,
+        h=16
+    })
+
+    self.goop = Entity:new({
+        x=-24+16,
+        y=0
+    }):add_comp(Sprite, {
+        file='res/usable_files/soup.png', 
+        w=32,
+        h=64
+    })
+
+    self.chest = Entity:new({
+        x=640-35-16,
+        y=21,
+    }):add_comp(Sprite, {
+        file='res/images/chest_sheet.png',
+        w=48,
+        h=42,
+    })
+
+    self.skull = Entity:new({
+        x=640-35-16,
+        y=71,
+    }):add_comp(Sprite, {
+        file='res/images/aghast_skull_sheet.png',
+        w=32,
+        h=32,
+        flip=true
+    })
+
+    self.ui_ents = {
+        Entity:new({
+            x=105,
+            y=0,
+        }):add_comp(Label, {
+            w=200,
+            text = (SAVE.LEVEL_NUM - 1)..(SAVE.GREED ~= 0 and ('.'..SAVE.GREED) or '')..". "..(level_name),
+            align = "left",
+        }),
+
+        Entity:new({
+            x=640-104-16,
+            y=15,
+        }):add_comp(Label, {
+            w=200,
+            binding = function() return string_format("%02d", SAVE.SCORE) end,
+            align = "right",
+        }),
+
+        Entity:new({
+            x=640-104-16,
+            y=65,
+        }):add_comp(Label, {
+            w=200,
+            binding = function() return string_format("%02d", SAVE.DEATHS) end,
+            align = "right",
+        }),
+
+        self.chest,
+
+        -- Entity:new({
+        --     x=640-104-16,
+        --     y=105,
+        -- }):add_comp(Label, {
+        --     w=200,
+        --     binding = function() return time_string(SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)) end,
+        --     align = "right",
+        -- }),
+
+        self.skull,
+    }
+
+    if SETTINGS.speedrun then
+        for i=0,0 do
+            local ent = Entity:new({
+                x=104,
+                y=10*i+100,
+            }):add_comp(Label, {
+                w=200,
+                binding = function()
+                    local best_time = (SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM] or 0)
+                    local compare_time = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
+
+                    return {
+                        {1,1,1,1},
+                        ' Level\n '..time_string(best_time)..'\n '..time_string(compare_time)..'\n',
+                        (compare_time < best_time) and {0,1,0,1} or ((compare_time == best_time) and {1,1,1,1} or {1,0,0,1}),
+                        (compare_time < best_time and '-' or '+')..time_string(math_abs(best_time-compare_time))
+                    }
+                end,
+                align = "left",
+            })
+            table_insert(self.ui_ents, ent)
+        end
+
+        local ent = Entity:new({
+            x=104,
+            y=200,
+        }):add_comp(Label, {
+            w=200,
+            binding = function()
+                local best_time = SAVE.BEST_TIMES['overall'] or 0
+                local compare_time = SAVE.TICK_COUNT - SAVE.START_TICK
+
+                return {
+                    {1,1,1,1},
+                    ' Overall\n '..time_string(best_time)..'\n '..time_string(compare_time)..'\n',
+                    (compare_time < best_time) and {0,1,0,1} or ((compare_time == best_time) and {1,1,1,1} or {1,0,0,1}),
+                    (compare_time < best_time and '-' or '+')..time_string(math_abs(best_time-compare_time))
+                }            end,
+            align = "left",
+        })
+        table_insert(self.ui_ents, ent)
+    end
+
+    self.pause_ents = Collection:new({
+        start_y = 120,
+        ents={
+            Button:new({
+                w=200,
+                text = 'Resume',
+                action = function()
+                    self.pause_menu = false
+                    self.paused = false
+                end,
+                align = "center",
+            }),
+
+            Button:new({
+                w=200,
+                text = 'Level Select',
+                action = function()
+                    SAVE.SCORE = SAVE_SCORE
+                    change_scene(LevelSelect:new())
+                end,
+                align = "center",
+            }),
+
+            Button:new({
+                w=200,
+                text = 'Main Menu',
+                action = function()
+                    SAVE.SCORE = SAVE_SCORE
+                    change_scene(MainMenu:new())
+                end,
+                align = "center",
+            }),
+
+            -- Button:new({
+            --     w=200,
+            --     text = 'Edit',
+            --     action = function()
+            --         change_scene(self.back)
+            --     end,
+            --     align = "center",
+            -- }),
+
+            Button:new({
+                w=200,
+                text = 'Quit Game',
+                action = function()
+                    love.event.quit()
+                end,
+                align = "center",
+            }),
+        }
+    })
+
+    self.pause_menu = false
+
+    -- Build SpriteBatches for edges and goop now that both the level bounds
+    -- (from load_level) and the edge/goop entities (created above) exist.
+    self:build_edge_batches()
+    self:build_goop_batch()
+    -- Build wall batches after find_closest_ents() has populated left_ents/right_ents
+    self:build_wall_batches()
+    -- Remove static collision entities from scene.entities — their Objects remain
+    -- in the spatial hash for collision detection but the entities have no render
+    -- or update work to do, so iterating them every frame is pure overhead.
+    self:purge_static_collision_entities()
+
+    collectgarbage('stop')
+    collectgarbage()
+end
+
+function GameScene:next(fancy_exit, fancy_enter, greed)
+    if self.back then
+        change_scene(self.back)
+        return
+    end
+
+    if greed then
+        SAVE.BEAT[''..SAVE.LEVEL_NUM] = 'greed'
+    elseif SAVE.GREED ~= 0 then
+        SAVE.BEAT[''..(SAVE.LEVEL_NUM+SAVE.GREED*0.01)] = true
+    else
+        SAVE.BEAT[''..SAVE.LEVEL_NUM] = true
+    end
+    SAVE.COINS[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = self.coins
+    SAVE.TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
+    if not SAVE[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] or SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK) < SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] then
+        SAVE.BEST_TIMES[''..SAVE.LEVEL_NUM+SAVE.GREED*0.01] = SAVE.TICK_COUNT - (SAVE.LEVEL_TICK or SAVE.START_TICK)
+    end
+
+    local cur, section = find_section_index(SAVE.LEVEL_NUM, SAVE.GREED)
+    local i = cur
+
+    print(cur, section)
+    while type(sections[section][i]) ~= 'table' and i < #sections[section] do
+        i = i + 1
+    end
+
+    local t = i - 1
+    local count = 0
+    while t > 0 and type(sections[section][t]) == 'number' do
+        if SAVE.BEAT[''..sections[section][t]] then
+            count = count + 1
+        end
+        t = t - 1
+    end
+
+    print(section, i, sections[section][i])
+    if not SAVE.OPENED[''..section..'-'..i] and ((type(sections[section][i]) == 'table' and sections[section][i].beat and sections[section][i].beat <= count) or i == cur + 1) then
+        save_game()
+        change_scene(LevelSelect:new())
+        return
+    end
+
+    if SAVE.GREED ~= 0 then
+        SAVE.GREED = SAVE.GREED + 1
+        print(inspect(levels[SAVE.LEVEL_NUM]))
+        if #levels[SAVE.LEVEL_NUM].greed < SAVE.GREED then
+            SAVE.GREED = 0
+        end
+    end
+    if greed then
+        print('greeding?')
+        SAVE.GREED = 1
+    end
+    if SAVE.GREED == 0 then
+        SAVE.LEVEL_NUM = SAVE.LEVEL_NUM + 1
+    end
+    SAVE.LEVEL_TICK = SAVE.TICK_COUNT
+    SAVE.COINS_MISSED = SAVE.COINS_MISSED + (self.coin_count or 0)
+    SAVE.TOP_LEVEL = math_max(SAVE.LEVEL_NUM, SAVE.TOP_LEVEL)
+    PERFECT = true
+
+    save_game()
+
+    local gs = GameScene:new()
+    gs.fancy_enter = fancy_enter
+    gs.fancy_exit = fancy_exit
+    change_scene(gs)
+end
+
+function save_game()
+    love.filesystem.write('save.json', json.encode(SAVE))
+end
+
+function GameScene:last()
+    SAVE.LEVEL_NUM = SAVE.LEVEL_NUM - 1
+    change_scene(GameScene:new())
+end
+
+function GameScene:restart(x, y, no_sound)
+    if self.killed then
+        return
+    end
+    self.killed = true
+    self.player[Player]:kill(x or 0, y or -1, no_sound)
+    self.skull[Sprite]:animate({1, 2, 3, 4, 0})
+    SAVE.DEATHS = SAVE.DEATHS + 1
+    if math_random() < 0.1 then
+        save_game()
+    end
+    -- change_scene(GameScene:new())
+end
+
+function GameScene:again()
+    SAVE.SCORE = SAVE_SCORE
+    PERFECT = false
+    change_scene(GameScene:new({back=self.back, lt=self.lt}))
+end
+
+function GameScene:push(is_left, amount)
+    if is_left then
+        self.left_vel = amount
+    else
+        self.right_vel = amount
+    end
+end
+
+function GameScene:move_wall(is_left, amount)
+    if is_left then
+        for i, ent in ipairs(self.left_ents) do
+            ent[Object]:move_x(amount, false)
+            ent[Object].x_remainder = 0
+        end
+        self.left_freeze = 8
+    else
+        for i, ent in ipairs(self.right_ents) do
+            ent[Object]:move_x(amount, false)
+            ent[Object].x_remainder = 0
+        end
+        self.right_freeze = 8
+    end
+end
+
+GameScene.game_update = profile("game_update", function(self)
+    local stop = true
+    self.sleep = self.sleep - 1
+
+    if self.left_ent and self.wall_key_count <= 0 then
+        self.left_vel = math_min(self.left_vel + 0.01, self.heavy_walls and 0.25 or (0.4))
+        self.right_vel = math_max(self.right_vel - 0.01, self.heavy_walls and -0.25 or (-0.4))
+
+        if self.sleep < 0 then
+            if self.left_freeze <= 0 then
+                local move = math_min(self.left_ent and self.right_ent.x - self.left_ent.x - 16 or 1, self.left_vel)
+                move = math_max(move, move)
+                if move == 0 then
+                    self.left_vel = math_max(-1, self.left_vel)
+                end
+                if move < 0 and self.player[Player].sm.state == 'push' then
+                    if not self.sound then
+                        self.sound = play_sound('push_wall', nil, 0.5, true)
+                    end
+                    if SAVE.TICK_COUNT % 4 == 0 then
+                        self.shake = 0.4
+                    end
+                    stop = false
+                end
+                for i, ent in ipairs(self.left_ents) do
+                    ent[Object]:move_x(move)
+                    break
+                end
+            else
+                self.left_freeze = self.left_freeze - 1
+            end
+
+            if self.right_freeze <= 0 then
+                local move = math_max(self.left_ent and self.left_ent.x - self.right_ent.x + 16 or -1, self.right_vel)
+                move = math_min(move, move)
+                if move == 0 then
+                    self.right_vel = math_min(1, self.right_vel)
+                end
+                if move > 0 and self.player[Player].sm.state == 'push' then
+                    if not self.sound then
+                        self.sound = play_sound('push_wall', nil, 0.5, true)
+                    end
+                    if SAVE.TICK_COUNT % 4 == 0 then
+                        self.shake = 0.4
+                    end
+                    stop = false
+                end
+                for i, ent in ipairs(self.right_ents) do
+                    ent[Object]:move_x(move)
+                    break
+                end
+            else
+                self.right_freeze = self.right_freeze - 1
+            end
+        end
+    end
+
+    if stop then
+        if self.sound then
+            self.sound:stop()
+            self.sound = nil
+        end
+    end
+
+    if self.has_goop then
+        self.kill_height_offset = self.kill_height_offset - 0.5
+    end
+end)
+
+
+
+FADE_RATE = 1/30
+GameScene.update = profile("GameScene.update", function(self)
+    
+    if pressed('pause') then
+        self.pause_menu = not self.pause_menu
+        self.paused = self.pause_menu
+    end
+
+    -- if pressed('q') then
+    --     love.graphics.captureScreenshot('diab' .. os.time() .. '.png')
+    -- end
+
+    self.fade = math_min(self.fade + FADE_RATE, 1)
+    FADE = self.fade
+
+    if not NO_SKIP then
+        if pressed('p') then
+            self:next()
+        elseif pressed('o') then
+            self:last()
+        end
+    end
+
+    if not self.paused then
+        self:game_update()
+    end
+
+    Scene.update(self)
+
+    for i, ent in ipairs(self.ui_ents) do
+        ent:update()
+    end
+    local i = 1
+    while i <= #self.ui_ents do
+        if self.ui_ents[i].dead then
+            _ = self.ui_ents[i].clean_up and self.ui_ents[i]:clean_up()
+            self.events:remove(self.ui_ents[i])
+            table.remove(self.ui_ents, i)
+        else
+            i = i + 1
+        end
+    end
+
+    if self.pause_menu then
+        self.pause_ents:update()
+    end
+end)
+
+function GameScene:calc_width()
+    if not self.max_right or not self.max_left then
+        return 0
+    end
+    return (self.max_right - self.max_left)
+end
+
+function GameScene:calc_height()
+    if not self.max_bot or not self.max_top then
+        return 0
+    end
+    return (self.max_bot - self.max_top)
+end
+
+function GameScene:render(scale, translate_x, translate_y, sc)
+    scale = scale or 1
+    translate_x = translate_x or 0
+    translate_y = translate_y or 0
+    love.graphics.push()
+
+    -- Cache once — these never change mid-frame and were previously called 7x
+    local cw = self:calc_width()
+    local ch = self:calc_height()
+
+    if ch > 30 * 16 then
+        if self.player.y < self.scroll_height - TOP_WINDOW_SCROLL then
+            self.scroll_height = self.player.y + TOP_WINDOW_SCROLL
+        elseif self.player.y > self.scroll_height - BOTTOM_WINDOW_SCROLL then
+            self.scroll_height = self.player.y + BOTTOM_WINDOW_SCROLL
+        end
+
+        if not self.player[StateManager]:attr('air') then
+            self.scroll_to = self.player.y
+        end
+
+        if self.scroll_to then
+            if self.player.y > self.scroll_to then
+                self.scroll_to = nil
+            else
+                self.scroll_height = math_min(math_max(self.scroll_height - 1, self.scroll_to + BOTTOM_WINDOW_SCROLL), self.scroll_height)
+            end
+        end
+
+        self.kill_height = math_min(self.max_top + ch + self.kill_height_offset, self.kill_height)
+        self.scroll_height = math_min(self.scroll_height, self.max_bot + 30)
+        self.scroll_height = math_max(self.scroll_height, self.max_top + 360 + 40)
+    end
+
+    local xs = 0
+    local ys = 0
+
+    if self.shake > 0 or self.shake_left > 0 or self.shake_right > 0 then
+        self.shake = math_max(self.shake - 0.1, 0)
+        self.shake_left = math_max(self.shake_left - 0.1, 0)
+        self.shake_right = math_max(self.shake_right - 0.1, 0)
+
+        local angle = math.pi * math_random() * 2
+
+        xs = math_cos(angle) * (self.shake + self.shake_left - self.shake_right) + (self.shake_left - self.shake_right)
+        ys = math_sin(angle) * (self.shake + self.shake_left - self.shake_right)
+    end
+
+    love.graphics.translate(
+        xs + ((320 - cw/2 - self.max_left)),
+        ys -(self.scroll_height - 30 - math_min(23, ch) * 16)
+    )
+
+    if not sc then
+        love.graphics.setScissor((-cw/2 + 320 + translate_x) * scale, translate_y * 0.5, cw * scale, ch * scale)
+    else
+        love.graphics.setScissor(math_max(374, (-cw/2 + 320 + translate_x) * scale), math_max(20, translate_y * 0.5), math_min(250, cw * scale), math_min(200, ch * scale))
+    end
+
+    Scene.render(self)
+
+    if ch > 30 * 16 and scale == 1 then
+        love.graphics.setColor(161/255, 161/255, 161/255)
+        love.graphics.rectangle('fill', self.max_left, self.kill_height, cw, 400)
+        love.graphics.setColor(1, 1, 1)
+
+        if self.goop_batches then
+            local goop_frame = math_floor(SAVE.TICK_COUNT / 6) % 18
+            if current_shader ~= self.goop[Sprite].shader then
+                love.graphics.setShader(self.goop[Sprite].shader)
+                current_shader = self.goop[Sprite].shader
+            end
+            love.graphics.draw(self.goop_batches[goop_frame], 0, self.kill_height)
+        end
+    end
+
+    -- OPTIMIZATION: Edges drawn as two SpriteBatch calls instead of
+    -- edge_height*2 individual Entity:render() calls per frame.
+    if self.max_right and self.level_data and self.edge_batch_left then
+        local edge_offset = math_floor(math_sin(SAVE.TICK_COUNT/CYCLE_LENGTH) * CYCLE_SIZE) - CYCLE_OFFSET
+
+        -- Rebuild batches only when offset changes (once per tick at most)
+        if edge_offset ~= self.edge_last_offset then
+            self:rebuild_edge_batches(edge_offset)
+        end
+
+        if current_shader ~= self.edge[Sprite].shader then
+            love.graphics.setShader(self.edge[Sprite].shader)
+            current_shader = self.edge[Sprite].shader
+        end
+        love.graphics.setColor(1, 1, 1, 1)
+        love.graphics.draw(self.edge_batch_left,  0, 0)
+        love.graphics.draw(self.edge_batch_right, 0, 0)
+    end
+
+    love.graphics.setScissor()
+    love.graphics.pop()
+
+    if scale ~= 1 then
+        return
+    end
+
+    love.graphics.push()
+
+    local ui_count = #self.ui_ents
+    for i=1, ui_count do
+        self.ui_ents[i]:render()
+    end
+
+    if self.pause_menu then
+        love.graphics.setShader()
+        current_shader = nil
+
+        love.graphics.setColor(0, 0, 0, 0.7)
+        love.graphics.rectangle('fill', 0, 0, 640, 360)
+        love.graphics.setColor(1, 1, 1, 1)
+
+        self.pause_ents:render()
+    end
+
+    love.graphics.pop()
+end
+
+function GameScene:exit()
+    self.lightning = false
+    local i = 1
+    while i <= #self.entities do
+        if true then
+            _ = self.entities[i].clean_up and self.entities[i]:clean_up()
+            self.events:remove(self.entities[i])
+            table.remove(self.entities, i)
+        else
+            i = i + 1
+        end
+    end
+
+    if self.sound then
+        self.sound:stop()
+        self.sound = nil
+    end
+end
+
+function GameScene:find_closest_ents()
+    self.left_ent = self.left_ents[1]
+    self.left_ent_start = self.left_ent and self.left_ent.x or -100000
+    self.right_ent = self.right_ents[1]
+    self.right_ent_start = self.right_ent and self.right_ent.x or 100000
+    local dis = 100000000
+    for i,le in ipairs(self.left_ents) do
+        for i,re in ipairs(self.right_ents) do
+            if re.y == le.y and re.x - le.x < dis and not le[Object].pass_through and not re[Object].pass_through then
+                self.left_ent = le
+                self.left_ent_start = le.x
+                self.right_ent = re
+                self.right_ent_start = re.x
+                dis = re.x - le.x
+            end
+        end
+    end
+
+    if not self.left_ent  then
+        self.left_ent = self:add_entity(Entity:new({x=-100000, y=0}))
+        self.left_ent_start = -100000
+    end
+
+    if not self.right_ent then
+        self.right_ent = self:add_entity(Entity:new({x=100000, y=0}))
+        self.right_ent_start = 100000
+    end
+end
+
+function GameScene:lookup(layer, x, y)
+    -- OPTIMIZATION: Pure-integer key replaces 3 string concatenations per call.
+    -- Formula: (x * 10000 + y) * 100 + layer_id
+    -- Assumes x,y are tile pixel coords (multiples of 16, up to ~10000px levels)
+    -- and layer_id fits in [1..99]. Safe for maps up to 625 tiles wide/tall.
+    return self.lookup_table[(x * 10000 + y) * 100 + layer_ids[layer]]
+end
+
+function GameScene:get_tile(x, y, layer_name)
+    local layer = self.layers[layer_name]
+    return layer.data[x + y * layer.width + 1] - 1
+end
+
+function GameScene:set_tile(x, y, layer_name, t)
+    local layer = self.layers[layer_name]
+    layer.data[x + y * layer.width + 1] = t + 1
+end
+
+function GameScene:load_level(name)
+    self.lookup_table = {}
+
+    local content = love.filesystem.read(name)
+    local level_data = json.decode(content)
+    self.level_data = level_data
+    self.layers = {}
+
+    for i, layer in ipairs(level_data.layers) do
+        if layer.name == "Text" then
+            self:load_text_layer(layer)
+        elseif layer.data then
+            self:load_tile_layer(layer)
+        else
+            for i, obj in ipairs(layer.objects) do
+                local ent = nil
+                local done = false
+                local tile = obj.gid - 1
+
+                local x = obj.x + 8
+                local y = obj.y - 8
+
+                local handler = tile_to_handler[tile]
+                if handler then
+                    done = true
+                    ent = handler(self, tile, obj.x + 8, obj.y - 8, layer)
+                end
+
+                if not done then
+                    ent = self:add_entity(Entity:new({
+                        x=x,
+                        y=y,
+                        layer=entity_layers[layer.name],
+                        tag='solid'
+                    }):add_comp(Sprite, {
+                        file='res/images/tiles_vault.png',
+                        w=16,
+                        h=16,
+                        frame=tile
+                    }))
+
+                    ent:add_comp(Object, {
+                        w=16,
+                        h=16,
+                        solid=true
+                    })
+                end
+
+                -- Write object-layer entities into lookup_table so that
+                -- scene:lookup() can find them (e.g. track_piece finding carts,
+                -- Saw:init() finding adjacent saws). Uses same key formula as
+                -- load_tile_layer. Only writes for known layer names.
+                if ent and layer_ids[layer.name] then
+                    self.lookup_table[(x * 10000 + y) * 100 + layer_ids[layer.name]] = ent
+                end
+            end
+        end
+    end
+
+    self:find_closest_ents()
+
+    if not self.max_left then
+        self.max_left = 0
+    end
+
+    if not self.max_right then
+        self.max_right = self.level_data.width * 16
+    end
+
+    if not self.max_top then
+        self.max_top = 0
+    end
+
+    if not self.max_bot then
+        self.max_bot = self.level_data.height * 16
+    end
+
+    self.kill_height = self.max_bot + 30
+    self.scroll_height = math_min(self.kill_height, self:calc_height() > 28*16 and (self.player.y + BOTTOM_WINDOW_SCROLL) or self.kill_height)
+    if self.kill_height_offset then
+        self.kill_height_offset = self.kill_height_offset - self:calc_height()
+    else
+        self.kill_height_offset = 90
+    end
+
+end
+
+-- Remove static collision entities from self.post (and self.entities if already
+-- moved there). Their Objects remain registered in the spatial hash and continue
+-- to handle solid collision detection — only the scene loop iteration is eliminated.
+--
+-- Safe because:
+--   - Collision tiles never move   -> no hash:update needed
+--   - No Sprite component          -> no render cost anyway
+--   - update already nooped        -> no update cost anyway
+--   - Found via lookup_table       -> direct entity ref, not via scene.entities
+--   - Object._reset() at begin()   -> clears shash on level reload, no leak
+--
+-- Result for go_up:  1908 entities removed from loop -> ~56 remain
+-- Result for cliffside: 585 entities removed from loop -> ~52 remain
+function GameScene:purge_static_collision_entities()
+    local i = #self.post
+    while i >= 1 do
+        if self.post[i]._static_collision then
+            table.remove(self.post, i)
+        end
+        i = i - 1
+    end
+    -- Also sweep self.entities in case render has already moved some across
+    i = #self.entities
+    while i >= 1 do
+        if self.entities[i]._static_collision then
+            table.remove(self.entities, i)
+        end
+        i = i - 1
+    end
+end
+
+-- Build SpriteBatches for the left and right walls.
+-- Also performs three rounds of cleanup that eliminate the remaining per-frame
+-- costs of having 1560 individual wall entities:
+--
+--   A) no_check Objects (interior tiles that can never touch a moving entity)
+--      are removed from the joint and the spatial hash entirely.
+--      Per-pixel joint cost: 780 hash:update calls -> 209.
+--
+--   B) All wall entities except ents[1] are spliced out of scene.entities.
+--      Eliminates 1558 noop update() + 1558 noop render() calls per frame.
+--      ents[1] stays so its Sprite.render (overridden below) fires at the
+--      correct z-order position within Scene.render().
+--
+--   C) ents[1] Object is kept in the joint so ent[1].x tracks movement,
+--      which is the offset used when drawing the batch.
+function GameScene:build_wall_batches()
+    local noop = function() end
+
+    local function build_for(ents, scene_entities)
+        if #ents == 0 then return end
+
+        local first_spr = ents[1][Sprite]
+        if not first_spr then return end
+
+        local image     = first_spr.image
+        local batch     = love.graphics.newSpriteBatch(image, #ents)
+        local initial_x = ents[1].x
+
+        -- Mark every wall entity for removal from scene.entities,
+        -- then unmark ents[1] which must stay for z-ordered batch draw.
+        for _, ent in ipairs(ents) do
+            ent._wall_remove = true
+        end
+        ents[1]._wall_remove = nil
+
+        for i, ent in ipairs(ents) do
+            local s   = ent[Sprite]
+            local obj = ent[Object]
+            if s then
+                local quad = s.quads[(s.frame or 0) + 1]
+                batch:add(quad, ent.x - initial_x, ent.y, 0, 1, 1, s.w/2, s.h/2)
+                s.render = noop
+                if i > 1 then ent.render = noop end
+            end
+            ent.update = noop
+
+            -- A) Strip no_check (interior) Objects from joint and spatial hash.
+            --    They serve no collision purpose so maintaining them is pure waste.
+            --    Keep ents[1] Object in joint so ent.x tracks wall movement.
+            if obj and obj.no_check and i > 1 then
+                obj:clean_up()
+            end
+        end
+
+        -- B) Splice surplus wall entities out of scene.entities.
+        --    Iterate backwards to avoid index shifting.
+        if scene_entities then
+            local i = #scene_entities
+            while i >= 1 do
+                if scene_entities[i]._wall_remove then
+                    table.remove(scene_entities, i)
+                end
+                i = i - 1
+            end
+        end
+
+        -- Override ents[1] Sprite.render to draw entire batch at current x.
+        first_spr.render = function()
+            if current_shader ~= change_shader then
+                love.graphics.setShader(change_shader)
+                current_shader = change_shader
+            end
+            love.graphics.setColor(1, 1, 1, 1)
+            love.graphics.draw(batch, ents[1].x, 0)
+        end
+    end
+
+    -- scene.entities is populated by Scene.render() from scene.post on first render.
+    -- build_wall_batches() is called from begin() before the first render, so
+    -- wall entities are still in self.post. We splice from self.post here;
+    -- ents[1] per wall will be moved to entities naturally on first render.
+    local pool = (#self.entities > 0) and self.entities or self.post
+    build_for(self.left_ents,  pool)
+    build_for(self.right_ents, pool)
+end
+
+-- Build (or rebuild) the left and right edge SpriteBatches.
+-- Called once at load, then each render frame only when edge_offset changes.
+function GameScene:build_edge_batches()
+    if not self.level_data then return end
+
+    local edge_sprite = self.edge[Sprite]
+    local image   = edge_sprite.image
+    local height  = self.level_data.height
+
+    self.edge_batch_left  = love.graphics.newSpriteBatch(image, height)
+    self.edge_batch_right = love.graphics.newSpriteBatch(image, height)
+    self.edge_last_offset = nil  -- force rebuild on first render
+end
+
+-- Rebuild edge batch contents for a given x offset.
+-- Only called when edge_offset changes (once per tick at most).
+function GameScene:rebuild_edge_batches(edge_offset)
+    local edge_sprite = self.edge[Sprite]
+    local quads   = edge_sprite.quads
+    local frame   = edge_sprite.frame or 0
+    local quad    = quads[frame + 1]
+    local height  = self.level_data.height
+    local w       = edge_sprite.w
+    local h       = edge_sprite.h
+
+    local bl = self.edge_batch_left
+    local br = self.edge_batch_right
+    bl:clear()
+    br:clear()
+
+    local lx = self.max_left  + edge_offset
+    local rx = self.max_right - edge_offset
+
+    for i = 1, height do
+        local y = i * 16 - 8
+        -- Left edge: drawn flipped=false  (scaleX=1)
+        bl:add(quad, lx, y, 0, 1, 1, w/2, h/2)
+        -- Right edge: drawn flipped=true  (scaleX=-1, origin at w/2)
+        br:add(quad, rx, y, 0, -1, 1, w/2, h/2)
+    end
+
+    self.edge_last_offset = edge_offset
+end
+
+-- Build one SpriteBatch per animation frame at load time.
+-- Each batch is a complete static row of goop sprites for that frame.
+-- No per-frame CPU work — render just picks the right batch by index.
+function GameScene:build_goop_batch()
+    if not self.has_goop then return end
+
+    local goop_sprite = self.goop[Sprite]
+    local image  = goop_sprite.image
+    local quads  = goop_sprite.quads
+    local w      = goop_sprite.w
+    local h      = goop_sprite.h
+    local cw     = self:calc_width()
+    local count  = math_floor(cw / 32) + 1
+    local frames = 18
+
+    self.goop_batches = {}
+    for frame = 0, frames - 1 do
+        local batch = love.graphics.newSpriteBatch(image, count)
+        local quad  = quads[frame + 1]
+        for i = 1, count do
+            batch:add(quad, i * 32 + self.max_left, 0, 0, 1, 1, w/2, h/2)
+        end
+        self.goop_batches[frame] = batch
+    end
+end
+
+function GameScene:load_text_layer(layer)
+    for i, o in ipairs(layer.objects) do
+        self:add_entity(Entity:new({
+            x=o.x+8,
+            y=o.y-8,
+            layer=entity_layers.Foreground,
+            tag='text',
+            update=function(self)
+                if inside(self, scene.player) then
+                    self[Sprite].frame = 63
+                    if pressed('w') then
+                        make_text(o.properties[1].value, true)
+                    end
+                else
+                    self[Sprite].frame = -1
+                end
+            end
+        }):add_comp(Object, {
+            w=16,
+            h=16,
+            pass_through=true
+        }):add_comp(Sprite, {
+            file='res/usable_files/diab_final_tileset.png',
+            w=16,
+            h=16,
+            frame=-1
+        }))
+    end
+end
+
+local tile_check_vals = {}
+
+function GameScene:check_adjacent_tiles(tile_x, tile_y, layer_name, layer_width, layer_height)
+    -- Reuse the same table instead of creating new ones
+    tile_check_vals[1] = self:get_tile(tile_x, tile_y, layer_name)
+    tile_check_vals[2] = self:get_tile(tile_x - 1, tile_y, layer_name)
+    tile_check_vals[3] = self:get_tile(tile_x + 1, tile_y, layer_name)
+    tile_check_vals[4] = self:get_tile(tile_x, tile_y - 1, layer_name)
+    tile_check_vals[5] = self:get_tile(tile_x, tile_y + 1, layer_name)
+
+    local t_val = tile_check_vals[1]
+    for i = 2, 5 do
+        if tile_check_vals[i] ~= t_val then
+            return false
+        end
+    end
+    return true
+end
+
+function GameScene:load_tile_layer(layer)
+    if layer.name == 'RIght' then
+        layer.name = 'Right'
+    end
+    if layer.name == 'Background 2' then
+        layer.name = 'Background2'
+    end
+    self.layers[layer.name] = layer
+
+    local sprite_batch = Sprite:new({
+        file='res/usable_files/diab_final_tileset.png',
+        w=16,
+        h=16,
+    })
+
+    self.main_batch = self:add_entity(Entity:new({
+        x=0,
+        y=0,
+        layer=entity_layers[layer.name]
+    }):add_comp(SpriteBatch, {
+        image=sprite_batch.image,
+        shader=change_shader
+    }))
+    local batch = self.main_batch[SpriteBatch]
+
+    for t, tile in ipairs(layer.data) do
+        tile = tile - 1
+        if tile >= 0 then
+            local tile_x = ((t-1)%layer.width)
+            local tile_y = math_floor((t-1)/layer.width)
+            local x = ((t-1)%layer.width)*16 + 8
+            local y = math_floor((t-1)/layer.width)*16 + 8
+
+            local ent = nil
+            local done = false
+            local handler = tile_to_handler[tile]
+            if handler then
+                done = true
+                local skip = false
+                ent, skip = handler(self, tile, x, y, layer)
+                if skip then
+                    done = false
+                end
+            end
+
+            if not done then
+                ent = self:add_entity(Entity:new({
+                    x=x,
+                    y=y,
+                    layer=entity_layers[layer.name],
+                    tag='solid'
+                }))
+
+                if layer.name == 'Left' or layer.name == 'Right' then
+                    ent:add_comp(Sprite, {
+                        file='res/usable_files/diab_final_tileset.png',
+                        w=16,
+                        h=16,
+                        frame=tile
+                    })
+                else
+                    batch.batch:add(sprite_batch.quads[tile + 1], x, y, 0, 1, 1, 8, 8)
+                end
+
+                if layer.name == 'Collision' then
+                    local done = false
+                    if tile_x > 0 and tile_x < layer.width - 1 and
+                       tile_y > 0 and tile_y < layer.height - 1 then
+                        done = self:check_adjacent_tiles(tile_x, tile_y, layer.name, layer.width, layer.height)
+                    end
+
+                    if not done then
+                        ent:add_comp(Object, {
+                            w=16,
+                            h=16,
+                            solid=true
+                        })
+                    end
+                    -- Collision tiles are static — their Objects live in the spatial
+                    -- hash for collision detection but the entities themselves serve
+                    -- no purpose in scene.entities. Flag them for removal after load.
+                    -- update is nooped as a belt-and-braces safety measure in case
+                    -- anything re-adds them to the loop before removal completes.
+                    ent.update = function() end
+                    ent._static_collision = true
+                elseif layer.name == 'Right' then
+                    local done = false
+                    if tile_x > 0 and tile_x < layer.width - 1 and
+                       tile_y > 0 and tile_y < layer.height - 1 then
+                        done = self:check_adjacent_tiles(tile_x, tile_y, layer.name, layer.width, layer.height)
+                    end
+
+                    if not done then
+                        ent:add_comp(Object, {
+                            w=16,
+                            h=16,
+                            solid=true
+                        })
+                    else
+                        ent:add_comp(Object, {
+                            w=16,
+                            h=16,
+                            solid=true,
+                            no_check=true,
+                        })
+                    end
+                elseif layer.name == 'Left' then
+                    local done = false
+                    if tile_x > 0 and tile_x < layer.width - 1 and
+                       tile_y > 0 and tile_y < layer.height - 1 then
+                        done = self:check_adjacent_tiles(tile_x, tile_y, layer.name, layer.width, layer.height)
+                    end
+
+                    if not done then
+                        ent:add_comp(Object, {
+                            w=16,
+                            h=16,
+                            solid=true
+                        })
+                    else
+                        ent:add_comp(Object, {
+                            w=16,
+                            h=16,
+                            solid=true,
+                            no_check=true,
+                        })
+                    end
+                end
+            end
+
+            if ent then
+                -- OPTIMIZATION: Integer key matches GameScene:lookup() — no string alloc.
+                self.lookup_table[(x * 10000 + y) * 100 + layer_ids[layer.name]] = ent
+                if layer.name == 'Right' then
+                    if ent[Object] then
+                        ent[Object]:set_position(ent.x, ent.y)
+                        ent.right = true
+                        ent[Object].check_list = Object.object_and_player_list
+                        ent[Object].joint = self.right_objs
+                        table_insert(self.right_ents, ent)
+                        table_insert(self.right_objs, ent[Object])
+                    end
+                elseif layer.name == 'Left' then
+                    if ent[Object] then
+                        ent[Object]:set_position(ent.x, ent.y)
+                        ent.left = true
+                        ent[Object].check_list = Object.object_and_player_list
+                        ent[Object].joint = self.left_objs
+                        table_insert(self.left_ents, ent)
+                        table_insert(self.left_objs, ent[Object])
+                    end
+                end
+            end
+        end
+    end
+end
+
+register_tiles({90}, function(scene, tile, x, y, layer)
+    scene.has_goop = true
+    scene.kill_height_offset = y
+end)
+
+register_tiles({77, 78, 76, 79}, function(scene, tile, x, y, layer)
+    if tile == 76 then
+        scene.max_bot = y + 8
+    elseif tile == 79 then
+        scene.max_top = y - 8
+    elseif tile == 77 then
+        scene.max_left = x - 8
+    else
+        scene.max_right = x + 8
+    end
+end)
+
+
+sections = {[1]={}}
+section_map = {}
+
+function find_section_index(level, greed)
+    greed = greed or 0
+    local section = section_map[level+greed*0.01]
+    print(level, greed, section)
+    print(level, greed, section)
+    for i=1,#sections[section] do
+        if sections[section][i] == level+greed*0.01 then
+            return i, section
+        end
+    end
+end
+
+local c_pal = 0
+local c_song = 'LockedUp.wav'
+local powerups = {}
+local c_section_name = ''
+
+function generate_lvl_info()
+    local section = 1
+    local lvl_count = 0
+    local setup_level_picker = nil
+    setup_level_picker = function(level, i)
+        if type(level) == 'string' and level:sub(1, 1) == 'b' then
+            return level
+        end
+       
+        if level.pal and level.pal ~= c_pal then
+            table_insert(sections[section], {'lock', beat=level.beat})
+            print(section, i)
+        end
+
+        if level.pal and math_floor(level.pal) ~= c_pal then
+            table_insert(sections[section], 'end')
+            section = section + 1
+            sections[section] = {}
+            table_insert(sections[section], 'start')
+        end
+        table_insert(sections[section], i)
+        section_map[i] = section
+        
+        if not level.name then
+            level = {name=level, pal=c_pal, current_song=c_song, powerups=powerups, section_name=c_section_name}
+        else
+            level.current_song = c_song
+
+            c_section_name = level.section_name or c_section_name
+            level.section_name = c_section_name
+            c_pal = level.pal or c_pal
+            c_pal = math_floor(c_pal)
+            level.pal = c_pal
+            c_song = level.next_song or level.song or c_song
+            if level.powerups then
+                levels[i-1].bag_level = true
+                powerups = {}
+                for k, v in pairs(level.powerups) do
+                    powerups[k] = v
+                end
+            else
+                level.powerups = powerups
+            end
+
+            if level.greed then
+                level.greed_up = greed_up
+                section = section + greed_up
+                sections[section] = {}
+                table_insert(sections[section], 'start')
+                for t,l in ipairs(level.greed) do
+                    levels[i+t*0.01] = setup_level_picker(l, i+t*0.01)
+                end
+                table_insert(sections[section], #sections[section], {'lock', beat=#sections[section] - 2})
+                print(sections[section][#sections[section]])
+                table_insert(sections[section], 'end')
+                greed_up = greed_up + 0.01
+                section = math_floor(section)
+            end
+        end
+
+        -- local gs = GameScene:new({lt=level})
+        -- gs:begin()
+        -- print(inspect(gs))
+        -- print(gs.coin_count)
+        -- total_coins = total_coins + gs.coin_count
+        -- lvl_count = lvl_count + 1
+
+        return level
+    end
+
+    local LEVEL_TOTAL = #levels
+    for i=1,LEVEL_TOTAL do
+        levels[i] = setup_level_picker(levels[i], i)
+    end
+
+    print(levels[2])
+    print('TOTAL ', total_coins)
+    print(lvl_count)
+end
+generate_lvl_info()
+
+function coins_gotten()
+    local gotten_coins = 0
+    for key, coins in pairs(SAVE.COINS) do
+        gotten_coins = gotten_coins + tablelength(coins)
+    end
+    print('GOTTEN COINS', gotten_coins)
+
+    return gotten_coins
+end
\ No newline at end of file
